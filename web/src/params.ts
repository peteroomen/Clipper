// Parameter ids — must mirror clipper::ParamId in
// core/include/clipper/Processor.h and the worklet's PARAM_GAIN.
export const PARAM_GAIN = 0;

// Absolute URL of the copied worklet module (served from public/generated).
export const WORKLET_URL = '/generated/clipper-processor.js';
