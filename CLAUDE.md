# Clipper / Tonesmith — Agent & Development Guidelines

This file is read by Claude Code at the start of every session. It captures conventions that keep human and AI contributions consistent.
**Do not skip it. Do not start writing code before completing the pre-session checklist below.**

**Ship small slices. One slice = one plan = one branch = one PR = one session.** This is the core working rhythm, shared across all of Peter's projects (riff, budget-app, pixel-horizons). See the **Pre-Session Checklist**, **Plan File Format**, and **Post-Session Checklist** below — they are not optional.

**One rule specific to this project, above all others: audio quality is the product.** A change that speeds something up or tidies something must be provably fidelity-neutral, or it is a tone change and needs to be argued as one. "Measured" beats "sounds fine to me" — see **Measure, Don't Assert**.

---

## Tech Stack

| Layer            | Choice                                                                                  |
| ---------------- | --------------------------------------------------------------------------------------- |
| DSP core         | Portable C++17, zero platform/OS/browser deps (`core/`) — CMake ≥ 3.16, clang++ or g++   |
| WDF library      | `chowdsp_wdf`, header-only, pinned by commit SHA via CMake `FetchContent`                |
| Web engine       | Emscripten → WASM ES module + `AudioWorklet` (`web/worklet/`, `web/public/generated/`)   |
| Web app          | Vite 5 + React 18 + TypeScript                                                          |
| Assistant        | Anthropic Claude via a zero-dependency Node proxy (`server/`), SSE streaming + tool use  |
| Desktop          | Electron 33 wrapper running the same proxy in-process (`electron/`)                      |
| Native plugin    | JUCE 8.0.4 (pinned tag, FetchContent) — Standalone + VST3 + AU (`native/`)               |
| Tests            | `ctest` (plain-assert, no framework) · Playwright `OfflineAudioContext` · `node --test`  |
| Package manager  | npm (per-workspace: root, `web/`, `electron/`)                                           |

---

## Key Docs

| Doc              | Path                            | Purpose                                                                 |
| ---------------- | ------------------------------- | ----------------------------------------------------------------------- |
| Roadmap          | `ROADMAP.md`                    | Build order and milestone definitions — the source of truth for "next"   |
| Engineering log  | `docs/DEVELOPMENT.md`           | Numbered sections (§N) — the long-form record of every milestone         |
| Work logs        | `docs/work/`                    | Per-session plan files — read the most recent before starting            |
| Decisions        | `docs/decisions/`               | Architecture decision records (ADRs)                                     |
| Audits           | `docs/audits/`                  | Read-only review passes; findings feed the roadmap                       |

`docs/DEVELOPMENT.md` is the deep record and is cited by section number throughout the code (e.g. "see docs §25"). When you change behaviour that a section documents, update that section in the same slice — a stale §N is worse than none, because the code cites it as justification.

---

## Current State

> **Update this section at the end of every session.**

- **In flight (2026-08-25): the "Decade" TEN-BAND GRAPHIC EQ — M13.6, the board's FIRST EQ and the FIRST PEDAL WITH MORE THAN THREE CONTROLS** (docs **§72**, no ADR, `docs/work/2026-08-23-mxr-10-band-eq.md`, branch `claude/roadmap-priorities-kok3vx`). Pedal type `eq`, slot **14** (`PEDAL_TYPE_COUNT` → 15), SILVER accent, wordmark "Decade", `EQ Nº1 · TEN BAND`. Wired end to end (core, C ABI `eq_*`, worklet, web face + a new `Fader` widget + tokens + assistant + an `eq worklet` spec, native engine + APVTS + `PedalCard`, and its own case in `identical_core_test`). **ALL FIVE GOLDENS UNCHANGED, nothing blessed** (0 files changed under `core/tests/goldens/`). Core ctest **38 → 40 entries, 40/40**; repo XFAIL ledgers **6 → 7**. Native **3/3**; Playwright **86**; node 15/10/12; electron 20; WASM artifact rebuilt (**109** hashed inputs, was 107).
- **THE ROADMAP'S OWN COST ESTIMATE WAS WRONG, in the same way §67's and §69's entries were wrong about their slices.** It says *"10 biquads, trivial DSP"* — true about the DSP, false about the slice. **Every pedal on this board was exactly three parameters, and that was the WIRE FORMAT**: the worklet dispatched three literal `_set_param(handle, 0|1|2, …)` lines in two places, `PedalParams` was three named fields feeding the serializer and the assistant's clamped allowlist, `PedalFace.knobs` was documented "1..3", and the native `Params` carried three floats per pedal. A ten-band EQ is TWELVE controls. **The C ABI was never the constraint** — `*_set_param(handle, int id, float)` always took any id — so the core side really is cheap and everything upstream was not.
- **PHASE A SHIPPED FIRST AND ALONE, and its bar was BIT-IDENTITY proved by the strongest evidence available: after a full rebuild `web/public/generated/clipper.js` came back BYTE-IDENTICAL to its pre-slice state** — the compiled engine is bit-for-bit the same binary, so no DSP could have changed by construction rather than by argument. The widening also came out SMALLER than planned: two of the twelve are ordinary slot reuse in the house style (the phaser takes slot 0 as SPEED, the wah as POSITION), so **GAIN rides slot 0 and VOLUME rides slot 2** and only the ten bands needed new ids (3..12). `PedalParams` gains ten OPTIONAL fields, mirroring **`AmpParams`' own established shape** (per-voice optional fields since M9.4) rather than inventing a second idiom — absence IS "this is a three-slot pedal". One type was NARROWED rather than loosened: `KnobSpec.param` is now `CoreParamName`, so `params[k.param]` stays a plain `number` instead of acquiring a `?? 0.5` fallback that could mask a face naming a param its pedal lacks.
- **THE RESEARCH CHANNEL WAS FAR BETTER THAN §64's, and it is a REAL NETLIST.** `WebFetch` is **EGRESS_BLOCKED on every host tried** — including the manufacturer's own manual PDF — but github.com is reachable and **`Cushychicken/ltspice-guitar-pedals`, the SAME repo §59 parsed for the Dyna Comp and §66 for the RAT, carries a complete annotated 7-band Boss GE-7**. Parsed by coordinate. **Nine sourced facts, six netlist-grade** (§72.1), including the file's OWN annotation of the slider law: *"10k nominal, linear taper. When param=10k, filter shorts to Eq_Feedback."* The design equation **`L = R1·R2·C2` reproduces all six of the GE-7's gyrator bands' labelled centres to −4.41…+3.22 %**, which is what confirms the topology. TRANSCRIBED: the leg topology, the equation, the shared 330 Ω, the slider law, the summing pair. DERIVED: the ten per-band L/C1. **RECONSTRUCTED and §57's rule applies verbatim: the ±12 dB range and the GAIN/VOLUME laws.**
- **ONE STRUCTURAL ERROR, caught by measuring, and both versions look identical at a glance. THE LEG RUNS FROM THE POT WIPER TO THE VIRTUAL GROUND, NOT TO GROUND.** The first draft had it to ground, which puts ten pots end-to-end across the summing node's feedback: **−20 dB of flat insertion loss with essentially NO equalisation** (a full slider moved the response 0.7 dB). Written into `EqModel.h`'s banner because "a pot with a resonant leg on the wiper" describes both.
- **THREE PROPERTIES ARE CONSEQUENCES OF THE TOPOLOGY, NOT COEFFICIENTS.** (1) **FLAT AT CENTRE IS EXACT** — at x = 0.5 the wiper sits at (Vin+Vout)/2, identically zero while Vout = −Vin, so the legs inject NOTHING: **+0.00000 dB at six probes, passthrough worst |out−in| 1.7e-19**. Not a calibration and must not become one; it is also why every slider defaults to 0.5 on all three front ends. (2) **BOOST AND CUT ARE EXACT MIRRORS** (+12.000/−12.000 at 250 Hz). (3) **PROPORTIONAL Q**, because the POT is the damping element. Band centres land at **+0.00 %** on all ten at 44.1/48/96/192 kHz via frequency prewarping — legitimate rather than a fudge because the per-band values are DERIVED and the design intent is "resonate at f0".
- **ONE XFAIL, and it is a PUBLISHED figure rather than this slice's own target.** `eq-proportional-q-shallow`: the narrowing from +3 dB to full boost measures **1.514× against a published ~3×** (0.5035 oct vs 0.3326). **Reported, not fitted, and the cause is structural: the two published figures the model is pinned to OVER-DETERMINE it** — within this topology max boost and full-boost bandwidth are BOTH governed by the single lumped leg loss. A smaller loss moves the ratio the right way (**2.37×** at the GE-7's own sourced 330 Ω) and takes the boost to **+29 dB**, which is why it cannot simply be lowered. **The weak band interaction is the SECOND SYMPTOM of the same defect, not a separate one** (+0.004 dB excess over dB superposition, against a published "significant") — interaction comes from adjacent bands OVERLAPPING, and a +3 dB bandwidth half the published width overlaps less. A slice fixing the XFAIL must re-measure the interaction too.
- **THREE MEASUREMENT BUGS, all in the TEST, all worth reading before the next filter bar.** (1) **A 64-cycle analysis window at 16 kHz is 192 samples, over which a high-Q leg under-reads by 3.5 dB** — the top band measured +8.4 dB instead of +12 and looked exactly like a broken band; a prewarp fix was written and a four-rate sweep run before the PROBE was suspected. **The rate sweep is what exposed it: the shortfall was rate-INDEPENDENT, which a discretization error cannot be.** Same family as §60's rectangular sidelobe and §70's uncorrected Hann gain. (2) Comparing `reset()` against a model still RAMPING its smoothers measured the ramp (3.0e-02); both now set sliders before `prepare()` so §35's deferred snap lands them. (3) **A 20 s silent tail reports 1.2e-26 and fails an `== 0.0` bar while the model works** — the 31.25 Hz leg at full boost is the slowest ring-down and reaches exact zero at **60 s** (1.95e-06 at 1 s, 5.05e-24 at 5 s). §60 measured the same shape; no plateau and no marginally stable mode, and that was CHECKED because a trapezoidal inductor companion has an undamped z = −1 mode if its branch loss is left out.
- **THE PREDICTED NUMERICAL TRAP WAS REAL AND WAS DESIGNED OUT UP FRONT.** A 31.25 Hz section at 96 kHz sits at a pole radius over 0.998 — §56.4's exact shape, which put an audible −73 dBFS hiss on the GOLD's divider in code that looked fine and was invisible to every single-bin bar. Both leg networks carry **double** state and the floor measures **−137.6 / −146.6 dBFS** at 48 / 96 kHz against the project's own −120 gate. **`Biquad` was NOT widened** — it is shared with all three valve tone stacks and the cab IR generator, so changing its state type would move every amp's renders.
- **Named follow-ups this slice leaves:** the **frequency-independent leg loss** owns the XFAIL *and* the weak interaction (one root cause, two symptoms) · **find the ten-band's own schematic** · **a native FADER widget** — the web face draws ten vertical faders, the native card draws twelve knobs in a 2×5 grid because the native kit has no fader; parameters and behaviour are identical and the divergence is recorded at the face's definition · the GE-7's **first-order top band** is not carried across · **THIS PEDAL EXACTLY FILLS THE PACKED CHAIN WORD**: `PluginProcessor.cpp` asserts `kChainField * (kMaxChain + 1) <= 64` with `kChainField = 4`, which at `PEDAL_TYPE_COUNT = 15` is `4 × 16 = 64`, so **a SIXTEENTH pedal type fires that static_assert**, exactly as the EIGHTH fired the `uint32` one it replaced.
- **In flight (2026-08-25): the FENDER TWEED CHAMP 5F1 — M10.10, the EIGHTH amp voice and the FIRST SINGLE-ENDED OUTPUT STAGE IN THE PROJECT** (docs **§73**, no ADR, `docs/work/2026-08-25-fender-champ-5f1.md`, branch `claude/roadmap-review-build-planning-l0eov1`). Amp voice **7** (`champ`), wordmark **"Cadet"**, `Combo Nº8 · Tweed`, lacquered-wheat accent, plus a synthesised **`tweed8`** 1×8 open-back cab. Wired end to end in one slice (core, C ABI, worklet-transparent, web face + tokens + assistant + a face spec, native engine + APVTS + editor face). **ALL FIVE GOLDENS UNCHANGED, nothing blessed** — no shared class was edited. Core ctest **38 → 39 entries, 39/39** measured on this branch alone; **MERGED with M13.6's EQ the tree measures 41/41** (a third pair of parallel slices — re-measure, do not reconcile by hand). `clipper_champ_tests` registers **NO xfail ledger** (zero known-bad properties). Native **4/4**. Node 15/10/12, electron 20. WASM artifact rebuilt (**113** hashed inputs, was 107).
- **THE ROADMAP'S OWN ENTRY WAS WRONG AND THE SLICE CORRECTED IT.** M10.10 read *"Single-ended class A 6V6, **ONE tone control**, no negative feedback"*. **A tweed 5F1 has NO tone control at all** — one knob, VOLUME; Fender did not put one on a Champ until the 1964 blackface AA764. So this is the first amp voice here with **no tone stack whatsoever**, its face has TWO knobs (the second is §19's reverb convenience), and the assistant is taught to answer an EQ request with the guitar's tone knob, a pedal or the cab rather than a knob that is not on the panel. Same class of error as §69's and §67's roadmap framings.
- **EVERY OTHER POWER SECTION HERE IS PUSH-PULL BEHIND A PHASE INVERTER; THIS ONE IS ONE TUBE.** Three properties fall out and each is a hard assert. **(a) NOTHING CANCELS THE EVEN HARMONICS** — h2 **−14.84 dBc** against the balanced Twin's **−39.72** on the identical stimulus, **24.88 dB of contrast**, and h2 > h3. That is topology; no re-voice of existing machinery can produce it. **(b) AUDIT FINDING 9 FROM THE OTHER SIDE** — `Vp = rail − (i − iq)·Rload` is a SINGLE-ENDED relation, exactly correct here and approximate in the push-pull amps, so the plate reaches the knee at **88.4 mA** (inside a real 6V6's capability) where finding 9 measured **530 mA from ONE EL34**, i.e. plate-load saturation is a live clipping mechanism here for the first time. **(c) THERE IS NO NEGATIVE FEEDBACK AT ALL** — the AC30's anti-NFB catcher verbatim: open- vs closed-loop renders **bit-identical, 0 of 24000 samples differ**.
- **THE 6V6 CARD IS DERIVED, AND THAT CLOSES AUDIT FINDING 10 ON A THIRD TUBE.** Two published Koren 6V6 fits are reachable and **disagree substantially**. Evaluated against the RCA/TAD datasheet at TWO points before a line of code: fit B's shape and `kg1` land the plate within **2–3 % at two different Vp AND Vg2 with errors of OPPOSITE SIGN** (a real external check, not a self-fit) while **both** fits put the screen **2.1–2.2×** over the datasheet. So B's `mu/ex/kg1/kp/kvb` ship UNMODIFIED — deliberately NOT trimmed to hit P1 exactly, which would worsen P2 — and only **`kg2` is derived, 4500 → 10148.2**. Finding 10's table reads EL34 0.102 / 6L6 0.210 / EL84 0.363; the 6V6 continues it exactly, and **at this amp's own idle the published fit puts screen dissipation at 2.53 W against the 6V6GT's 2.75 W RATING** — the AC30's exact "exceeds its rating at idle" pathology. Ours: **1.12 W**. **Residual reported not fitted:** one `kg2` cannot match both points (P2's screen 1.63× high) because **the Koren screen law has no `Vp` dependence** — finding 10's own closing paragraph, and the mechanism behind real screen sag. **Free corroboration:** `pedalkernel`'s 6L6GC row is byte-for-byte this repo's shipped `Tube6L6Params`.
- **THE SECOND ABSOLUTE REFERENCE IS FENDER'S OWN MEASURED 5F1 VOLTAGES — only the SECOND amp here to have one** (§69's Mesa was the first). Fender publish 19 V across the 470 Ω cathode (= 40.4 mA), a ~305 V plate node and 37 mA of plate current. **EXACTLY ONE constant is fitted to it** (`kVsupply`, pinning the 305 V node); everything else is the device card's own prediction: **cathode 18.892 V vs 19 (0.9943×), Ip 36.280 mA vs 37 (0.9805×), Ik 40.196 mA vs 40.4 (0.9950×), Vpk 286.108 V vs 286 (1.0004×)**. Plate dissipation 10.38 W of 14 (74 % — a Champ runs hot, correctly).
- **THE POWER IS REPORTED, NOT CHASED (§57.3), and §42's criterion is UNSATISFIABLE here.** The power section's **own sine ceiling is 5.17 W into 8 Ω** against a rated ~5 W and the datasheet's 4.5 W @ 10 % THD — so the card and the load line are right. The **composed cranked amp makes 3.89 W**, because the preamp hands it a blocking-limited waveform (crest factor ~2.5). The sweep is recorded: 0.80 → 3.896, 1.00 → 3.890, and **above 1.0 the power DECLINES** (2.00 → 3.788, 3.00 → 3.772) — §57.9's exact signature. **`kInterstageScale` is an UN-FITTING to 1.0** and here it is simply the circuit: a real 5F1 has NO interstage divider. **Do NOT close the 1.4 W with a higher `kVsupply` or a softer grid coupling.**
- **IT BREAKS UP ALMOST IMMEDIATELY AND CLEANS UP WHEN YOU PLAY SOFTER — the second half is the property the amp exists for.** At the house 0.15 V probe: VOL 0.05 → **3.89 %**, 0.10 → **7.55 %**, 0.30 → **28.61 %**, i.e. **14.11× the Twin's THD at the same volume**. At a fixed VOL 0.20: hard pick **16.63 %**, soft pick **5.86 %** = **2.84×**. With no master volume and no tone stack, how hard you pick IS the other half of the tone control, and the assistant coaches "play softer" before it touches a knob.
- **THE ONE KNOB GETS ITS OWN PARAM ID (17, `champVolume`), AND THE REASON IS THE DEFAULT.** §63.14 measured the cost of getting this wrong — the Rockerverb inherited `AMP_KNOB_DEFAULTS`' JCM values and "the amp opens at the wall". **A Champ at the shared volume default of 0.40 measures ~50 % THD.** Shipped **0.20**, derived from this amp's own geometry as the EDGE OF BREAKUP. It is also genuinely a different function: on every other voice VOLUME is an output level AFTER the preamp; this one sits BETWEEN the two preamp triodes with no master downstream. The VOLUME pot is **its own grid leak**, so its law is the **BARE** audio taper (11.92029 % at half rotation — §68's corrected figure), not a loaded one.
- **A NEW CAB, and it is the only voice whose real speaker is SMALLER than every cab that preceded it.** `tweed8`, synthesised in the §15 modal house style. Measured (−6 dB corner re each cab's own peak; tilts re 400 Hz): **tweed8 198.3 Hz / +3.04 / −6.02** against brit412 75.0 / −0.04 / −19.06, orange412 103.2 / +1.24 / −13.98, clean212 104.7 / +2.45 / −11.01. **1.9–2.6× the low corner of any 12" cab, the most upper-mid-forward, and the one that keeps the most top** — all asserted as an ORDERING so a re-voicing of any single cab cannot break them silently. **APPENDED at 3 (C ABI) / 4 (native `CabChoice`)**, never inserted (§57.11's stored-session divergence).
- **ONE OVERSAMPLING DOMAIN FROM THE FIRST COMMIT** (§63.14 applied up front as §69 did): latency **72 samples / 1.50 ms**, against the **216** measured for per-stage domains. **Resting state decided BY MEASUREMENT (ADR 006) and it is the §69 case:** the three zero-resting states decay then **PLATEAU** (otLp/otHp 2.7159e-12, vCc 5.4341e-11 — identical at 1/4/8/20/41 s) because `TriodeStage`'s grid Newton exits at a residual tolerance and re-excites them, ~27 decades above subnormal. So the flushes are **guard-rails, not fixes**, and the test asserts what the policy is FOR — **no subnormal float leaves the model, 0 over the whole silent tail** — plus that the plateau is SETTLED, rather than a zero this amp does not have.
- **A REAL BUG FOUND BY WRITING THE WEB SPEC, PRE-EXISTING ON `main` AND FIXED HERE (§73.7):** `web/src/components/Board.tsx`'s `AMP_TYPE_LABEL` was a `Record<string, string>` with **no `mesa` entry**, so **M10.4's Dual Rectifier has been rendering as a BLANK item in the amp menu** — `AMP_TYPE_LABEL[t]` returned `undefined` and React drew nothing. A `Record<string, …>` accepts any key, so nothing could catch it. Retyped `Record<AmpType, string>`: a missing voice is now a **BUILD ERROR**. Same structural fix `AMP_MODEL_COUNT` made natively (§71) and the same class of defect as §61.10's `kFaces` and §67.10's `pedalMenuLabel` — **the fourth time a keyed table has silently lost an entry, and the second fixed by making the table's type carry the count.**
- **The research channel, honestly: a PARTIAL TRANSCRIPTION — better than §57's OR120 (full reconstruction), worse than §69's Mesa (factory sheets).** `robrobinette.com`, `ampbooks.com`, `frank.pocnet.net` and `prowessamplifiers.com` all EGRESS_BLOCKED; GitHub's **code search API is repository-scoped** (public `git clone` works, which is how the device cards were read). **RULE ZERO held** on `valtyr/rust-5f1` — another vendor's model of this same amp — using §63's line: a **netlist is a circuit description**, a **tube fit or solver is someone's model**. `netlist.rs` was read for R/C values; `tube_models.rs`, `solver.rs`, `sim.rs`'s loop and the whole `dsp/` directory were **NOT opened**, and no constant here is justified by agreeing with that project's output. **Two corrections to that source, both independently sourced and both shipping:** it OMITS the 6V6's 25 µF cathode bypass cap (unbypassed, that 470 Ω is strong local degeneration and a different amp), and its 100 Ω supply lands the plate node at 320 V against Fender's measured 305 (replaced by §55's Thévenin form: `kRrect5Y3` **480 Ω** SOURCED from the 5Y3GT's published 60 V at 125 mA, plus a NAMED 200 Ω winding reconstruction — **680 Ω against the AC30's 134.6**, and with the 16 µF reservoir a **10.9 ms** supply RC, which is why a cranked Champ blooms).
- **Named follow-ups this slice leaves:** **the EDITOR still has no automated behaviour test** — §71's gap, now covering one more face (`updateAmpFace()`'s `case 7` is covered by review) · a **Vibro-Champ** is cheap now (`OptoTremolo` exists) · **audit findings 9 and 10 on the EXISTING push-pull amps** (this slice built the load line correctly for a genuinely single-ended stage and derived ONE screen fit; touching the others moves four goldens and is its own slice) · **the Koren screen law's missing `Vp` dependence**, absent in every pentode here · **`kRptSecondary` (200 Ω) is a reconstruction** — find a Champ PT winding resistance and the supply is fully sourced.
- **In flight (2026-08-19): the MESA WAS UNREACHABLE IN THE PLUGIN — a wiring defect M10.4 shipped** (docs **§71**, no ADR, `docs/work/2026-08-19-native-mesa-voice.md`, branch `fix/native-mesa-voice`). Found by the owner on a one-day-old build: *"I can see cellar, the drop pedal. I can't see my mesa."* **The DSP was never broken.** §69 wired `MesaAmp` into `ClipperEngine` correctly and stopped at the engine boundary: `kAmpModelChoices` had SIX entries and the Mesa is the seventh, so the `ampModel` Choice parameter **could not express the value 6** — no host, no automation lane and no UI could ever select it. Plus no APVTS params for MODE/RECT/POWER (frozen on their defaults) and `updateAmpFace()` clamping `jlimit(0, 5, …)` with no `case 6` (so the panel would have silently relabelled itself Clean 120). **Core untouched, so NO golden can move and NO WASM rebuild is needed.** Native **3/3 → 4/4**.
- **WHY THE WHOLE SUITE STAYED GREEN, and it is the transferable lesson.** `identical_core_test` compares the plugin against a reference chain per voice — but it can only test a voice it can **SELECT**, and no parameter could name this one. **A test that enumerates what the system OFFERS cannot notice something the system never offers.** The bug lived in the gap between *"every voice I can name renders correctly"* and *"every voice that exists can be named."* Same shape as §61.10's `kFaces` ordering bug and §67.10's missing `case PEDAL_OPTO` — a keyed table right and a switch incomplete — on the amp side for the first time.
- **THE STRUCTURAL FIX IS `AMP_MODEL_COUNT`, and the Mesa is four lines of it.** The voice ids were a **COMMENT** in `ClipperEngine.h`, so nothing could compare the engine's voice count against the plugin's. They are now an `enum AmpModel` with a count, read by a `jassert` in `createParameterLayout()`, by `updateAmpFace()`'s clamp (which was a hardcoded `5`) and by the new test's headline bar. **A voice added to the engine and not the plugin is now a FAILING TEST rather than an invisible amp.**
- **THE MEASUREMENT IS REACHABILITY, NOT TONE — a render comparison could not have been the bar**, because the pre-fix editor cannot PRODUCE voice 6 to compare. New `clipper_amp_voice`: the Choice parameter must offer exactly `AMP_MODEL_COUNT` (**engine 7, parameter 7**, was 6); "Dual Rectifier" specifically present; the Mesa render differs from the Clean 120 (**tail rms 0.1637 vs 0.0414**); and **MODE is checked STRUCTURALLY rather than by level** — §69 measured sheet `mbdr7` opening the power amp's loop in the two MODERN modes, so PRESENCE must be dead there (**0.0000 dB**) and live elsewhere (**0.3109 dB**). A wrong mode table can pass "the output changed"; it cannot pass a bar whose two halves disagree in opposite directions.
- **A BAR THAT LOOKED SUFFICIENT WAS NOT, and the perturbation proved it.** Reverting `kAmpModelChoices` to six entries takes **4 of 6 bars RED**, restore GREEN — but *"the Mesa render differs from the Clean 120"* stayed **GREEN**, because a request for index 6 against six choices **clamps to 5, the Rockerverb**, a real and different amp. That bar catches a `default:` fall-through and nothing else. **The bars that caught the shipped defect are the COUNT bar and the MODE bar.** Reported (§71.4) rather than quietly kept.
- **§70's NAMED GAP IS CLOSED in the same slice**, since it was the same missing reference: `identical_core_test`'s driver now builds a `MesaAmp`, and the drop pedal's board case moved from the JCM800 to **`Cellar -> RAT -> Mesa` with ALL THREE Recto switches off-default** (RED VINTAGE, 5U4, SPONGY) — the only case driving them end to end. **max |plugin − ref| = 0.000e+00.** The switch quantization is written out AGAIN in the driver rather than calling `ClipperEngine::applyMesaSwitches`: that driver's job is to be an INDEPENDENT reconstruction, and sharing the helper would make the comparison circular.
- **The panel: NINE knobs**, the busiest on the board, crimson (`--accent-mesa` verbatim, both themes). **No reverb and no bright — both circuit facts** (a reverb tank makes it a Trem-O-Verb, a different amp; a Recto has no bright switch). All three switches are **KNOBS, not switch widgets**, mirroring the web: MODE has FIVE positions where `ModeSwitch` does two and three, and the core already quantizes the 0..1 value. **PRESENCE is shown although it does nothing in the two MODERN modes** — hiding it per mode was considered and rejected, because the knob is real and the AMP is what makes it inert.
- **CI: `clipper_amp_voice` was added to BOTH the build list and the ctest filter.** `ci.yml`'s own comment records `clipper_cab_state` landing red in PR #35 for exactly that mismatch — a registered test that is not built reports "Not Run" and exits 8.
- **Named follow-up, and it is now the only hole of this class: the EDITOR still has no automated behaviour test.** CI compiles `PluginEditor.cpp` and nothing exercises it, so `updateAmpFace()`'s `case 6` is covered by REVIEW rather than by a test. The reachability test proves the parameter and the engine agree — which is what made the amp invisible — but **a panel drawing the wrong knobs would still ship green.**
- **In flight (2026-08-19): the "Cellar" DROP-TUNE — M13.10, the lineup's FIRST PITCH EFFECT and a WHOLE NEW DSP FAMILY** (docs **§70**, no ADR, `docs/work/2026-08-18-drop-pitch-shifter.md`, branch `feat/drop-pitch-shifter`). The second half of the owner's 2026-08-17 ask (*"the mesa finally and the drop-pedal … I can play AIC down tuned half step"*) — §69 is the amp. Pedal type `drop`, slot **13** (`PEDAL_TYPE_COUNT` → 14), GRAPHITE-CYAN accent, wordmark "Cellar", `PITCH Nº1 · POLY`. Wired end to end in one slice (core, C ABI `drop_*`, worklet, web face + tokens + assistant + a `drop worklet` spec, native engine + APVTS + `PedalCard`, and its own tenth case in `identical_core_test`'s plugin driver). **ALL FIVE GOLDENS UNCHANGED, nothing blessed** (it is in no golden rig and touches no other model). Core ctest **36 → 38 entries, 38/38**; repo ledgers **5 → 6**. Native **3/3**; Playwright **85 passed**; node 15/10/12; electron 20; WASM artifact rebuilt (**107** hashed inputs).
- **THE ACCEPTANCE SHAPE IS DIFFERENT FROM EVERY VOICE BEFORE IT, AND THAT IS THE POINT.** A DigiTech Drop is a **DSP box** — there is no schematic, no netlist and no component values, so §57's "find the schematic" rule has **no target**. What replaces an external reference is arithmetic: the interval is `2^(-n/12)` **exactly**, so the bars are **cents accuracy, latency and a measured artifact floor**. **Measured 0.00 cents at all nine detents on single notes and 0.00 / 0.25 on power chords.** Through the whole WEB path (zero crossings on the rendered worklet output): DROP 1 **207.65**, DROP 4 **174.61**, OCTAVE **110.00 Hz** — exact.
- **IT IS POLYPHONIC BY CONSTRUCTION, NOT BY TUNING.** The mechanism is RESAMPLING — read a delay line at a position advancing `r` samples per written sample, which scales EVERY frequency present — so it **never forms an opinion about pitch**, which is exactly what a tracker-based shifter gets wrong on a chord. The polyphony bar is a consequence of the algorithm and is asserted anyway, because a wrong ratio or a mis-wrapped tap would still break it. **The reusable deliverable is the PRIMITIVE:** `core/include/clipper/dsp/PitchShifter.h` is `DelayLine`'s second consumer and knows nothing about semitone selectors, dry blends or footswitches, so a future harmoniser or detune can hold one without inheriting a drop pedal.
- **TWO STRUCTURAL ERRORS, both found by measuring and neither fixed by tuning.** (1) **The TEXTBOOK two-tap arrangement is wrong, not merely suboptimal** — taps a half window apart crossfaded sin/cos across the whole cycle leaves both at ~0.707 gain most of the time with a fixed `W/2` delay between them, i.e. a permanent deep comb: **96 % of the energy landed OFF the harmonics and the apparent pitch was up to 150 cents wrong at an octave.** Fixed structurally — ONE tap live at unity for `1 - kCrossfade` of the window, second tap OFF except during the handover. (2) **A FIXED SPLICE gives a systematic pitch error that scales with the shift** — `r_eff = 0.9697·r + 0.0303`, **+3.1 cents at a semitone and +51.7 at an octave**, with the exact target 60 dB below the peak. **Attribution ISOLATED, not assumed:** with the wrap disabled the resampling measured **exactly 0.000 cents**, so the splice carried all of it. Fixed with **SOLA** (normalised cross-correlation), which stays polyphonic because a correlation search asks "where does this waveform repeat", not "what note is this".
- **THE TRIAD BAR WAS MET WITHOUT BUYING LATENCY, and that is the arithmetic worth keeping.** The first triple read **9.75 / 22.75 cents** because SOLA aligns where the waveform REPEATS and a 4:5:6 triad only repeats at its **composite** period (~48 ms on E2), past a 17.5 ms search span. The obvious fix — a longer window — costs 2× the latency. **Shortening `kCrossfade` 0.25 → 0.10 bought the span for FREE** (the span is what is left of the window after the crossfade): window stays 65 ms, mean algorithmic delay stays **35.8 ms**, and the artifact floor improved in the same change because the comb is present for less of the time.
- **ONE TARGET MISSED, REGISTERED AS AN XFAIL RATHER THAN LOOSENED, AND TWO CANDIDATE FIXES REFUTED.** `drop-triad-spread-at-minus-2`: a major triad at −2 semitones measures a spread of exactly **2.0000 cents** against **this slice's own** 2-cent target — the **5-cent PERCEPTUAL bar is met everywhere with 3.75 cents to spare**. Built and measured: a **wider SOLA span** (55/58 ms) does nothing, and **sub-sample lag refinement** moved it **2.0280 → 2.0291**, i.e. nothing. **A comment claiming the latter fixed it to 0.30 cents was written during the slice and was FALSE; the mechanism was reverted and both failed attempts are recorded in the header so they are not tried a third time.** Diagnosis: one SOLA lag cannot align three partials at once. **The fix is a frequency-domain shifter (`FFT.h` exists) and it owns the entry.**
- **THE ARTIFACT IS NAMED UP FRONT RATHER THAN TUNED AWAY.** During the crossfade — and only then — two taps read `(1-x)·W` apart, which is a comb recurring at the GRAIN RATE `(1-r)/W`: **1.1 Hz at a semitone, 10 Hz at an octave**, so it is far rarer at small shifts, which is what this pedal is mostly bought for. Non-harmonic energy **−18.94 / −18.35 / −17.42 dB at −1 / −5 / −12**, measured per position rather than asserted inaudible — a claim this shape cannot honestly make. The equal-power `cos²+sin²` gain pair is load-bearing: a **linear** pair sums to 1 in AMPLITUDE, which for two decorrelated taps is a **3 dB power dip** at every crossfade — an audible suckout at the grain rate. Do not "simplify" it.
- **ONE KNOB, NO MIX, AND THAT IS A RESEARCH DECISION.** The reference has one control and deliberately no MIX — the box replaces your tuning, so a wet/dry blend would be two guitars a semitone apart. The owner was asked and chose *"go for faithfull"*. Slots 1/2 are carried and UNUSED and the face draws ONE knob (the Ninety's `single` layout), because dead UI is forbidden and inventing a control the reference refuses is worse. AMOUNT is a **9-position ROTARY** the core quantizes (detents at 0, 12.5 … 100): DROP 1…7, OCTAVE, OCTAVE+DRY. **Detent 0 is DROP 1 — one semitone down, E flat standard — so the DEFAULT is the setting it was asked for.**
- **`latencySamples()` RETURNS 0 BY DESIGN and the ~36 ms is REPORTED, not compensated** — the read tap is a **SAWTOOTH**, so there is no single group delay to subtract and reporting the mean would have the host compensate by a number right on average and wrong at every instant (§60's call, different reason). **It is NOT oversampled, and that was checked:** the path is an interpolating read plus a crossfade, linear time-varying with no nonlinearity, so `setOversampling` is accepted and ignored and the render is asserted **BIT-IDENTICAL at 1× and 8×** (0 / 5120) so a later slice cannot quietly add 72 samples of latency for nothing.
- **TWO TEST BUGS AND ONE LEDGER GAP, all in the MEASUREMENT, all worth knowing before the next spectral bar.** (1) The XFAIL predicate keyed on "three partials present", which an E5-plus-octave stimulus also satisfies — so the entry **wrongly XPASSed** on a stimulus that was never the defect; re-keyed on the major third. (2) A **Hann-windowed DFT was read for amplitudes without correcting its 0.5 coherent gain**, under-counting harmonic POWER 4× and reporting the artifact floor as **−1.35 dB** — a number that would have condemned a working pedal; a rectangular Goertzel on an integer number of periods reads **−18.94 dB**. §60 records the MIRROR-IMAGE trap (a rectangular sidelobe read as an alias floor); the rule covering both is *pick the window for the quantity you are measuring, and correct for it*. (3) **`clipper_add_xfail_ledger` was registered but the test's `main` never called `ledgerMain()`**, so the ledger entry ran the whole suite and reported **Passed** instead of `***Skipped` — the known defect would never have shown in a plain `ctest` run, which is the whole point of the ratchet. **Found by measuring the ctest LISTING rather than trusting the CMake call.**
- **The recurring trap bit a THIRD time in three days and is worth reading before any new model:** `build-wasm.sh`'s emcc source list is **EXPLICIT** (§60, §64, §69 all record it), so `DropModel.cpp` had to go inside the `STAMP:EMCC-ARGS` markers. A malformed `EXPORTED_FUNCTIONS` edit (comma-separated names crammed into single JSON strings) was caught by **reading the file back**, not by any build failure — the C++ compiled clean either way.
- **Named follow-ups this slice leaves:** the **frequency-domain shifter** (owns the XFAIL) · **formant preservation** is not modelled and is CORRECT for a drop pedal, named so nobody reads it as a defect · a `--no-splice` flag on `clipper-render`, deliberately not added · **the Mesa is still absent from `identical_core_test`'s REFERENCE driver**, which is why this slice's board case uses the JCM800, and §64's standing note about comp/gate/delay/wah/chorus is open too · **no ADR was written** — the two decisions a future slice could reasonably "fix" back (one live tap rather than the textbook two; `latencySamples()` = 0 for a sawtooth read) are argued in the `PitchShifter.h` banner and §70 instead.
- **In flight (2026-08-17): the MESA/BOOGIE DUAL RECTIFIER — M10.4, the SEVENTH amp voice, and the FIRST one TRANSCRIBED FROM A COMPLETE FACTORY DRAWING SET** (docs **§69**, `docs/work/2026-08-17-mesa-dual-rectifier.md`, branch `claude/mesa-drop-pedal-setup-9lxs5g`). Amp voice **6** (`mesa`), wired end to end in one slice (core, C ABI, worklet-transparent, web face + tokens + assistant, native engine). **ALL FIVE GOLDENS UNCHANGED — `clipper_player_expectations_tests` passes, and a new voice cannot need a bless.** **Core ctest MEASURED ON THE MERGED TREE: 31 targets + 5 skipped ledgers = 36 entries, 36/36 green.** `clipper_mesa_tests` adds one target and registers **NO xfail ledger** (zero known-bad properties). Note the arithmetic, because two parallel slices landed again: this branch alone measured **37** (it still carried the RAT's ledger), and §68's output-pot-taper slice closed that ledger on `main` — so merged it is 36, not 37. Re-measure rather than reconciling the two by hand. WASM artifact rebuilt (**103** hashed inputs).
- **THE RESEARCH CHANNEL IS THE STORY: THE OWNER SUPPLIED THE SHEETS, AND THEY CARRY THE MANUFACTURER'S OWN MARKED DC NODE VOLTAGES.** `recto.zip` — 11 legible GIFs (`mbdr1`…`mbdr8`): preamp, power amp, effects loop, power supply, switching matrix and a **TRUTH TABLE**. Every other channel was checked and is dead: `WebFetch` is **EGRESS_BLOCKED** (verified on warpedmusician and prowessamplifiers), GitHub **code search is repository-scoped** this session, and `dsharlet/LiveSPICE` was cloned and has **no Mesa example**. So without the zip this would have been another §57-style reconstruction. **The marked voltages are the strongest check this project has ever had on an amp** — an ABSOLUTE external reference rather than an analytic form derived from the same netlist (§29's standing complaint): **V2B plate 0.01 %**, V1A 1.69 %, V2A 1.25 %, V3A 3.98 %, V3B cathode 2.97 %, idle rail **0.14 %**.
- **BAR (a) — THE TWO "MODERN" MODES RUN THE POWER AMP OPEN-LOOP, and it is TOPOLOGY not an EQ curve.** Sheet `mbdr7` switches LDR19/LDR20 per mode and β = 4k7/(4k7+Rfb) comes straight off the drawing: **OR CLN 9.27 · OR NORM 6.19 · RED VINT 6.19 · OR MOD 0.00 · RED MOD 0.00 dB** of loop depth. Every other amp here has a permanently wired loop (JCM 6.37, OR120 7.81, Rockerverb 6.65). **That is why a Recto's low end is loose.** Asserted at **exactly** zero (`< 1e-9`), not "small". Player-facing consequence, coached rather than apologised for: **PRESENCE does nothing in the two MODERN modes**, because the loop it works through is open.
- **BAR (b) — THE RECTIFIER SELECT MOVES SAG, and SPONGY/BOLD IS A DIFFERENT SWITCH.** The sheet settles a thing people routinely conflate: `rect select` is 4×1N4007 vs 2×5U4 (tapping **different HT taps** — 350+50 V vs 350 V, so the 0.875 ratio is transcribed), while **SPONGY/BOLD is a MAINS-PRIMARY-SIDE switch**. Rail droop on a sustained low E: silicon BOLD **25.34 V**, 5U4 BOLD **56.66 V** = **2.24×** against a **2.0×** bar — the plan said 3.0× *before anything was measured*, and the honest number is what shipped (**RECORDED not snugged**). SPONGY is asserted to move sag on its own so nobody wires the two together.
- **Three structural findings a reconstruction would have got wrong.** (1) **V3B is a DIRECT-COUPLED CATHODE FOLLOWER** driving the stack — corroborated independently by the marked 213 V plate vs 216 V cathode (Vgk = −3 V); source impedance **375 Ω** against the OR120's 45 534 Ω, which is why this amp's tone controls interact less than either Orange's. (2) **THE TWO INPUT SELECTS ARE DIFFERENT KINDS OF COMPONENT** — RED couples through an **82 pF CAPACITOR**, ORANGE through a **2M2 RESISTOR**; measured tilt **RED +22.23 dB vs ORANGE −1.07 dB = 23.29 dB**, asserted on the NETWORK because five stages of clipping compress it away downstream. (3) **Every switched cathode bypass is a GAIN switch** (1 µF in series with a 47 k that an LDR SHORTS OUT).
- **THE POWER SHORTFALL IS REPORTED AND ITS OBVIOUS CAUSE IS REFUTED.** The amp makes **~48 W against a rated 100**. The transcribed **1 k/2 W screens** against this repo's known-hot screen term (findings 9/10) look like the cause and are **NOT**: perturbed to 470/100/1 Ω the ceiling moves only 47.99 → 52.26 → 55.55 → **56.46 W**. One real error WAS found by measuring rather than reading — `kRaa` was set to 3800 (a PAIR's reflected load) where `TwinPowerAmp` already carries **2000 for the same 6L6 quad**. **Do not close the residual with a higher `kVsupply` or a softer screen network** (§57.3's standing instruction).
- **ONE OVERSAMPLING DOMAIN FROM THE FIRST COMMIT — §63.14's lesson applied up front rather than retrofitted.** Both halves prepared at the oversampled rate with their own resamplers at 1× (`Oversampler`'s documented exact pass-through). **Latency 72 samples / 1.50 ms** for a FIVE-triode cascade plus a power section, and this voice does **not** inherit the 44.1 kHz alias XFAIL the OR120 and the Rockerverb both opened.
- **TWO REAL BUGS THE TESTS FOUND, both fixed rather than bounded.** (1) **`reset()` did not reproduce a fresh model (4.699e-01)** — bisected: the power amp was already exact at 0.000e+00 and `prepare+reset` equalled `junk+reset` exactly, so `reset()` was deterministic and it was **`prepare()`** that differed. It ended by setting `primed_ = true`, so §35's deferred snap never fired and a knob pushed after prepare ramped from the default FOREVER AFTER. Now prepare snaps and then CLEARS `primed_`, as `Jcm800Preamp` does → **0.000e+00**. (2) `kRaa`, above. **A third trap bit again and is worth repeating: `build-wasm.sh`'s emcc source list is EXPLICIT** — the first artifact rebuild failed at `wasm-ld` with undefined `MesaAmp` symbols, exactly as §60 and §64 record for the delay and the opto. Add new `.cpp` files INSIDE the `STAMP:EMCC-ARGS` markers.
- **Resting state is the §59 case, decided by measurement (ADR 006).** Both MNA networks would rest at zero if their input did; it does not, because `TriodeStage`'s Newton exits at a **residual tolerance** and keeps re-exciting them. Over 41 s of silence they decay then **PLATEAU** at **2.40e-08 / 1.04e-08** — ~30 decades above subnormal, so the `flushDenormal` calls are **guard-rails, not fixes**, and the test asserts what the policy is actually about: **no subnormal float leaves the model** (0 over a second of silence).
- **Named follow-ups this slice leaves:** a **Mesa oversized 4×12 IR** (it reuses `brit412`) · the **EFFECTS LOOP** as a feature (V4A/V4B are transcribed; this model runs the sheet's own LOOP BYPASS path, and their standing draw IS accounted for in `kIdownstreamC`) · the **EL34 bias option** (the sheet supports it; this ships 6L6 at the transcribed −51 V) · **the power shortfall** · **a series-R field on `TriodeStage`'s cathode network** (the bypass-OFF state ships as unbypassed; bounded at under 0.4 dB and measured) · **three FMV stacks now exist** (JCM, Rockerverb, Mesa) with the same topology and different values — extract only when a slice can MEASURE the union is not a union (ADR 021).
- **The SECOND half of the owner's 2026-08-17 ask — a DigiTech Drop-style polyphonic pitch shifter — SHIPPED 2026-08-19 as M13.10, docs §70** (see the top entry). It was deliberately a separate slice: a whole new DSP family with no circuit and no schematic, so its acceptance shape is cents accuracy / latency / artifact floor rather than a measured contrast against a sibling. `DelayLine.h` was indeed the starting point (`PitchShifter` is its second consumer); **`FFT.h` was NOT needed and is now the named fix for that slice's one XFAIL.**
- **In flight (2026-08-10): the OUTPUT-POT TAPERS, lineup-wide — five pots, and only FOUR of them are audio tapers** (docs **§68**, no ADR, `docs/work/2026-08-10-output-pot-tapers.md`, branch `feat/output-pot-tapers`). §66.7's item 1, taken in the shape §66.3 said it had to be taken. **THE HEADLINE: the RAT measures exactly the −11.61 dBFS §66.3 measured for the one-pedal version, and §36 STILL HOLDS — it keeps rank 2 of 5 at the shipped defaults, the same rank it had before, where fixing it alone would have put it at rank 5.** Absolute level was never the property; rank was. Core ctest **35 → 34 entries**, repo ledgers **6 → 5** (`rat-level-pot-linear-not-log` XPASSed → deleted → hard, and `clipper_rat_tests` now has ZERO known-bad properties). **FOUR GOLDENS MOVED AND WERE RE-BLESSED ON EXPLICIT OWNER AUTHORIZATION 2026-08-10** ("bless and merge", after being shown the per-golden table) — the hand-run ritual, no `/dev/tty` here: clean tree, `--golden-report` re-checked against the approved figures immediately before `--update-goldens`, justification in `GOLDENS.md` in the same commit. `rat_jcm800` −1.20 dB · `sd1_twin_reverb` −8.57 · `muff_twin` −10.23 · `ts_ac30` −7.17; **`clean120_chorus` UNCHANGED and BYTE-IDENTICAL on disk**, the scope check. Three of the four moved essentially FLAT across every band (spread 0.45/0.92/0.54 dB) — a pot is a frequency-flat scalar, so that is quieter-with-the-voice-intact. `rat_jcm800` is the exception and the one to read: only −1.20 dB broadband although the RAT drops 5.21 (the JCM800 is driven less hard and gives it back), and its **14.63 dB outlier at 252 Hz is a band with NO harmonic of the stimulus in it** (f0 = 220 Hz; the band spans 224–283), so it carries only the amp's distortion products, which collapse with less drive.
- **EACH POT GOT ITS OWN NETLIST'S LAW, AND THE GOLD'S IS LINEAR — that refusal is the point of the slice.** RAT: 100 k LOG, unloaded (the `.asc` annotates `R14 is volume pot (100k, logarithmic)`; driven by the 2N5485 source follower, wiper on the jack) → the **bare** taper, 11.920 % at half rotation. TS: `R15 is level pot (1-100k, log)`, cross-checked independently by the IceScreamer clone's build doc, but its **wiper is LOADED** — R19 510 k plus C8 into (R18 510 k ∥ the 2SC4081 follower's ~2 M base) = 226 k, traced out of the file's own WIRE/FLAG records → **11.391 %**, not 11.920. Muff: the Circle-Circuits Big Muff BOM lists `R25 (Level)` as **A250k** while listing Gain/Tone as B100k (an author distinguishing the letters — §63's LiveSPICE precedent), Q4's 10 k collector load is in series AHEAD of the pot so it is **law-neutral**, wiper on the jack → the bare taper. **GOLD: a LINEAR (B) 10 k pot inside the reference netlist's own R25 560 Ω / R28 100 k network** → 48.976 % at half rotation, **within 0.180 dB of the identity map it replaced at every position**. Applying the house `audioTaper` there would have cost it 7.71 dB at its shipped default against every source available. §63.14's lesson, honoured: a taper is derived, not chosen.
- **THE ACCEPTANCE BAR AS WRITTEN IS NOT MET, and the cause is the DEFAULTS, not the laws.** Spread at shipped defaults **4.5 → 8.0 dB**. Recorded, not snugged. The mechanism is measured: at a COMMON output position the spread was **6.40 dB at every position** before (a linear pot makes the lineup pot-independent) and is still **6.40 dB at OUTPUT 1.0** after — so **the shipped OUTPUT defaults were a level calibration expressed in linear-pot coordinates, and changing the law invalidates the coordinates rather than the calibration.** Had the GOLD wrongly received the audio taper the spread would have read 4.89 dB, i.e. the bar would have been "met" by putting a law on a pedal against its own source. **Named follow-up with the arithmetic already done** (§68.5): rat 0.9454 · sd1 0.9319 · ts 0.9461 · muff 0.8753 · gold 0.7124 reproduce the old delivered gain exactly. NOT bundled here — mixing it into these goldens would leave the owner unable to attribute the drift.
- **THE SD-1 IS THE ONE RECONSTRUCTION, AND NO LETTER WAS INVENTED FOR IT (§57's rule).** No SD-1 netlist or parts list is reachable — the proxy is github.com-only (re-confirmed: hobby-hour, effectslayouts, stompboxschematics, electricdruid, electrosmash and kitrae all fail at CONNECT, and GitHub code search needs a login), there is no SD-1 `.asc`, and the search extracts **contradict each other** on this exact question, so none is used. It carries the TS's law through the shared `OverdriveEngine` exactly as it has always carried the TS's tone stack and DC blocker — a documented inheritance, now **stated** rather than implied, carried in `OverdriveConfig` so one pedal can move alone, and guarded by a test bar that goes red if the two silently diverge. **Do not re-tune it toward a sound; find the schematic.**
- **`testLevelLinearity` was named in THREE files; the linear map was asserted in SIX** — five in the core suites and one in the WEB suite (`audio.spec.ts`'s "LEVEL scales RMS" wanted a 1.8-2.2 ratio; it now wants **8.0-8.8**, the audio taper's 8.389, so a linear map misses by 4x — the core owns the SHAPE, the web spec owns the DELIVERY PATH, §57.13). A SEVENTH web clause had to be RE-DERIVED and got stronger for it: `cab.spec.ts`'s declick test proved its batch landed with `afterRms < beforeRms * 0.6`, a RELATIVE proxy that broke because the driven rig got quieter while the BYPASSED input did not move — replaced by the absolute target its own comment always described (no pedals + amp down = the trimmed input, `0.1/√2 = 0.070711`, **measured 0.0707107**), which cannot be broken by a future knob-law change at all. `test_muff_model.cpp`'s `testVolumeLinearity` and `test_gold_model.cpp`'s "OUTPUT is a linear pot (house convention)" carried it too — which is itself why this had to be one slice. All five **rewritten, not deleted**, each against a reference outside this codebase: the 10–20 % band (§58), and a SHAPE bar (largest quarter-turn dB step over the smallest — log measures 1.23–1.27 against a 1.60 bar where the linear map measures **2.409**). The GOLD gets the mirror bar: its ratio must be **> 2.00** (measures 2.258), so folding it into the house taper goes red. New **A5 `testDirtLineupStaging`** in the player-expectations suite renders all five at the shipped defaults and asserts the RAT is not the quietest — §66's argument, as a test.
- **THREE INDEPENDENT PROOFS THAT ONLY THE POT MOVED.** (1) **OUTPUT 1.0 is BIT-IDENTICAL on all five** by FNV-1a render hash against a pristine build of merged `main` — every law is normalised so `law(1) == 1` exactly, which is §51's rule and is asserted per pedal. (2) The RAT's factor-1 **drift guard** (which §36 and §66.5 both label a drift guard, not a property) had to be regenerated, and all seven hardcoded samples moved by the SAME ratio **0.737965** = the analytic `audioTaper(0.9)/0.9`; a uniform ratio to six decimals is what says the clipper, the shaping filter, the op-amp and the tone stage are untouched. (3) Only ONE of the five A4 level rows left its window — the Muff's, by the derived −10.13 dB, and its window was **shifted** 10 dB (18..38 → 8..28), not widened.
- **A DELIBERATE SCOPE BOUNDARY, stated so it is not read as a fudge: every law is NORMALISED so the top of the knob does not move.** Two networks have a fixed insertion loss at full rotation — the Muff's 10 k source into a 250 k pot is **−0.341 dB**, the GOLD's 560 Ω R25 is **−0.519 dB** — and those are LEVEL facts, not LAW facts. Shipping them would smuggle an absolute trim in under a knob-law change, which is what §66 refused. Measured, recorded (§68.3), and left to a staging slice.
- **15 PERTURBATIONS, ALL RED ON THE NAMED BAR, EVERY RESTORE GREEN** (§68.7, §68.12) — including **P8, which is §66's own scenario** (the RAT keeps its log law, the four siblings go back to linear) and turns A5 red on *"the RAT is the QUIETEST of the five"*. **And the run found a bar that could not fail:** `tsLevel()` was a test-facing convenience while `OverdriveEngine` inlined the same expression, so patching it moved the TEST's idea of the law and left the AUDIO alone — the SD-1's inheritance bar would have agreed for the wrong reason. Fixed by extracting `pot::loadedLogPot()` and having the engine call it: one function, both callers. **One honest non-failure reported too:** `clipper_player_expectations_tests` can never restore GREEN on this branch (the goldens are un-blessed by design), so its restore was verified by hand instead.
- **A CONFLATED BAR WAS SPLIT, and the flatness half got no easier.** `test_gold_model.cpp`'s `testTransparency` measured `|gain in dB|` per probe against 0.25 dB, silently mixing FLATNESS with LEVEL — they coincided only because the pot was an identity map and `kSumGain 2.0 × 0.5` came out exactly 1.0. The sourced network puts noon at 0.489758, so the conflated bar measured 0.3181 dB and failed **on a 0.18 dB level move with the flatness it exists to protect completely unchanged**. Now two bars: flatness re 1 kHz keeps the same 0.25 dB bound and measures **0.1384**, and the level is derived from two sourced numbers (`kSumGain` 2.0 × law(0.5) 0.489758 = −0.1798 dB, unity now landing at OUTPUT 0.5105).
- **THE BLESS ITSELF FOUND A BAR THAT WAS UNSOUND, and it was NOT the voicing gate (§68.10).** `--update-goldens` aborted on `sd1_twin_reverb`, and the abort was CORRECT. The write-back check was `worstBandDb <= kQuantizationFloorDb` — a per-third-octave BAND proxy whose own comment claimed it is "bounded by 16-bit quantization by construction". It only is while the render's quietest COMPARED band sits above the LSB, and the window admits every band within 55 dB of the loudest. Measured against the quietest compared band: **rat −3.59 dB → 0.0005 · muff −24.50 → 0.0009 · ts −29.26 → 0.0097 · clean120 −38.03 → 0.1059 · sd1 −40.27 → 0.1585** — monotone, and §68's level drop pushed the SD-1's reverb tail through the bar. **`clean120_chorus` was ALREADY at 70 % of budget on a golden this slice never touches**, so the proxy was fragile before and §68 only exposed it. **NOT fixed by raising the number** — `kQuantizationFloorDb` is shared with `statusWord()`, so loosening it would permanently coarsen UNCHANGED/CHANGED for every future bless, i.e. spend the anti-drift mechanism to get one bless through. A round-trip is a PER-SAMPLE property and is now measured as one: **`kWriteBackLsbBar` = 2.0 LSBs against a measured 1.51–1.59 on all five, level-independent**, and strictly stronger (8-bit storage reads ~256 LSB). Perturbation P10 (coarsen the write path to 8 bits) RED, restore GREEN with the goldens byte-identical.
- **A FIFTH BAR WAS CALIBRATED AGAINST THE OLD LEVELS, and CI found it in a file this slice does not otherwise touch (§68.12).** `native/tests/chain_edit_test.cpp`'s `runCase` holds a mid-note chain edit's seam to the signal's own steady slope and read that slope from the **PRE-edit** window only. With the TS's LEVEL pot on its sourced log law the pedal is a far bigger attenuator at knob 0.6, so the bound's DENOMINATOR collapsed **0.002488 → 0.000567** while the seam step went **DOWN, 0.001395 → 0.001087** — the bar failed on an IMPROVEMENT (the fade still turns a 0.017348 hard splice into 0.001087, a **16× reduction**). **The fix was already written 200 lines down the same file:** `runCabSwapCase` hit this when the cab swap started changing level and says so in a comment — *"the honest bound is the larger of the two settled slews"*. `runCase` now does the same, reading both from renders it already computes. **One case's bar is VACUOUS and now says so out loud** rather than reading as a pass: an unfaded splice of the *reordered* board would pass reorder's bar (×0.4, against bypass/remove ×5.4 and add ×12.8), so that case proves nothing about level continuity — reported, **not asserted**, because gating it would have taken two invented constants with a false-failure band between them. **P11:** collapse the fade (`kDeclickSeconds` 0.006 → 0.000002) → RED on 4 of 5 cases; restore GREEN. **The pattern is the finding:** five bars in this slice were level-calibrated — the GOLD transparency bar, the web LEVEL ratio, the web declick proxy, the golden write-back check and this one — and four had a strictly stronger level-INDEPENDENT replacement. Worth reading before follow-up 1, **which moves the levels again**.
- **Also corrected in passing:** §66.3 quotes the house `audioTaper` (k = 4) as "12.15 % at half rotation". The closed form is `1/(e^{k/2}+1)` = **11.920 %** exactly. Nothing was ever calibrated against the 12.15 figure — it appears only in prose — and the tests now assert the real number so it cannot rot again.
- **In flight (2026-08-10): M13.5 — the "Swirl" UNI-VIBE, the board's SECOND phaser and `OptoCell`'s NAMED consumer** (docs **§67**, **ADR 026**, `docs/work/2026-08-10-univibe.md`, branch `feat/univibe`). Pedal type `vibe`, slot **12** (`PEDAL_TYPE_COUNT` → 13), AMBER accent, wordmark "Swirl", `MODULATION Nº9 · PHOTOCELL`, knobs **SPEED / INTENSITY / MODE** — all three real. Wired end to end in one slice (core, C ABI `vibe_*`, worklet, web face + tokens + assistant, native engine + APVTS + `PedalCard`, **and its own case in `identical_core_test`'s plugin driver**). **ALL FIVE GOLDENS UNCHANGED at ±0.00, nothing blessed**, and `PhaserModel` is **BIT-IDENTICAL** (9 renders — 3 speeds × 3 rates — `cmp`'d byte for byte against a `main` worktree build, **0 differing**). No shared class was touched: the core diff is new files plus the C ABI, CMake and the two tools. Core ctest **35 → 36 entries, 36/36**; repo XFAIL ledgers unchanged at **6** (this suite registers none).
- **`OptoCell` WAS NOT WIDENED — zero new fields, zero new methods — and the LAMP came out as its own component**, which is exactly what `OptoCell.h`'s banner instructed. `core/include/clipper/dsp/LampDrive.h` is a driven tungsten filament: lumped heat capacity, `v²/R(T)` heating with tungsten's `R ∝ T^1.2`, Stefan–Boltzmann `k(T⁴ − Ta⁴)` cooling, emission `L ∝ T^m`. **The seam is `elExponent = 1.0`** — the existing published-source field with a DEGENERATE exponent, so the quantity handed to the cell IS the light and the source law lives in the lamp. ADR 026. **The header's stated reason turned out to be STRONGER than written:** a lamp's light is not a function of the drive at all, it is a function of a **STATE** — the same 0.66 V after different histories measures **21.43×** different light, which no `lightSourceKind` case on a memoryless map could express. **There is deliberately no `riseTau`/`fallTau` pair on `LampSpec`:** the asymmetry falls out of the inrush and the T⁴ law, and a tunable pair would be exactly the fitting §57 forbids (perturbation **P8**, linearising the cooling, takes the bar red).
- **THE ROADMAP'S OWN FRAMING WAS WRONG AND THE SLICE CORRECTED IT.** It says "four **mismatched** phase stages (not the Ninety's matched four)" — but `PhaserModel` has carried a deterministic **±1.5 %** per-stage detune since it shipped, specifically so its four corners do not stack into one null. "Mismatched" is not the distinction; the MAGNITUDE is: **468.1 : 1 of capacitance = 8.871 octaves against 0.0433 (204.9×)**. The player-facing consequence is the **SPREAD OF THE NOTCH PAIR**, measured on BOTH models from the identical stimulus: **vibe 102.51 / 5463.14 Hz = 5.7359 oct · ninety 832.07 / 4685.87 Hz = 2.4935 oct · contrast 3.2424 oct (2.300×)**. Bars 5.0 / 2.8 / 2.5 / 2.0×, **margins RECORDED not snugged**. **And the metric is SCALE-FREE, measured rather than claimed:** over a 2.37× resistance excursion (frozen LFO, four phases) the spread reads **5.7246…5.7809 oct** against the analytic analog **5.7249** at every point — so no reconstructed constant on this voice can reach the acceptance bar.
- **THE SECOND BAR IS THE ONE A JFET PHASER CANNOT FAKE: the lamp makes the sweep ASYMMETRIC IN TIME.** Mean `|d log2(fc)/dt|` rising vs falling — a SLOPE ratio, not a time ratio, because a lag that merely delays a symmetric waveform leaves both half-periods equal. **vibe 1.1713 / 1.2494 / 1.3806 at 0.565 / 1.344 / 3.196 Hz against the Ninety's 1.0008 / 1.0029 / 1.0015.** **Attributed with TWO independent hooks, not assumed** (§66's lesson): shipped **1.3025** · lamp instantaneous **1.0569** · cell memoryless **1.2385** · both removed **1.0015** — it is the LAMP. Third contrast, asserted: sweep depth **3.281 → 0.959 oct** from SPEED 0.2 → 1.0 against the Ninety's **3.322 → 3.321** — the filament cannot keep up and a number generator does not care.
- **THE OVERSAMPLING MEASUREMENT OVERTURNED THE PRECEDENT, for a reason nobody named in advance.** On ALIASING the phaser precedent (§12) is right about this pedal too — the cells are lit by an LFO under 8 Hz and by nothing else, so there is no audio-rate multiply, and the Hann-windowed floor is **FLAT**: 1× −140.41 / 2× −142.59 / 4× −143.05 / 8× −142.85 dB (2.63 dB of spread), which is §54's tell. **It ships at 4× anyway, because of the 470 pF stage.** S1's stagger puts one allpass corner at 14.6 kHz at the bias point and **33.9 kHz at the bright end** — against and past Nyquist — and a bilinear allpass's phase warps there. Against the analytic **ANALOG** network at 44.1 kHz the upper notch lands **+18.49 % at 1× and +1.13 % at 4×**, where `PhaserModel`'s own PRE-EXISTING error is **−3.10 %**. So the factor is **DERIVED — 4× is the smallest house factor beating the sibling's own error** (2× measures 3.61 %). Latency **72 / 1.50 ms**; the WHOLE model including the dry sum sits in the one domain so the mixer cannot comb. **This also closed a rate dependence that would otherwise have needed an XFAIL** (upper notch 15.9 % → **0.599 %** over 44.1–96 kHz).
- **Two things the plan told this slice to MEASURE rather than assume, and both came out against the tidy answer.** (1) **INTENSITY 0 is NOT bypass.** The sources make INTENSITY the fraction of the LFO reaching the LAMP DRIVER, so at zero the lamp still sits at its bias point: chorus measures **27.56 dB** of span (a real comb) with **0.000e+00 octaves** of corner movement at SPEED max, and vibrato measures **0.0008 dB** of gain deviation (a pure allpass). (2) **The SAME `OptoCell` sits on the OPPOSITE side of ADR 006's scope rule here from where it sits in `OptoModel`** — a Uni-Vibe's lamp is never off, so the cells never rest at zero. `VibeModel::maxAbsRestingState()` covers the four allpass memories and **nothing else** (lamp 2323.5 K, cell 11 765 Ω, trap 0.9805, output at exact digital silence). Related honest finding: **the trap-occupancy mechanism — `OptoCell`'s whole point inside the compressor — is effectively INERT here**, pinned at 0.9805 because the cells never go dark. Not removed; folded into the release derivation and stated.
- **The research channel was NARROWER and RICHER than the plan expected.** Narrower: `geofex.com` and `montagar.com` still **403 at the egress proxy**, and this session's GitHub access is **repository-scoped**, so even the code search §59/§63 leaned on was unavailable. Richer: search extracts gave **eight** sourced facts, not one — §67.1 tabulates them. **S6 (the LDRs' 10 kΩ…100 kΩ illuminated range) + S8 (INTENSITY as an LFO amplitude control) together DERIVE the lamp's bias and swing** rather than leaving them to be chosen: `LampDrive`'s closed-form inverse says the endpoints are **0.318097 / 1.000000 V**, and the rendered excursion measures **10 001 … 99 462 Ω**. **RULE ZERO held** — none of the three GitHub Uni-Vibe implementations was opened. Everything outside S1–S8 is a documented reconstruction and **§57's rule applies verbatim**; the two weakest rows are the whole `LampSpec` and the cell's DYNAMIC constants (the latter forced by a published figure: `OptoCell`'s LA-2A card is **REFUTED** by the sourced 7.6 Hz top speed, whose half period is 65.8 ms).
- **12/12 perturbations RED, every restore GREEN** (§67 and the plan file tabulate them): the lamp's thermal mass removed from the shipped path, the four caps MATCHED, 4× → 1×, MODE as a hard switch, the published lit resistance 10 k → 30 k, INTENSITY as a wet/dry mix, `elExponent` put back to 2.5, and the T⁴ cooling linearised — plus **four supplementary runs (P2b/P3b/P6b/P7b) with the earlier bar removed**, because four of the patches tripped a bar ordered ahead of the one they are named after (§58.8's discipline). **A BAR THAT COULD NOT FAIL WAS FOUND AND FIXED:** `testOptoCellIsUsedUnwidened` originally built its OWN `OptoCell` with `elExponent = 1.0` and asserted on that, so P7 walked straight through it. A new `VibeModel::cellCompositeExponent()` reads the SHIPPED object; P7b is red on it. §66's lesson, found again.
- **The rest, measured:** CPU **12.86× realtime / 7.78 % of one 48 kHz stream** (the Ninety is 0.48 %, the Lumen 3.67 %) — reported, not bounded, and the named optimisation is that the four cells are identical by construction so three of the four `OptoCell::process` calls recompute the same number (collapsing them would be bit-identical; deliberately NOT taken, because four physical devices are where a sourced per-cell figure has to go). Ragged-block invariance and `reset()` both **0.000e+00**; one NaN 11 899/12 000 → **0/12 000**; rate spread notch-spread **0.0085 oct** / lower notch **+0.010 %**; DC on signal **−0.0104 %** clean and a +0.1 V offset **passes straight through** (linear network, no coupling caps — a named gap, the §62 CE-1 precedent); zipper SPEED **0.53×** / INTENSITY **0.09×** / MODE flip **0.95×** (§62 measured 17.02× for a forwarded hard switch); the four cells agree **exactly**; envelope swing on a chord **vibe 7.69 dB vs ninety 2.49 dB**.
- **Found in passing and FIXED (§67.10):** `native/src/PedalCard.cpp`'s `pedalMenuLabel()` had **no `case PEDAL_OPTO`**, so M13.3's Lumen showed in the native gear tray and swap menu as the bare word **"Pedal"** while its CARD drew correctly — the same class of defect as §61.10 and §62's `kFaces` bug, in the opposite direction (the keyed table was right, the switch was incomplete).
- **Named follow-ups this slice leaves:** find the Uni-Vibe schematic (every constant outside S1–S8 is a reconstruction) · **S3's transistor stages** — the sources say their "imperfections" are part of the sound and this model uses ideal first-order allpasses, the largest known gap · INTENSITY's true identity (LFO amplitude, shipped per S8, vs a wet/dry mix) · **`PhaserModel`'s own −3.10 % discretization error**, pre-existing, untouched, now measured · collapsing the four identical cells to one (bit-identical, most of the 7.78 %) · **comp / gate / delay / wah / chorus are STILL absent from `identical_core_test`'s plugin driver** — this slice added only its own, so §64's gap is still open.
- **In flight (2026-08-01): the ROCKERVERB FIELD REPORT — one fix, three refutations** (docs **§63.14**, amended into §63 in place + a §57.7/§57.13 amendment; `docs/work/2026-08-01-rockerverb-field-report.md`, branch `claude/rockerverb-field-report-6f557i`). The owner on a voice hours old: *"a bit tinny/squashed … a touch brittle almost … the gain needs scaling back too 100 is totally unusable … too noisy, too much gain."* **RULE ZERO held: the Sunshine Stack is another vendor's model and nothing was tuned toward it.** Everything was measured before anything changed (§43's report said "the RAT" and measured as the Muff). **ALL FIVE GOLDENS UNCHANGED at ±0.00 — nothing blessed, and by construction: no shared class was touched.** Core ctest **35 → 34 entries** (the Rockerverb's ledger came OFF), repo ledgers **6 → 5**.
- **THE ONE FIX: `RockerverbAmp` now owns ONE shared oversampling domain, and it closed the XFAIL.** The voice shipped with FIVE independent 4× domains (one per triode stage + one round the power section). Now the preamp and the power section are prepared **at the oversampled rate** with their own resamplers at **1×** — `Oversampler.h`'s documented exact pass-through — so **`TriodeStage`, `RockerverbPreamp` and `RockerverbPowerAmp` needed NO edit** and the four valve goldens were never at risk. Composed cranked 4× alias floor **44.1 kHz −52.7 → −72.3 dB** (48 kHz −80.1 → −84.9) against the **unchanged −56 dB bar**, now a HARD assert at both rates; at an ordinary crunch setting (GAIN 0.50 / VOLUME 0.10) **44.1 kHz −61.1 → −115.1 dB**, i.e. the 10.4 dB rate penalty REVERSES. **Latency 360 → 72 samples (7.50 → 1.50 ms)**, asserted against `Oversampler`'s own 0/64/72/76 rather than read back, with the shipped 4× default pinned. `rockerverb-alias-44k1` XPASSed → deleted → hard.
- **REFUTATION 1 — the SAME change makes the OR120 WORSE, so §57.7's "it is the architecture speaking" is half wrong.** Built and run on `OrangeAmp` too: **44.1 kHz −50.8 → −48.7 dB, 48 kHz −73.0 → −67.8**. **REVERTED** — `core/src/dsp/OrangeAmp.cpp` is byte-identical to its pre-slice state; only its banner and the XFAIL's `fix` string changed. Mechanism measured, not assumed: removing the intermediate band-limitings lets each stage's products reach the NEXT nonlinearity unfiltered — a large win behind triodes, a loss behind a hard rail, and the OR120's **cathodyne clips on COMPLIANCE** (Vk pinned to [0, C+/2]), the hardest clipper in the lineup. `orange-schematic-alias-44k1` **stays** and is re-owned to the CATHODYNE, not the domain layout. Never a lower bar.
- **REFUTATION 2 — "squashed" is NOT §63.3's missing preamp grid blocking.** Dynamic range on a 26.0 dB input span at VOLUME 0.10: **GAIN 0.30 → 25.52 dB, 0.50 → 10.45, 0.70 → 1.01, 1.00 → −0.04**. The amp has essentially perfect touch sensitivity at 0.30 and none at 0.70 — **the squash is where the knob puts you, not a missing mechanism**, and grid blocking would put a bloom on top of a brick wall. **`TriodeStage` was NOT touched** (the highest-risk thing in the brief); §63.11's entry for it is re-scoped to a fidelity item.
- **REFUTATION 3 — "too much gain / 100 unusable" is REAL but the TAPER cannot fix it.** In wiper space (taper-independent) the useful span is 0.002…0.20 and **above wiper 0.20 the output moves 0.22 dB** — with the shipped k = 4 that is knob 0.615→1.00 dead (38 % of travel; 50 % by a 1 dB level-ceiling test). But the house law puts the wiper at `1/(e^{k/2}+1)` at half rotation: **k = 4 → 11.9 %, INSIDE §58's documented 10–20 % audio-taper spec; k = 8 → 1.8 %, which is not an audio pot.** The spec's limit (k = 4.394) moves the ceiling 0.500 → **0.537**. And the geometry is fixed — clean→onset is 23.5 dB of a 40 dB span — so any taper putting saturation at the top puts the ≥5 % onset at ~59 % (k = 8 → 0.56, k = 10 → 0.65), which §63.5's own 0.08–0.35 window forbids. **§51's method does not apply**: it could pin `taper(1)=1` because "100 is perfect"; here "100 is unusable" and the circuit says 100 is a wall. **What IS wrong and is NAMED as its own slice: the DEFAULTS** — this voice inherited `AMP_KNOB_DEFAULTS`' JCM values (GAIN 0.5 / master 0.4) although §63.5 documents its LINEAR master's useful range as 5–15, so **the amp opens at the wall**. The assistant prompt was sharpened with the measured geometry in the meantime (§48/§63.10 precedent).
- **CPU: NO measurable change, and the reason is honest.** Interleaved same-machine A/B (§35), 6 alternating pairs: AFTER median **46.08 %** of one 48 kHz stream, BEFORE **47.65 %**, against a **7.6-point within-binary spread**. Eight halfband passes were deleted and the three interstage 4×4 MNAs plus the FMV stack's 8×8 now run at 192 kHz — they cancel. **Latency is the win: 360 → 72.** Perturbations **P1/P1b/P2 all RED, restore GREEN** — and P2 is the isolation that matters: with the shared domain kept but the inner power domain re-armed, every alias bar still PASSES (−75.5 / −117.1) and only `testOneOversamplingDomain` goes red, because that bar is written against `Oversampler`'s **documented** 0/64/72/76 rather than read back from the amp.
- **"TINNY" IS A BASS DEFICIT, NOT A TREBLE EXCESS — measured against two siblings.** Composed tilt re each amp's own 660 Hz: at 4.4 kHz **Rockerverb +8.37 vs JCM800 +10.15** (it is LESS bright than the Marshall) and at low E **−14.79 vs the JCM's −3.36** — 11.4 dB thinner. All of it is in the preamp (−14.82 dB at 82 Hz preamp-only), i.e. the transcribed **1 n / 2n2 / 4n7** interstage cascade (398/134/49 Hz). Not touched; BASS has +9.77 dB of authority at 82 Hz to answer it.
- **THE NETLIST WAS RE-VERIFIED INDEPENDENTLY, because two complaints rest on it.** `dsharlet/LiveSPICE` re-cloned and the `.schx` re-parsed from scratch: **C1 = 1 nF CONFIRMED**; **the MIDDLE pot is NOT a rheostat CONFIRMED** by re-deriving the terminal geometry (`Potentiometer` anode (−10,−20) / cathode (−10,+20) / wiper (+10,0); `Middle` at (470,100), `Rotation="-10"` ≡ 180°, `Flip="true"` resolving as y→−y). **Newly measured consequence, REPORTED:** because C→GND is 25 k at every position instead of a canonical FMV rheostat, the stack's insertion loss is **−13.24 dB at 1 kHz** where a canonical Marshall FMV at noon is ~−20 — **~7 dB of "too much gain", and it is the netlist's, not a fit.** Also found: both GAIN pots carry `Wipe="0.6"` in the file (weak evidence, unused). **Still open: the file is a Rockerverb 50 preamp and this voice ships as a 100** — no reachable source settles whether they are identical.
- **In flight (2026-08-01): the RAT polish slice — BOTH halves REFUSED on measurement** (docs **§66**, no ADR, `docs/work/2026-08-01-rat-polish.md`, branch `claude/rat-polish-6f557i`). §36's named follow-up ("downstream level staging") plus the open "LM308 slew-rate research" item. **NO TONE CHANGE SHIPPED. ALL FIVE GOLDENS UNCHANGED at ±0.00, nothing blessed, the core suite is GREEN** — the report table is in the plan file, and it is row-for-row identical to the clean-tree run taken before any edit, which is the scope check. Core ctest **35 → 36 entries, 36/36** (`clipper_rat_tests` opens its FIRST xfail ledger; repo ledgers 6 → 7); WASM artifact rebuilt.
- **PART 1 IS REFUTED, AND §36 IS THE REASON.** All five dirt pedals at their shipped defaults, 220 Hz, 48 kHz, tail RMS at the 0.15 V unity-trim probe: **rat −6.40 · sd1 −7.97 · ts −9.19 · muff −4.73 · gold −8.38 dBFS** — the RAT is second loudest of five inside a **4.5 dB** spread, and by 0.30 V the whole lineup is within 2.5 dB. There is no outlier to trim. With §36's diode reverted the same probe reads **rat −11.17**, i.e. **before §36 the RAT was the QUIETEST pedal in the lineup**: the diode fix moved it from the bottom of the pack to the middle, and a compensating trim would put it back where the bug had it.
- **A REAL NETLIST IS REACHABLE FOR THE RAT AND THIS FILE'S VALUES HAD NEVER BEEN CHECKED AGAINST ONE.** `Cushychicken/ltspice-guitar-pedals` → `proco-rat-distortion.asc` (github.com is still the only reachable host; `WebFetch` 403'd the TI/mit.edu/onsemi/studylib datasheet mirrors and electrosmash does not resolve). Traced node by node: both feedback legs, the 100 k gain pot, R10 = 1 k into the diodes and **C1 = 30 pF of op-amp compensation** agree exactly. Approximations now written into `RatModel.cpp`'s header with a number each — **C9 = 100 pF across the gain pot is masked by a CONSTANT 36.7× at every knob position** (its pole and the modelled `GBW/A` corner both scale as 1/Rf, so the ratio is knob-independent: < 0.03 dB in band, a derivation not an omission); **`kCp` = 10 nF at the clipping node is FABRICATED** (it is the WDF library example's, the real node is loaded by `R17 100 k log + R15 1.5 k` into `C11 3.3 nF`); the three output high-passes are worth ≤ 0.75 dB at low E; the FILTER law sweeps the cutoff where the circuit sweeps the resistance.
- **THE ONE REAL APPROXIMATION PART 1 FOUND — the LEVEL pot — is measured, registered as an XFAIL and DELIBERATELY NOT FIXED.** The netlist's own annotation says `R14 is volume pot (100k, logarithmic)` and it sits last, after the source follower, essentially unloaded, so its law is the bare audio taper; `RatModel` maps it identity-linear and has said so since M1. Substituting the house `audioTaper` (k = 4, 12.15 % at half rotation) **measures −5.21 dB at the shipped 0.8 default → −11.61 dBFS, which makes the RAT the QUIETEST of the five again and reproduces the pre-§36 staging to within 0.4 dB.** Shipping that on top of a measurement saying the level is right would undo §36 by a knob law — and **every** sibling carries the same approximation (`OverdriveEngine`: *"identity linear map, as the RAT"*), so it is a lineup-wide slice, five output pots, one bless. New ledger entry **`rat-level-pot-linear-not-log`** (measured: LEVEL 0.5 delivers **50.0 %** of the LEVEL 1.0 output, against the 10–20 % audio-taper band); repo ledgers **6 → 7**. Note `testLevelLinearity` **asserts the linear map** — it is a drift guard on a known approximation, now labelled as such, and the fixing slice must rewrite it.
- **PART 2's PREMISE IS REFUTED TOO: the LM308 slew rate has been modelled since M6.5, at 0.3 V/µs — but its JUSTIFICATION was a fit, and NOTHING GUARDED THE VALUE.** The comment read *"(0.15-0.3 V/us is the cited band; 0.3 keeps note attack alive while still killing the razor edges — measured.)"* — a number picked from a band by tonal outcome. Un-fitted: **the RAT fits the datasheet's STANDARD 30 pF compensation (netlist C1), which is the condition the 0.3 V/µs typical is quoted under**, and the competing **0.15 V/µs figure belongs to the LM308H**, a different part. Provenance caveat recorded rather than dressed up: no datasheet PDF was openable, so the slew figure is search-summary-grade; the 30 pF it is conditioned on is netlist-grade.
- **THE SLEW LIMIT IS DORMANT ON ORDINARY PLAYING AND DOMINANT ON HIGH NOTES — measured, not asserted.** Percentage of oversampled samples at which the clamp binds, with the demanded slope: 220 Hz 0.15 V **0.000 % / 0.0237 V/µs** · 1 kHz **0.000 % / 0.1026** · **low-E pluck 0.30 V 0.000 % / 0.2235 (74.5 % of the limit)** · high-E pluck 0.30 V dist 1.0 **1.257 % / 21.33** · **4186 Hz 0.15 V 92.97 % / 2.4949** · 4186 Hz dist 1.0 **97.84 % / 6.8330**. What it is worth: **exactly zero below ~1 kHz** (220 Hz, 1 kHz and a low-E pluck are **bit-identical** with the clamp removed, 0 differing samples of 66150/72000/144000 at 44.1/48/96 kHz) and **0.343 / 0.360 / 0.354 dB** plus 134 Hz of centroid on the 4186 Hz probe. **Why it is small is structural:** the two LM308 behaviours are in SERIES, bandwidth first, and the closed-loop pole (`GBW/A` = 4.9 kHz at DISTORTION 0.7, **501 Hz** at 1.0) has already removed most of the slope demand before the clamp sees it. Nothing was done about it — the ordering is the textbook cascade and both constants come from the same 30 pF.
- **THE BAR THAT COULD NOT FAIL WAS FOUND IMMEDIATELY, and it was the one NAMED after the constant.** `testSlewRate` builds its own `LM308Stage` with local copies `GBW = 1.0e6, SR = 0.3e6`; nothing in the suite read `kOpAmpSlewVoltsPerSec`. Perturbed to **0.15** and **3.0 V/µs** it stayed GREEN both times — the only thing that noticed was `testFactorOneRegression`, a hardcoded-sample **drift guard** §36 labels as such and which is REGENERATED whenever the clipper legitimately changes. Replaced by **`testSlewInModel`** (44.1 + 96 kHz) on a new measurement hook **`RatModel::setSlewLimit(bool)`** — needed because `setIdealOpAmp` removes the bandwidth pole AND the clamp together. Two bars: **(a) a 0.30 V low-E pluck must be BIT-IDENTICAL with the clamp removed** (this is what separates the two candidate datasheet figures — at 0.15 V/µs it slews) and **(b) a 4186 Hz tone must lose 0.20–0.55 dB** (0.15 V/µs measures 0.75, 0.6 V/µs measures 0.07). **5/5 perturbations RED, restore GREEN** — including **P4, which is the important one: neutering `setSlewLimit` leaves bar (a) vacuously true, and bar (b) is what stops (a) from being another bar that cannot fail.** P5 (the audio taper) trips the drift guard and then XPASSes the new XFAIL.
- **Named follow-ups this slice leaves, in order:** **the output-pot tapers lineup-wide** (rat/sd1/ts/muff/gold together, one bless — the XFAIL is the reminder) · **`kCp`, the fabricated clipping-node pole** (a §54-shaped clipper slice; `testAdaaTracksWdf` has to be re-tied inside it exactly as §36 refitted `kDefaultVk`) · the FILTER law and the three output high-passes · a `--no-slew` flag on `clipper-render`, deliberately not added here to keep the diff out of a file a parallel slice is editing.
- **In flight (2026-08-01): the "Lumen" OPTICAL COMPRESSOR — M13.3, the SECOND dynamics voice** (docs **§64**, **ADR 025**, `docs/work/2026-08-01-optical-compressor.md`, branch `claude/optical-comp-6f557i`). A Teletronix LA-2A-style **electro-optical leveling amplifier** as pedal type `opto`, slot **11**, PERIWINKLE accent, wordmark "Lumen", `DYNAMICS Nº2 - LEVELER`. THREE controls — PEAK REDUCTION / MODE / GAIN — and it is the **first pedal on this board whose MIDDLE slot carries a real control**. Wired end to end in one slice. **ALL FIVE GOLDENS UNCHANGED at +/-0.00, nothing blessed**; the Dyna Comp and the noise gate are **BYTE-IDENTICAL** (three `clipper-render` renders `cmp`'d before and after). Core ctest **32 -> 33 entries, 33/33**; native **3/3** with a ninth `identical_core_test` board at **0.000e+00**; Playwright **80 passed**; node 15/10/12; electron 20; WASM artifact rebuilt (**88** hashed inputs — `OptoModel.cpp` had to be added to `build-wasm.sh`'s source list, inside the `STAMP:EMCC-ARGS` markers, exactly as §60 records for the delay).
- **BOTH ADR-019 SEAM PREDICTIONS CAME OUT NO, AND BOTH WERE MEASURED (ADR 025).** ADR 019 named this voice as `CompressorEngine`'s consumer ("a config plus one `applyGainCell()` case"); ADR 021 narrowed it to "reuse the DETECTOR, expect the CONTROL side to be your own"; ADR 023 left the procedure "build the substitution and measure it". **(1) Not a config:** of the engine's EIGHT config structs exactly **one** (`OutputStage`) survives contact with an optical leveler — `DriveNetwork` is the CA3080's differential input attenuator, `LoadStage`/`Splitter`/`compliance_` are the Dyna Comp's own stages, and `applyGainCell()` returns a **CURRENT** where an optical cell is a **RESISTOR IN A DIVIDER**. Making it fit needs four axes whose settings share no component = the union ADR 021 forbids. **(2) Not a consumer of `SidechainDetector`, and the substitution was BUILT AND RUN** on a replica validated against the shipped model to **0.05 dB**: proportional range **2.031 dB** (M13.1's detector) vs **14.323 dB** (this pedal's panel + cell); the ratio curve goes non-monotone to **10.40:1** with **0.09 dB** of reduction at -30 dBV (the level the reference's own manual says it is already compressing at); and the acceptance property collapses to **1.000x**. **The physical reason: this pedal has no envelope capacitor at all** — an EL panel emits on BOTH polarities, so the PANEL is the rectifier and the PHOTOCELL is the integrator. Neither type was widened. `OptoModel` is standalone (the shape `GateModel` took one slice earlier) and holds a NEW shared component, **`OptoCell`**, whose named future consumer is **M13.5's photocell-driven Uni-Vibe**.
- **THE ACCEPTANCE BAR IS PROGRAM DEPENDENCE, and the OTA voice is the control.** Identical stimulus on both compressors — a 220 Hz 0.30 V tone for 150 ms and for 6 s, same depth to 0.07 dB, then the time until the gain reduction falls under 0.5 dB: **Lumen 1.00 s -> 2.88 s = 2.892x . Squash 4.11 s -> 4.11 s = 1.000x.** Bars `> 2.0` and `< 1.25`; the **margin is RECORDED not snugged** — the cell's two release constants were pinned to the published 0.5-5 s window, and 2.892 is what fell out. The mechanism is asserted alongside the symptom (trap occupancy **0.0686 vs 0.6163**). DEPTH axis, which is what the published sentence literally says ("depending upon the amount of previous reduction"): complete release **1.04 -> 3.67 s = 3.53x**, every point inside the published window.
- **THE RATIO IS A PREDICTION FROM TWO PUBLISHED DEVICE EXPONENTS, not a fit.** Deep in reduction a feed-back loop around a power-law divider settles at **1 + n*gamma** — the EL panel's brightness exponent (published "second to third power") times the CdS cell's gamma (published 0.58...0.92). Shipped mid-range values give **2.875:1**; the rendered curve measures **2.81:1** (2.2 %), inside the published bracket 2.16-3.76, against the reference's widely-quoted "about 3:1". Soft knee (1.34:1) stiffening with depth — the divider's own shape. **WHAT IS MISSED and reported per the honesty gate:** the manual ALSO describes the curve going near-horizontal above -20 dB (>20:1), and no choice inside the published exponent ranges reaches that. Not fitted.
- **Attack 9-10 ms across the WHOLE knob (spread 1.0 ms), and the spread is the bar with teeth** — the absolute window is close to reading the cell's own constant back, but knob-INDEPENDENCE is a prediction: §59.2 measured the OTA voice's attack moving **14 -> 3 ms** across SUSTAIN because its attack is a current-starved discharge. Also: 50 % release **47 ms** (published 60, window 40-80); ceiling **40.09 dB analytic / 39.64 dB rendered** (`Rs` is DERIVED from two published figures, and the analytic line is an Ohm's-law identity so it is JOINED by a slammed render — §57.9's lesson).
- **The control-law table now has three rows, and this pedal supplies two of them.** Squash SUSTAIN = **25.33 dB of GAIN** against 0.28 dB of output; Curfew THRESHOLD = **40.00 dB of THRESHOLD** against 0.00 dB of gain; **Lumen PEAK REDUCTION = 45.44 dB of THRESHOLD**, and **Lumen GAIN = 32.00 dB of output against 0.0000 dB of gain reduction** (the sidechain taps BEFORE the pot — a real property, and moving the tap after it is perturbation P7, which reads **11.8587 dB**). **MODE SHIPS, and §61.3 is why that needed a number:** the gate's MODE was refused because it changes what the footswitch does; this one changes the AUDIO by up to **7.71 dB** (LIMIT mixes a sourced 1/25 of the input into the sidechain), and it is asserted monotone in sign.
- **The honest expectations are largely the OPPOSITE of §59.6's, which is the point of having two compressors.** The pick attack **SURVIVES** (+0.93...+1.22 dB re settled, against the Squash LOSING 7.72-20.00 dB); it does **NOT invert phase** (the Squash does); **turning it up does not raise the noise floor** (idle gain +4.69 dB with **0.00 dB** of travel across the whole PEAK knob, against the Squash's 25.34 dB); and it is nearly **tone-neutral** (worst **0.12 dB** from 82 Hz up, against the Squash's -2.42 at 82 Hz / -2.88 at 10 kHz). What it IS slow at is letting go, by design.
- **It IS oversampled, and that was checked BEFORE the loop was written.** §61.7's gate declined OS on measurement, so this slice tried to: it cannot, because **the gain-reduction divider is a multiply by an audio-rate signal** (the panel is the rectifier, so the cell's conductance carries a 2*f0 ripple). Single 9 kHz tone: **1x -86.16 -> 2x -103.19 -> 4x -113.09 -> 8x -114.07 dB** — it MOVES with the factor (§54's tell; a two-tone stimulus would have reported a flat floor and measured nothing, §59.7's trap). Shipped **4x, latency 72**, and the shipped factor is pinned (§58.7's could-not-fail finding). The ADAA limiter's own half-sample droop is the second reason: **-1.99 dB at 10 kHz at base rate, -0.12 dB at 4x**.
- **A modelling error found by measuring the first draft, and reported rather than quietly fixed.** The trap occupancy's target was written as a LINEAR `dG/dG_max`; the cell's limit corresponds to the full 90 V panel drive, so at any playable level the occupancy sits at ~1e-3 and the program dependence measures **1.083x** — i.e. none. Replaced by the standard trap-kinetics (Langmuir) form `dG/(dG + dG_half)`, which needs ONE constant instead of an arbitrary normalization and produces BOTH dependences the reference is described by. That is perturbation **P2** (1.249x).
- **A second real bug, and it was `identical_core_test` that caught it.** The first version set the knob defaults inside `prepare()` (copying `CompressorEngine`), which **clobbers a `setParameter` made BEFORE prepare** — which is exactly what the test's reference chain does. Plugin vs reference differed by **2.4e-03** while engine vs reference was 0.000e+00, i.e. the fault was in the parameter path and nowhere else. Defaults moved to the CONSTRUCTOR (`OnePoleSmootherT::prepare` snaps value to TARGET, so they survive), and the opto's four parameters were added to the test's plugin driver — which is what makes slot 1 provably plumbed end to end. **Note for the next slice: comp / gate / delay / wah / chorus are still absent from that driver, so their board cases agree only because the reference's pre-prepare writes are being clobbered back to the same defaults. That is a latent gap, not a property.**
- **Research honesty: NO SCHEMATIC WAS READ, and there was no independent simulation either — the biggest caveat on this voice.** github.com only; `WebFetch` 403'd every other host; a GitHub search found no LA-2A netlist or `.asc`, so unlike §59 there was nothing to parse. §64.1 tabulates eleven SOURCED facts (the T4B's construction, the ~90 V panel drive, the CL-505L's <1 kOhm/>1 MOhm range, the photocell-as-bottom-leg divider, the 2M2 bleeder, both device exponents, the published attack/release/max-GR/response/THD figures, the COMPRESS/LIMIT sidechain blend — that last one a forum reading, flagged as weaker) against everything else RECONSTRUCTED. **Every dynamic curve in §64.4 is this model's own**, the tube stages are a linear gain plus one soft ceiling, and §57's rule applies verbatim: do not re-tune any of them toward a sound; find the schematic. §59's strongest asset was an outside SPICE run agreeing to 4 % that caught its one real bug; **nothing equivalent existed here**, and the substitute — a ratio predicted from two exponents — is one number, not a whole loop.
- **8/8 perturbations RED, every restore GREEN** (§64.9): the trap multiplier neutered (**2.892 -> 1.000x**), the linear trap normalization (1.249x), the EL exponent flattened, `Rs` 100 k -> 10 k, the LIMIT blend zeroed (**7.71 -> 0.00 dB**), a drive-scaled attack coefficient (spread **1.0 -> 4.0 ms**), the sidechain tapped after the GAIN pot, and the shipped OS default 4 -> 1. **One weak bar is NAMED rather than hidden:** `testRatioCurve`'s measured-vs-predicted comparison is computed from the same two constants, so P3 leaves it agreeing to 1 % — the bars that catch P3 are the PUBLISHED bracket and the knee shape, and both are in the same test. CPU **2.97 % of one 48 kHz stream** (the OTA voice is 7.92 %); DC on signal 0.0000 %; block-size invariance and `reset()` both **0.000e+00**; rate spread 0.0032 dB; zipper 1.00x; `maxAbsRestingState()` exactly **0.0** — after **137 s**, measured, because the trap state's tau is 2 s and it has to fall ~30 decades (nothing is denormal in the meantime; the output is exact digital silence within 5 s).
- **In flight (2026-08-01): the Orange Rockerverb 100 — the SIXTH amp voice, and the MODERN Orange** (docs **§63**, `docs/work/2026-08-01-rockerverb.md`, branch `claude/rockerverb-6f557i`) — ROADMAP M10.7, the second half of the owner's *"or120 and rockverb. I'm an orange man."* Amp voice **5** (`rockerverb`), wired end to end in one slice (core, C ABI, worklet-transparent, web UI + face + assistant, native engine + APVTS + editor). EL34 push-pull like the OR120 and the JCM800, **reusing that power machinery wholesale** (Koren EL34 fit, per-tube plate Newton, grid blocking, OT, rail/screen sag, and the `LtpInverter` with §42's `tailRef`) — **no new device model**. **ALL FIVE GOLDENS UNCHANGED at ±0.00 — nothing blessed, and a new voice cannot need it.** Core ctest **32 → 34 entries, 34/34** (exit code checked UNPIPED); native 3/3 with a sixth `identical_core_test` board at **0.000e+00**; node 15/10/12; electron 20; WASM artifact rebuilt (**91** hashed inputs).
- **THE RESEARCH CHANNEL WAS BETTER THAN §57's, AND IT IS A REAL NETLIST.** The proxy still permits **github.com only** (re-confirmed: `el34world.com` and `en.wikipedia.org` both fail `CONNECT tunnel failed, response 403`; `WebFetch` 403s on el34world's Rockerverb PDF and on dirtboxlayouts). But a GitHub code search found **`Orange Rockerverb 50 Preamp.schx` shipped as an EXAMPLE INSIDE LiveSPICE** (`dsharlet/LiveSPICE`, mirrored in four other repos) — a schematic FILE with every component's value, position and wire, so the netlist is *recoverable exactly*. It was parsed node by node (terminal offsets out of LiveSPICE's own `LayoutSymbol` code, the rotation/flip transform out of `Schematic/Symbol.cs`, the pot's `R(cathode→wiper) = R·P` convention out of `Potentiometer::Analyze`, wires resolved by union-find with collinear merging). **The DIRTY-CHANNEL PREAMP IS TRANSCRIBED; the power section, the PI and the supply are a documented RECONSTRUCTION** — §63.1 tabulates which is which.
- **ONLY THE DIRTY CHANNEL SHIPS, and that is the research discipline, not a shortcut.** The netlist is the dirty channel's; a clean channel would be pure invention and a footswitch in front of an invented channel is worse than no footswitch. Named as the §63.11 follow-up, and the ROADMAP entry is amended with the reason. The **ATTENUATOR** is not modelled either (a post-OT load device).
- **THE BAR HAS TWO INDEPENDENT HALVES, both hard asserts, because these two amps are the same manufacturer and the same output valves.** **(a) THE TONE NETWORK IS A MARSHALL-LINEAGE FMV** — 560 p / 39 k slope / 22 n / 22 n, treble 250 k lin, bass 500 k log, mid 25 k lin. On §57.4's own scale-free metric it measures **−6.01 dB (a mid SCOOP) against the OR120 James's +0.75 (a BUMP) = 6.76 dB of contrast** against a shipped **5.0** bar (1.76 dB of margin, **RECORDED not snugged**), and **−0.72 vs +2.78 = 3.50 dB on the COMPOSED amps** against a **3.0** bar (0.50 dB — the tighter of the two, and reported as such). The −6.01 lands within **0.02 dB** of the figure §57.4 measures for the actual Marshall FMV: on this axis **the modern Orange IS a Marshall and the vintage Orange is not**, which is the most surprising thing the netlist says. **(b) THE MASTER VOLUME DECOUPLES DRIVE FROM LEVEL** — level-matched at −20 dBFS the Rockerverb makes **29.75 % THD against the OR120's 1.89 %, a 15.76× (23.95 dB) ratio** against a shipped **5×** bar; and across VOLUME 0.01 → 0.10 at a fixed GAIN it moves **19.89 dB of level for a THD ratio of 0.971**. A no-master amp cannot do that at any setting.
- **The third structural difference is REPORTED, not asserted as the bar, because it is an INFERENCE.** The PI is a **long-tailed pair** — sourced only as "the phase-inverter valve is an ECC83 in its own socket", i.e. a WHOLE dual triode, where the OR120's Field Guide says *"Cathodyne type: 1/2 x 12ax7"*. Measured: balance **0.971988** after a resistor sweep (Ra2 120 k / tailRef −12 V, legs **−25.4276 / +24.7153**, both plates 81–83 % of B+, Ip1 0.829 mA) against the cathodyne's **0.999965 by TOPOLOGY with no calibration at all**, and its legs AMPLIFY (×25) where a split load cannot (×0.97). **The sweep landed on the SAME Ra2 = 120 k §45's independent sweep found for the 2204** — same tube, same 10 k tail, similar B+, so the same compensation. What IS asserted: anti-phase (the signed product of the leg gains), balance > 0.90, both legs > ×10.
- **NO NEW PARAM ID, and that is the other half of §57's rule.** GAIN → the JCM's `gain` (10), the post-stack VOLUME → the JCM's `master` (12), BASS/MIDDLE/TREBLE → 1/2/3 (**the first Orange in this repo with a mid**), REVERB → 9. §57 gave the F.A.C. a NEW id because no other voice had a six-position switch; here the FUNCTION matches exactly, so the slot is reused. **The panel word and the slot deliberately disagree in one place**: the knob printed VOLUME binds to the MASTER slot, because it sits after the tone stack and is a master by function — the same doctrine §57.11 records for the OR120 printing GAIN on slot 0. **NO NEW CAB either**: it reuses `orange412` (same PPC-style 4×12), so §57.11's native `CabChoice`-divergence trap is untouched.
- **Three things this slice measured and REPORTED rather than tuned.** (1) **The amp does not quite make its rated 100 W** — composed cranked tops out at **93.9 W into 8 Ω** and the power section's own sine ceiling is **82.15 W with feedback / 84.78 W without**, so §42's "smallest scale that reaches rated power" was again unusable and `kInterstageScale` is an **UN-FITTING to 1.0** (justified by an 80× sweep instead: the onset moves 0.35 → 0.10 and the power is flat from 0.25 up). **Do not close it with a screen filter, softer grid coupling or a higher `kVsupply`** — same instruction, same suspects, as §57.3's 93 W. (2) **The VOLUME pot is LINEAR and ships linear** — the `.schx` marks Gain and Bass `Logarithmic` and Treble/Middle/Volume `Linear`, i.e. its author distinguished them; the useful range is genuinely the bottom of the travel (−25.6 dB at 0.05, −7.3 at 0.5) and the assistant coaches 5–15 rather than apologizing for it. (3) **Breakup onset is EARLY at GAIN 0.20** (against the JCM's 0.31 at the same probe) because this is a FOUR-stage preamp whose GAIN pot sits AFTER the first stage — the netlist's arrangement, not a fitted one.
- **ONE MODELLING DEPARTURE, named up front, and it is a `TriodeStage` limitation not a shortcut (§63.3).** `TriodeStage` uses ONE `(Cc, Rgl)` pair for both its input and its output coupling, so a cascade applies every interstage high-pass TWICE. That is inaudible in a uniform cascade (the 2204's 7 Hz, the OR120's 2.5 Hz) and a **gross error here**: the real caps are 1 n into 400 k = **398 Hz**, 2n2 into 540 k = 134 Hz, 4n7 into 690 k = 49 Hz. So every real coupling cap lives ONCE inside the interstage MNA where its divider is, and each stage carries a transparent 1 µF coupling with `Rgl` = the network's real resistive input and `Rg` = that grid's real source impedance. **The cost, stated: preamp grid BLOCKING is not modelled in this voice** (grid conduction clamping is, and the EL34 blocking that dominates a cranked amp is fully present). **Do NOT "fix" it by setting `Cc` to the real cap** — that re-introduces the doubled corner. The proper fix is independent input/output coupling configs on `TriodeStage`.
- **ONE XFAIL, and it is the OR120's defect appearing on a second amp.** `rockerverb-alias-44k1`: the composed cranked alias floor is **−80.1 dB at 48 kHz / 4× and −52.7 dB at 44.1 kHz** against the same −56 bar §57.7 registered (the OR120 measures −50.8 there). It is genuine foldover — it improves **11.3 dB at 8×** and beats 1× by **28.1 dB** — and **two amps failing the same bar at the same rate, both running per-triode oversampling domains into a separately oversampled power section, is the architecture speaking**. The "4× must beat 1× by ≥ 12 dB" clause stays HARD at both rates. Repo ledgers **5 → 6**; the fix is one shared OS domain around the whole cascade (§57.13's own candidate), never a lower bar. Latency **360 samples / 7.50 ms** — the deepest cascade of any voice — is the second reason to do that slice.
- **The rest, measured:** preamp DC S1–S4 at **1.085 / 1.296 / 0.956 / 1.168 mA** with every current cross-checked against Ohm's law on the transcribed 100 k (these are ABOVE the 0.5–0.9 mA window and that is CORRECT — that window is for 220 k-load stages, the §57.3 point); EL34 quad **33.89 mA / 16.59 W = 66 % of 25 W**; supply **400 V → 355.18 → 331.49 V** through the transcribed 10 k pair, asserted as ABSOLUTE windows rather than the Ohm's-law identities §57.10 caught; loop depth **6.65 dB and FLAT** (0.26 dB spread 220 Hz→5 kHz — this panel has no presence control at all, unlike both siblings); network discretization worst **0.55 dB** against a 0.60 warp bound; knob authority BASS **+9.77** @82, MIDDLE **+8.93** @650, TREBLE **+10.80** @5 k; DC on signal **0.2152 %** clean and **0.2157 %** with +0.1 V of input offset; reset + ragged 128-frame blocking **0.000e+00**; rate spread **0.007 dB** over 44.1–96 kHz; every zero-resting companion **exactly 0.0** after a 4 s silent tail.
- **THE MID POT IS NOT A RHEOSTAT, transcribed and asserted.** Both sections sit between node C and ground, so C→GND is 25 k at EVERY position; what the knob moves is where the 22 n cap TAPS IN. Measured consequence: MIDDLE moves **8.93 dB at 650 Hz and 1.85 dB at 20 Hz**, and the ratio is asserted so nobody can quietly "tidy" it into the canonical FMV rheostat. **And the two GAIN pots are ONE knob — SOURCED, not assumed**: both carry `Group="Gain"` in the schematic file, LiveSPICE's own gang marker.
- **In flight (2026-08-01): the LAST duplicate envelope follower — settled by MEASUREMENT, and the answer is a REFUSAL** (docs **§58.8**, **ADR 023**, `docs/work/2026-08-01-wah-detector-unify.md`, branch `claude/wah-detector-unify-6f557i`). §58.5, §58.7 item 5, ADR 019 and ADR 021 all named the same follow-up: three envelope followers were written in one week on three parallel branches, §61 extracted two into a real component (`SidechainDetector`), and the wah's was the last one standing. **Outcome (b): they are genuinely different circuits, the wah keeps its own follower, and `SidechainDetector` was NOT widened.** ADR 021's own prohibition — *"not to grow it a parameter per voice until it is a union of three models"* — is what bound the slice, so the refusal had to be earned. **Scope: core tests + docs + two header banners.** Core ctest **32/32**; ALL FIVE GOLDENS **UNCHANGED at ±0.00**, nothing blessed; compressor and gate **BIT-IDENTICAL by render hash** (four `clipper-render` renders, `cmp` byte-for-byte); WASM artifact rebuilt because two `core/include` comment blocks changed.
- **THE DECIDING NUMBER: `SidechainDetector` IS A THRESHOLD DETECTOR, AND SO IS THE COMPRESSOR'S OWN.** The metric is the **proportional range** — the input dB span over which the envelope travels 10 % → 90 % of its swing, i.e. how much of a player's pick-strength range the control answers proportionally. Open loop: **M13.1 compressor (10 µF/150 kΩ) 1.950 dB · M13.6a gate (47 nF/220 kΩ) 2.384 dB · best wah-plausible config 6.711 dB · the wah's own one-pole on `|x|` 19.085 dB with NO threshold.** The clamp is a DC restorer into a base-emitter junction, so conduction is exponential and a conducting 2N3904 delivers orders of magnitude more than the envelope resistor's pull-up. **The compressor's GRADED response is its FEEDBACK LOOP, not the detector** — §59 measured the same fact from the other side (216:1 feed-back vs 3.3:1 feed-forward) — and the gate feeds it to a comparator, with §61.4 *building* a 40 dB dB-linear threshold on exactly that steepness. **A wah has no gain to reduce, so it is feed-forward by necessity and cannot borrow the loop.**
- **The drive gain is a PURE LEVEL TRANSLATION, so the range is set by the current balance alone** (identical to 3 dp across 1 → 300× of drive). With the published 166.7 ms release pinned as `R_env·C_env`, widening it means pushing R_env down: **16.7 k/10 µF → 6.64 dB · 1.67 k/100 µF → 13.81 · 167 Ω/1000 µF → 17.09 · 16.7 Ω/10 000 µF → 18.09**, still 1 dB short. **The wah's ideal rectifier IS the R_env → 0 limit of this detector** — the limit where the transistor's exponential is swamped. That is a different circuit, not a component value; a 10 000 µF cap behind a 17 Ω resistor across a 9 V rail is a power supply.
- **The substitution was BUILT AND RUN, not argued, and every §58.6 AUTO number regressed.** `WahModel` re-plumbed onto a `SidechainDetector` in a scratch tree, given the topology's best shot (envelope pair pinned to the published release, clamp in the gate's op-amp precision-rectifier form, calibrated at ONE point — the follower's own 0.25 V reference). Full-SENSE sweep **1.534 → 0.958 octaves**, which fails §58.6's *existing* `prevOct > 1.0` acceptance bar; SENSE 0.25/0.50/0.75 **0.381/0.762/1.146 → 0.239/0.478/0.718**; t_peak **82.7 ms flat → 144.0 ms**; t_back **840 → 439 ms** *even though `R_env·C_env` is exactly the shipped release τ*. **The real damage is level dependence:** sweep depth vs pick strength at SENS 1.0 goes **0.101/0.254/0.508/0.762/1.018 → 0.000/0.000/0.000/0.014/0.207** octaves at 0.02/0.05/0.10/0.15/0.20 V — a substituted wah **does not move at all at the house clean probe level (0.10 V)** — and t_peak becomes non-monotone across 56–179 ms.
- **One consequence is a FORMAL CONTRADICTION, not a voicing regression: the two designs sit on OPPOSITE SIDES of ADR 006's scope rule.** The wah's `env` rests at **zero**, so it is `flushDenormal`-guarded and `maxAbsRestingState()` is asserted **exactly 0.0**; the detector's envelope node rests at the **SUPPLY RAIL** and is explicitly NOT guarded, with that reasoning written into `SidechainDetector.h`. Substituted, the wah measures **2.675e-13** and that bar goes red. One object cannot be both. CPU also rises ~27 % (interleaved same-machine A/B, 6 pairs, median **0.484 → 0.617 s** per 10 s = 4.8 % → 6.1 % of one stream) — a Newton solve per leg per sample where there were three flops. **What survived: SENS = 0 is still bit-identical (0.000e+00)**, because the envelope term is multiplied by zero — that bar could never have distinguished the two.
- **The refusal has TEETH: `testFollowerLevelLaw`, three player-observable bars, all perturbation-proven.** (1) a QUIET 0.05 V pick still opens the filter — **0.254 oct** against a `> 0.15` bar; (2) sweep depth is PROPORTIONAL — **2.002 / 4.014** for 2× / 4× the pick strength (not an identity: the tank's `f0(p)` law is nonlinear, so proportionality in OCTAVES is a prediction about the FOLLOWER); (3) time-to-peak does not move with level — **0.00 ms of spread across 12 dB**. **Perturbations: P1** the whole substitution → RED (and it trips §58.6's existing bar *first*, so it was re-run with the new test ordered ahead, RED on bar 1, then again with bars 1–2 removed, RED on bar 3 — all three proven independently); **P2** a Vbe-style deadband on `|x|` → RED on bar 1; **P3** attack coef scaled down by env → RED on bar 1; **P3b** attack coef scaled up by env → RED on bar 2. All restores GREEN, `touch`ed after BOTH patch and restore.
- **P4 is an honest NON-failure and is reported as one.** A HALF-wave rectifier moves t_peak **82.7 → 90.0 ms** and scales every depth by 0.783 uniformly, but the spread stays **0.00 ms** and the ratios stay 2.001/4.010 — so bar 3 stays GREEN, correctly: it asserts level-INDEPENDENCE, not the value 82.7 ms. That confirms §58.6's attribution of the 82.7 ms to the rectifier's zero crossings while showing the *law* belongs to the linear follower.
- **The rule this leaves for M13.3, and it is procedural not a veto.** The optical voice is still expected to reuse `SidechainDetector` — it is a feed-back compressor with a gain cell, the case the component is built for. **"Both blocks rectify and integrate" is not a reason to share and not a reason to refuse: build the substitution and measure it.** And the honest reason the wah is different — **the GCB-95 has no envelope follower at all.** §58's AUTO mode is a synthesised feature whose constants come from a published *behavioural* spec (Geofex ~10 ms attack / ~500 ms drift-back), not a netlist; forcing it onto a transcribed component would have meant inventing part values to hit a published time constant, which is the fitting §57 spent a whole slice undoing.
- **In flight (2026-08-01): the "Echoman" BBD analog delay — M13.4, the lineup's FIRST DELAY and a whole new DSP family** (docs **§60**, `docs/work/2026-08-01-delay-memory-man.md`, branch `claude/delay-memory-man-6f557i`). An EHX Deluxe Memory Man-class **bucket-brigade** delay as pedal type `delay` (slot **8**, pre-reserved), knobs DELAY / FEEDBACK / BLEND, wired end to end on both fronts in one slice. **The reusable deliverable is the primitive:** `core/include/clipper/dsp/DelayLine.h` is a plain interpolating ring buffer that knows nothing about bucket brigades, feedback or companders — M13.6's flanger and any future tape echo are cheap only if that separation is right, so it carries its own bars in `clipper_delay_tests` rather than being an untested header. **ALL FIVE GOLDENS UNCHANGED at ±0.00, nothing blessed** (the delay is in no golden rig and touches no other model). Core ctest **29 → 30 entries**; native 3/3 (a new `RAT → Echoman → JCM800` `identical_core_test` case at **0.000e+00**); Playwright **77 passed**; node 15/10/12; electron 20; WASM artifact rebuilt (**80** hashed inputs — `DelayModel.cpp` had to be added to `build-wasm.sh`'s source list, which is inside the `STAMP:EMCC-ARGS` markers).
- **The three things the brief said must be modelled, and what each measures.** (1) **The BBD is a SAMPLED device and the DELAY knob moves its CLOCK.** Two MN3005s = 8192 stages = **4096 sample slots**, delay = `4096/f_clk` — a law CHECKED against the datasheet's own endpoints (`4096/(2·10 kHz) = 204.8 ms` and `4096/(2·100 kHz) = 20.48 ms`, both exactly). The input is sample-and-held at the clock; the filters are FIXED. (2) **The compander is the NE570's actual sqrt/square pair**, 2:1 in and 1:2 out, unity at every level by construction, with **two independent rectifiers** — the expander's sees the delayed, degraded signal, and that disagreement IS the breathing. (3) **Degradation is cumulative**: FEEDBACK returns to the COMPRESSOR'S INPUT, so every repeat goes through the compander, both filters, the S/H and the charge smear again — **9.92 dB of HF lost from repeat 1 to repeat 5**, monotone, ~2.5 dB per pass.
- **The darkening is DERIVED from the device, not fitted.** A BBD stage leaves a fraction ε of its charge behind; N of them cascade to `exp(−μ(1−z⁻¹))` — a **Poisson kernel** with μ = N·ε/2 = 0.4096 slots — applied once per slot AT THE CLOCK, so its −3 dB corner is a fixed fraction of it: **`0.2254·f_clk`**, i.e. 30.8 kHz at 30 ms down to **1.68 kHz at 550 ms**. Measured consequence: repeat 1 is **8.99 dB darker at 550 ms than at 30 ms**. A fixed filter on a variable-length digital line measures 0.00 dB there — P1 and P2 prove it (kCte → 1.0 collapses the per-pass loss from 2.54 dB to **0.02**; removing the S/H makes it *reverse*).
- **THE OVERSAMPLING FACTOR IS 8×, DERIVED FROM THE DEVICE, and it is a real exception to the house 4×.** The BBD clock reaches **136.53 kHz** at 30 ms, so the internal rate must clear **273.07 kHz** or the device's own clock images fold inside the oversampled domain. 4× at 44.1 k is 176.4 kHz and does NOT clear it. Measured worst non-harmonic product at DELAY 0.0: **−65.5 dB at 4× → −122.1 dB at 8×**, for 2.9 % → **5.5 %** of one 48 kHz stream and **zero** latency change. At the OTHER end the device aliases and that is CORRECT (a 9 kHz tone into a 7.45 kHz clock): −9.66 dB at 1× / −16.23 at 4×, and no host oversampling can or should remove it.
- **`latencySamples()` returns 0 by design, and the ~1.75 ms offset is REPORTED not compensated.** The dry path never enters the oversampled domain — that is what keeps **BLEND 0 bit-identical to bypass** (0/48000 samples differ, hash `6276ea7e3109b4c7` both sides). So the oversampler's 76-sample group delay lands on the wet only: the delay-time table reads law + 1.75 ms at every position, while the **repeat SPACING carries no offset at all** (30.67 / 290.10 / 550.69 ms against laws of 30 / 290 / 550). Compensating the read would have made the spacing wrong instead.
- **Two claims REFUTED by measurement, and one clock range reported rather than clamped.** (1) The `pedalkernel` BBD reference delays the compressor's envelope and feeds it to the expander; a real NE570 has one rectifier per half, each on its own input, so **nothing delays an envelope** — the netlist wins and the breathing is the consequence. (2) **The DMM runs its BBDs past BOTH ends of the MN3005's spec sheet**: EHX's published 30–550 ms with 4096 slots means **136 533 Hz** (36 % above the rated 100 kHz) down to **7 447 Hz** (26 % below the rated 10 kHz). The published TIME range is the stronger source, so it wins and the excursion is documented. (3) **"FEEDBACK 0 gives exactly one repeat" is 100 dB down, not bit-zero** — echo 1 measures 6.14e-03 and echo 2 **1.70e-22**, which is the input filters' own ring-down arriving one delay later through a continuously-fed line, not a second pass.
- **`clipper_denormal_tests` FOUND A REAL DEFECT this slice had shipped, and the mechanism is worth knowing.** A ring-down at ~1e-40 is a perfectly **normal double**, so nothing upstream flushes it — but `float(1e-40)` is **SUBNORMAL**, and that is where it leaves the model. Worse, the halfband's tap sums CANCEL: the difference of two ~1e-30 floats agreeing to a ULP is ~6e-42. Measured over a 200 s tail at DELAY 1.0 / FEEDBACK 0.8: **5395 → 1666 → 0** subnormal output samples as the emitted sample and then the mixed output were flushed. **Everything recursive in this pedal rests at exactly zero, the 550 ms line included** (a decaying feedback tail circulating float subnormals through a quarter-million-sample ring is exactly ADR 006's case, and WASM has no FTZ at all).
- **The shared denormal bar had to be re-aimed, and the reason is physics not a guard.** "Exact digital silence within 7 s" is unreachable for a feedback pedal at a long setting: measured settle-to-exact-zero after 2 s of program is **0.26 / 0.78 / 1.08 s at 30 ms** (FB 0 / 0.5 / 0.8), **0.52 / 7.68 / 15.55 s at 290 ms**, and **0.78 / 15.51 / 199.85 s at 550 ms**. 200 s is a loop gain of 0.736 per 550 ms pass = 4.8 dB/s over 590 dB — the pedal WORKING. Subnormals are **zero at all nine settings**; the shared test runs the 30 ms case and the long line is owned by `clipper_delay_tests` over a 40 s tail.
- **`reset()` deliberately does NOT reproduce a fresh model bit-for-bit.** It re-zeroes the BBD clock PHASE, which is a physical quantity with no canonical value, so two models differing only in phase sample the input at different instants within one clock period and their echoes differ by a sub-clock-period timing (~2.4e-03 on a 0.15-peak echo). The bar is the steady-state LEVEL instead: **0.016 dB**. One NaN poisons 1/256 samples and `reset()` takes it to **0/96000**.
- **A trap that nearly went the wrong way, and it is a MEASUREMENT bug not a DSP one.** The first alias test reported **−55.8 dB that did not move with the oversampling factor** — which reads like §54's "not aliasing" tell and was actually the **rectangular window's own sidelobe**: a 3 kHz tone read at 3409 Hz with 2 Hz bins leaks `1/(π·204) = −56 dB`, the number measured to the decimal. A Hann-windowed Goertzel put that 90 dB lower and the real floor appeared — **along with the 56 dB step at 8× that the whole oversampling derivation turns on**. Before trusting a flat-in-the-factor floor, check the window.
- **Research honesty: the DMM's SCHEMATIC was never read, and the filters are borrowed.** `ehx.com`'s own schematic thread, `electronicservicemanuals.com`, `freestompboxes.org` and every datasheet PDF (MN3005, NE570, AN174) **403 at CONNECT** — github.com only, as §59 already recorded. What WAS read: a full working implementation of Holters & Parker's DAFx-18 BBD model in `chowdsp_utils` (cloned), and that is where the **fixed 9900 Hz / 9500 Hz** anti-alias and reconstruction cutoffs come from — **they are the JUNO-60 chorus's, NOT the Memory Man's**, and are stated as such. Also unsourced: the CD4047's timing network, this pedal's `C_RECT`, the BBD bias trimmer, the feedback pot's taper, and the MN3005's charge transfer efficiency specifically (`kCte = 0.9999` sits inside the literature's sourced 0.9998…0.99995 band and is a **stated modelling parameter**). **There is no published DMM response curve in reach — every curve in §60.4/§60.5 is this model's own. Do not re-tune the filters toward a sound; find the sheet.**
- **Perturbation-proven 6/6 RED, restore GREEN** (§60.8), re-run after the denormal fix: kCte → 1.0, the S/H removed, the compander bypassed, the bucket compliance removed (30 s peak at FEEDBACK max **1.14 → 8.04**, i.e. the loop runs away), a 0.001 BLEND floor (**34 000 of 48 000** samples differ), and one MN3005 instead of two. **CPU 5.5 % of a 48 kHz stream** (18.0× realtime); block-size invariance and two-fresh-models both **exactly 0.0**; rate spread **0.0001 dB** over 44.1–96 kHz with the delay at 238.000 ms at every rate; DC on signal **0.0000 %** of peak with and without a +0.1 V input offset.
- **Named follow-ups this slice leaves:** an **ADR is needed and its number was NOT chosen** (assigned centrally; 019 is the highest taken) covering *the primitive/device split* and *the device setting the oversampling factor* — both are decisions a future slice could reasonably "fix" back · **fold `ChorusModel`'s private cubic ring onto `DelayLine`** (it still indexes with `%`, docs §32's cost, and it is in the `clean120_chorus` golden, so it needs its own bit-identity slice) · the DMM's own chorus/vibrato section, once the parallel CE-1 lands · **`kMaxChain` is one-instance-per-type natively**, so a short slap into a long wash works on the web and not in the plugin.
- **Found in passing and NOT fixed (it is not this slice's):** `native/src/PedalCard.cpp`'s `kFaces` table has the **WAH and COMP entries at indices 6 and 7 SWAPPED** relative to `PedalType` (`PEDAL_WAH = 6`, `PEDAL_COMP = 7`), so the native editor labels a Weeper card "Squash" and vice versa. Pre-existing on `main` from the parallel wah/comp merge; one-line fix, belongs to those slices.
- **In flight (2026-08-01): the "Curfew" NOISE GATE — M13.6a, the first UTILITY and the SECOND CONSUMER of M13.1's detector** (docs **§61**, `docs/work/2026-08-01-noise-gate.md`, branch `claude/noise-gate-6f557i`). A Boss NS-2-class noise suppressor as pedal type `gate` (slot **9**, pre-reserved; SLATE accent; wordmark "Curfew", `UTILITY Nº8 · GATE`), wired end to end on both fronts in one slice. **ALL FIVE GOLDENS UNCHANGED at ±0.00 — nothing blessed, nothing written.** Core ctest **29 → 30 entries, 30/30** (new `clipper_gate_tests`, no XFAIL ledger — zero known-bad properties); native 3/3 with a sixth `identical_core_test` board at 0.000e+00; node 15/10/12; electron 20; WASM artifact rebuilt (80 hashed inputs). **Web-suite caveat, environmental not code:** three full Playwright runs gave three DIFFERENT failing sets (20/4/4), every one including pre-existing specs this slice does not touch, and every failure passes in isolation — `playwright.config.ts`'s own documented Chromium `OfflineAudioContext` flake. The new `gate worklet` spec measures **hiss drop 80.00 dB, note change 0.000 dB** in isolation every time. Port 4173 was taken mid-session by a PARALLEL agent's worktree; the later runs used a throwaway config on another port (deleted) — do NOT `pkill` a sibling's preview server.
- **THE POINT OF THE SLICE WAS ADR 019's SEAM, AND THE VERDICT IS: THE CLAIM HELD, THE SEAM AS WRITTEN DID NOT — corrected, NOT widened.** ADR 019 said the gate would be *"this same `Sidechain` feeding a different `ControlMap`"*, and named the risk that a seam written before its second consumer can be the wrong one. Four findings, all in §61.2 and now in the `CompressorEngine.h` banner and an ADR 019 amendment: **(1) RIGHT** — the detector really is the shared part; the gate changes nothing but component values (envelope pair 10 µF/150 kΩ = 1.5 s → **47 nF/220 kΩ = 10.34 ms**, and EQUAL leg source impedances because a gate's rectifier is driven by an op-amp, not a phase splitter), and no second envelope follower was written. **(2) WRONG (structure)** — `Sidechain` was a config struct with **no code attached**; the detector was `CompressorEngine::detectorLeg()`/`::advanceEnvelope()`, both **private**, with their state in that class's members, so there was nothing a second consumer could hold. It is now the standalone **`SidechainDetector`** owned by value by both pedals, and the move is **bit-identical for the compressor** (two `clipper-render` renders `cmp` byte-for-byte; comp/denormal/NaN suites unchanged). **(3) WRONG (`ControlMap`)** — it is three resistor values that make an OTA *bias current*, not a policy hook; a gate has no rheostat, cell pin or control current, so `GateModel` carries its own `GateControl` and **`ControlMap` was NOT widened**. **(4) WRONG (`detectorFromOutput`)** — for a gate feed-back is not a variant, it is a **latch**: once it closes the detector sees only the VCA's off-isolation and can never re-open (**−0.02 dB feed-forward vs −80.02 dB feed-back** on the same note, and the feed-back build never opens at any level). **M13.3's rule is narrowed: reuse the DETECTOR, expect the CONTROL side to be your own.**
- **THRESHOLD IS A THRESHOLD, and the contrast against §59's SUSTAIN is the headline pair.** Across the whole knob the open level moves **40.00 dB** (−59.05 → −19.05 dBV, monotone, worst |measured − stated| **0.01 dB**) while the gain the pedal applies when it IS open moves **0.0000 dB** (−0.0176 dB at every position — the two 7.234 Hz coupling caps, i.e. unity). §59.4 measured the Dyna Comp's SUSTAIN doing the mirror image: **25.33 dB of gain against 0.28 dB of output**. Both directions are asserted. The SHAPE is the real property, not the offset: the detector between the log pot and the comparator is EXPONENTIAL, so a dB-linear threshold across 40 dB is a prediction. Absolute windows at the 0.35 default, also asserted: a −60 dBV single-coil hiss floor stays SHUT, a 0.15 V note OPENS.
- **HYSTERESIS IS TAKEN IN THE SIDECHAIN, and the measurement is why.** The obvious place — a fixed voltage offset on the comparator reference — is worth **0.21 dB** of input level here, because the detector's transfer is exponential. So the positive feedback goes round the **sidechain attenuator** instead (+6 dB while open), which makes the gap a defined number of dB: measured **6.09 dB** on an up-then-down level ramp. **THE CHATTER TEST is the bar that carries the slice**: a decaying low E (82.41 Hz, τ 0.9 s) gives **1 open / 1 close** at THRESHOLD 0.35/0.50/0.65, and **5/6/7** with the hysteresis removed — the mechanism being the detector's own 2·f₀ ripple against a 10.34 ms envelope.
- **THE GATE IS NOT OVERSAMPLED, and that was decided mid-slice BY MEASUREMENT.** The first draft ran the whole thing at 4× like every other nonlinear pedal. On the worst stimulus available (a 9 kHz tone forced open and shut at 4 Hz) the non-harmonic floor measures **−176.93 dB at 1×** and −166.45 / −162.18 / −162.79 at 2/4/8× — oversampling makes it slightly WORSE, because the signal path is a MULTIPLY and the sidechain reaches the output only through a 400 Hz control ramp. 72 samples of latency and ~2× CPU deleted; `setOversampling` is the phaser's no-op and the test asserts the render is **bit-identical at 1/2/4/8×** and `latencySamples() == 0`. The open threshold is unmoved 82 Hz–10 kHz (1× vs 4× within **0.09 dB**).
- **TWO KNOBS, NOT THREE, AND THAT IS A RESEARCH FINDING.** The reference exposes THRESHOLD, DECAY and a MODE switch — but per Roland's own support article MODE does not change the audio at all, it changes what the FOOTSWITCH does (Reduction: the switch bypasses suppression; Mute: suppression is always on and the switch mutes). This board's footswitch is bypass on every pedal, so shipping MODE would be dead UI or a footswitch that means something different on one pedal. Not shipped, documented (§61.3). Slot 1 carried and unused — the compressor's precedent. Fixed ATTACK **0.40 ms** and fixed HOLD **30.0 ms**, both the gate literature's own numbers (0.1–1 ms guitar attack; 20–30 ms of built-in hold), because the reference exposes neither.
- **THE RESEARCH HONESTY ITEM: NO SCHEMATIC WAS EVER READ — this is the §57 situation, not the §59 one.** Reachable (search extracts only; `WebFetch` 403'd every host including Wikipedia): Boss's own copy ("a high-quality VCA and high-speed envelope-detecting circuits", the expander acting *below* the threshold), the manual's control complement, and ONE forum circuit description naming a gain stage into "various stages with diodes" feeding an **M5207 dual VCA** — which is exactly M13.1's detector shape and is why the reuse is defensible. Unreachable: freestompboxes, ronsound, elecsprout, schematicsonline, eserviceinfo and the Roland manual PDF itself; a GitHub search found no netlist. **So every component value in `GateModel.cpp` is a documented reconstruction** — the threshold range in dBV, the decay range, the VCA's −80 dB off-isolation and whether the real NS-2 has hysteresis at all are ALL this model's own. §57's rule applies verbatim: do not re-tune them toward a sound; find the schematic. The device cards are NOT reconstructed (the shared 2N3904/1N4148 in `SidechainDetector.h`).
- **The rest, measured:** pick attack through an opening gate loses **0.01 dB** of 20 ms peak / **0.02 dB** of rms and is **0.00 ms** late against a held-open reference (the compressor loses 7.72–20.00 dB of the same transient); noise reduction **80.01 dB** = the modelled VCA off-isolation; DECAY −20 dB point **97.8 / 197.4 / 512.4 / 1508.5 / 4658.6 ms** across the knob; DC on signal **0.0000 %** both stimuli; ragged-block invariance **exactly 0.0**; `reset()` vs a fresh model **0.000e+00** and one NaN 246/256 → **0/48000**; zipper **1.00×**; rate spread **0.02 dB**. Denormal scope by measurement (ADR 006): the four coupling-cap states are guarded and `maxAbsRestingState()` is **exactly 0.0**; the envelope node (**9.0000 V**) and the VCA gain (**1.0000e−04**) are NOT, each at a real operating point. **6/6 perturbations RED, restore GREEN** — including P6, whose FIRST version could not fail (it read `control().holdSeconds` straight back, so moving the constant moved the bar); replaced by the measured form, unity surviving **35.8 ms** after the comparator closes against **5.5 ms** with the hold removed.
- **TWO PRE-EXISTING NATIVE BUGS FOUND ON THE WAY IN (§61.10), both in `PedalCard.cpp`'s `kFaces`.** It was a `[PEDAL_TYPE_COUNT]` array **indexed by PedalType**, and (1) the **wah's and compressor's entries were in the WRONG ORDER** — `PEDAL_WAH = 6`, `PEDAL_COMP = 7`, but Squash was listed first, so the native editor drew each pedal's face on the other; (2) the slot-reservation commit widened `PEDAL_TYPE_COUNT` to 11 while the table still had **8** entries, so types 8/9/10 resolved to a value-initialized face with **NULL** strings — and `showTrayMenu()` loops to `PEDAL_TYPE_COUNT`, so the gear tray was already offering all three. Fixed structurally: `PedalFace` gains an explicit `int type`, `kFaces` is unsized, `pedalFace()` is a keyed lookup with a RAT fallback, and a new `pedalHasFace()` filters the tray and swap menus. **The delay's and chorus's slots are untouched** — they add a pair when their slices land.
- **ADR 021** — "a shared sub-circuit is a COMPONENT, not a config struct", amending ADR 019: its seam was corrected by its first real consumer. ADR 019's own text carries an amendment pointing at §61.2. Named follow-ups: find the NS-2 schematic; a gate before AND after the dirt. **The wah-follower unification named here was TAKEN AND REFUSED on 2026-08-01 — docs §58.8, ADR 023: `SidechainDetector` is a THRESHOLD detector (1.95 dB of open-loop proportional range here, 19.09 for the wah's one-pole) and the wah keeps its own follower.**
- **In flight (2026-08-01): the Boss CE-1 Chorus Ensemble — M13.7, pedal type `chorus`** (docs **§62**, `docs/work/2026-08-01-ce1-chorus.md`, branch `claude/ce1-chorus-6f557i`, **ADR 020** for the un-ganged INTENSITY decision). Slot `PEDAL_CHORUS = 10` (pre-reserved, never renumbered), wordmark **"Ensemble"**, `MODULATION Nº8 · ENSEMBLE`, MAGENTA accent, knobs **RATE / DEPTH / MODE**. Wired end to end in one slice (core, C ABI `chorus_*`, worklet, web face + tokens + assistant, native engine/APVTS/PedalCard). **ALL FIVE GOLDENS UNCHANGED at ±0.00, nothing blessed** — and `clean120_chorus`, the one that would move if this slice had disturbed the amp, is among them. Core ctest **29 → 30 entries, 30/30**; node 15/10/12; electron 20; WASM artifact rebuilt (**79** hashed inputs, was 77).
- **THE HONEST ANSWER IS "THE JC-120 CHORUS RE-VOICED", AND IT SHIPPED AS THAT.** The CE-1 *is* the Roland JC-120's chorus circuit in a floor box — Boss's first pedal, 1976, derived from the amp the year after. This project has modelled that circuit since M6.3 (§11.2), so **`Ce1Model` OWNS a `ChorusModel`**: the swept delay line, the 4-point Lagrange interpolator, the LFO accumulator and the ~8 ms smoothing are the amp's validated code, unmodified, driven through an additive **voicing seam** (`setBaseDelayMs`/`setSweepMs`/`setRateHz`/`setWaveform`). There are exactly **three** differences from the amp's voicing, each sourced, and nothing else was invented to make the slice look bigger. **The amp is BIT-IDENTICAL: 108/108 render hashes** (3 rates × 3 modes × 4 speeds × 3 depths, ragged blocks, FNV-1a over raw float bits) match the pre-slice source, re-verified after a later change to the same file.
- **THE RATE TAPER IS DERIVED, AND A LINEAR POT IS REFUTED BY A FACTOR OF FOURTEEN.** The teardown publishes chorus endpoints **1.0–3.0 Hz** *and* a midpoint of "about 1.75 Hz". Log gives `sqrt(1·3) = 1.7321` (**1.03 %** off); linear gives 2.0 (**14.29 %** off). The third number picks the law — nothing fitted. Vibrato is **3.2–11.6 Hz** on the same taper (its midpoint was never published — a **recorded gap**, carried over rather than fitted, because it is one LFO circuit with a switched range). **A cross-check landed:** the two ranges were sourced independently and do not overlap (3.0 < 3.2), *and* the non-overlap is stated separately in prose — three facts agreeing, and it is asserted so a later edit cannot break it quietly.
- **MONO IS THE FACTORY OUTPUT, NOT THE COMPROMISE — and it is the whole reason this pedal is not the amp's chorus.** The CE-1 has **both** a stereo pair (dry one jack, wet the other) **and a MONO jack that mixes wet and dry internally**. Mixing them ELECTRICALLY combs; the amp's chorus never combs, because `ChorusModel` in CHORUS mode is a **split** whose L channel is the **bit-exact dry input** (measured max|L−in| = **0.000e+00**). So a mono host taking the amp's chorus gets **0.00 dB** of envelope movement — *no chorus at all* — against the CE-1's **14.72 dB**. Comb null measured at **125 Hz** = 1/(2·4 ms) with a **24.20 dB** contrast against the 250 Hz peak, and it MOVES with DEPTH (+16.97 dB at 125 Hz, −7.78 dB at 100 Hz). **WHAT IS LOST: the stereo image**, stated rather than papered over — no mono model can have it.
- **The delay window is read literally off the source, and DEPTH 0 STILL MODULATES.** "3–5 ms at min, 3–7 ms at max" pins the delay FLOOR at 3 ms while the ceiling opens 5 → 7, so `base = 4 + depth` and `sweep = 1 + depth` ms — both exactly linear, no taper invented. Rendered pitch deviation **35.2 / 53.0 / 71.0 cents** at DEPTH 0/0.5/1 (analytic 34.8/52.2/69.6, within 2.1 %). **The DEPTH knob at zero still detunes by 35 cents** — that is the circuit and it is asserted, so a later slice cannot "fix" it to bottom out at unity. Vibrato at max rate + depth measures **272 cents** (2.7 semitones); reported, not tamed.
- **THREE THINGS WENT WRONG AND WERE FIXED RATHER THAN BOUNDED — two found by this slice's own tests.** (1) **Switching the LFO waveform mid-cycle STEPS the delay.** Sine and triangle agree only at their zero crossings; at phase π/4 they differ 0.7071 vs 0.5000, so a flag flip jumps the read pointer **0.207·A ≈ 14 samples** at 48 kHz. Now a smoothed blend with a `blend <= 0` fast path so every JC-120 caller still evaluates `std::sin` and nothing else. (2) **Forwarding the mode to `ChorusModel` CLICKED at 17.02×** the signal's own slew — its mode is a hard switch by its own documented convention, so L changes meaning from dry to wet in one sample while the dry mix weight is still mid-ramp at 0.5. **Fixed by never switching it:** the owned model stays in `MODE_CHORUS` for both of our modes and vibrato is that same dry/wet pair with dry taken to zero, which makes "vibrato is the wet path alone" structurally true rather than a second code path that has to agree. **17.02× → 0.95×. Do not "simplify" it back.** (3) **A PRE-EXISTING NATIVE BUG, found while extending `kFaces`, fixed and reported loudly:** the array is positional but had **Squash at index 6 and Weeper at index 7** while `PEDAL_WAH = 6` / `PEDAL_COMP = 7`, so **a wah card drew the compressor's face and a compressor card drew the wah's** — wrong wordmark, accent, knob labels AND param attachments. `pedalMenuLabel` uses explicit `case` labels and was right, which is exactly why menu and card disagreed. Indices 8/9 now hold explicit empty faces reserved for the delay and gate slices.
- **A numeric fact worth knowing before the next smoother test: `OnePoleSmoother` converges EXACTLY to a ZERO target and NOT to a nonzero one.** Toward zero the value halves until the residual clears the 1e-30 guard, which snaps it — so the vibrato dry weight really does reach **exactly 0.0**, and "the dry path is absent" can be an `==` rather than a tolerance. Toward a nonzero target the guard can never fire: the ramp stalls once the increment drops below half a float ULP at the target, i.e. at residual ≈ `ULP/coeff = 1.19e-7/0.0026 ≈ 4.6e-5`. Measured, the wet weight settles at **0.99998856** (1.14e-5 short, −98.8 dB) and sits there. Inaudible, but an `==` there would be a flake.
- **The one deliberate departure, recorded not smuggled (ADR 020).** The real CE-1's chorus mode has **ONE** knob (INTENSITY — ganged rate+depth, and the rate is the part you notice) while vibrato has two. Reproducing that gang would leave a slot doing **nothing** in chorus mode, which this codebase forbids in terms — so RATE and DEPTH are independent in both modes and MODE selects the rate RANGE and the WAVEFORM. **Cost:** the chorus mode's knob feel is not the real pedal's, and you can reach combinations one INTENSITY knob cannot. MODE itself is genuinely DISCRETE (a real footswitch between two circuits), so unlike §58's wah SENSE it is not smuggled into a continuous law; the `< 0.5` / `>= 0.5` threshold and both ends are asserted.
- **What is NOT modelled, named rather than guessed — the largest known gap on this voice.** The CE-1 is a **BBD** circuit (MN3002, 512 stages, behind a compander, anti-alias filter in and reconstruction filter out) and the sources call it "dark, full and throaty". **None of that is modelled**: the wet path is a clean Lagrange delay exactly as the amp's is, so this pedal **inherits that approximation rather than adding a new one**. No corner frequency, compander ratio or BBD bandwidth could be sourced, and inventing one to chase an adjective is the fitting this project forbids. **NO SCHEMATIC WAS EVER READ** — `WebFetch` 403'd on every host tried and there is no CE-1 schematic or model on GitHub, so the channel was search-result extracts only (§57's Orange precedent). Also gapped: the vibrato rate midpoint, and the chorus mode's own delay excursion (the published window is the vibrato DEPTH knob's).
- **Housekeeping, all measured:** DC on signal **0.026 %** of peak clean — with +0.1 V of input offset the offset **passes straight through**, because a linear delay has no coupling cap, so it is bounded and stated rather than hidden; `reset()` **0.000e+00** against a never-played model; one NaN in → **0/48000** after reset; zipper on a RATE slam **0.98× / 1.00×**; block-size invariance **0.000e+00** ragged vs 128; rate independence **0.00 %** over 44.1–96 kHz; ADR 006 resting state — the delay ring is a pure FIFO with **no recursion into it**, so it reaches **exactly 0.0** on silence and **this file adds no new denormal guard**, which is a measured conclusion not an omission. Latency **0** and no oversampling (linear time-varying — the phaser precedent, §12), both asserted.
- **In flight (2026-07-31): the Orange OR120 SCHEMATIC CORRECTION** (docs **§57**, amended in place — no new § number; `docs/work/2026-07-31-orange-schematic-correction.md`, branch `claude/orange-schematic-correction-6f557i`). The OR120 shipped in PR #41 as an explicit **documented reconstruction** — no schematic was reachable from the build container, so the topology came from forum prose and nearly every component value was invented under physics constraints, and §57.1 said in terms *"do not re-tune any of them toward a sound; find the schematic."* **The owner supplied it** (B. Hickmott's 2004 redraw of the 1972 factory sheets, plus the post-'74 factory sheet and the Field Guide). Ten defects corrected; §57.1 is now a **transcribed parts list** and the era choice (EARLY 1970s, the 1n5 treble cap) is recorded with its consequence. **ALL FIVE GOLDENS UNCHANGED at ±0.00 — nothing blessed, and a correction confined to one voice cannot need it.** Core ctest **28 → 29 entries, 29/29**; native 3/3; Playwright **76 passed**; node 15/10/12; electron 20; WASM artifact rebuilt (77 hashed inputs). Scope: `Orange*` + its test + docs. **No wiring changed** — voice 4, cab 2, param id 13, the web/native/assistant vocabulary are all untouched, so both front ends inherit the fix through the rebuilt artifact.
- **THE STRUCTURAL DEFECT WAS #1, and it is a RESPONSE difference, not a frequency-response one.** The model ran `V1A → VOLUME → V1B → F.A.C. → James stack`, i.e. the James network as a TERMINAL EQ. The real amp is **`V1A → James stack → GAIN → V1B → F.A.C. → power amp`**: the stack is driven by V1A's **220 k** plate (measured source impedance **45 534 Ω** — no cathode follower exists anywhere in this amp), its insertion loss is made up by V1B, and the **F.A.C. feeds the POWER AMP**. So BASS/TREBLE change *what gets distorted*, not what comes out of the distortion. The stack, the **GAIN pot** (its load, so its position changes the network's own response) and the **330 p → V1B grid** are ONE 9-node MNA, because the 330 p sees the wiper impedance and its corner therefore MOVES with the knob (≈436 Hz at noon). **That 330 p did not exist in the reconstruction at all and is the single largest audible consequence: composed low E sits −20.26 dB re 660 Hz against the JCM's −2.91.** Nothing was done about it — the BASS control has **11.96 dB** of authority at 82 Hz to answer it.
- **#7 split the joint Newton, and #8 put the repo's first inductor in an amp.** The early-70s driver→cathodyne is **AC-coupled (68 n + 1 M)**, so the reconstruction's single 3×3 in (Vpd, Vkd, Vkc) became a **2×2** driver (with the transcribed 1 n across its 100 k plate load, and the coupling folded in as one conductance + a history current) plus a **1-D** cathodyne. The split loads stay EQUAL — **leg balance 0.999965**, still topological. **The H.F. Boost is a 1 k lin pot + a 2 mH CHOKE + 0.47 µF at the driver's cathode** — a series R-L-C resonant at **5191.1 Hz**, Q **8.15** at full boost — where the reconstruction had a one-pole shelf in the feedback path. Tilt across the control **+6.32 dB** (was +6.80): the headline number barely moved, the mechanism/shape/place all did. NFB is now **15 k from the 16 Ω tap** (×√2 vs this model's 8 Ω secondary), depth **7.07 dB**.
- **`kInterstageScale` is an UN-FITTING to 1.0, and the sweep REFUTED the first release's own finding.** The corrected preamp ends at the F.A.C. network, which already terminates at the driver's grid node, so its output IS the grid voltage and there is nothing for a trim to represent. §57.9 used to record that "kInterstageScale does NOT set the breakup onset" (it sat at 0.59 across a 5× sweep because the PREAMP clipped first); the real preamp loses ~35 dB before V1B, so the POWER section clips first and the onset now moves **1.00 → 0.35** across the sweep — landing at **VOLUME 0.50** at unity, i.e. the §46 no-master window is met BY THE CIRCUIT. `kFullScaleSecV` 50.7 → **43.086** off the measured 38.777 V cranked secondary (peak 0.9000, 0.42 % rate spread).
- **AND THE AMP NO LONGER MAKES ITS RATED 120 W — reported, not tuned away.** The power section's own ceiling measures **92.71 W with feedback / 93.56 W without**, so §42's "smallest scale that reaches rated power" criterion is **unsatisfiable** and was not used. Attribution measured, not guessed: the transcribed EL34 grid network (**68 n into 110 k**, against 22 n into 220 k) couples the legs ~3× more stiffly and drives the grids harder into conduction, so §18's blocking shifts the bias further toward cutoff; the transcribed **per-tube 1 k screens with no bypass cap** are worth ~4 W on their own (at 1 Ω the ceiling reads 97.7 W). **Do not close it by re-inventing a screen filter cap or softening the grid coupling.**
- **THE §57.4 BAR SURVIVED, and both contrast bounds are UNCHANGED.** Network: James **+0.75 dB** (a BUMP, was +2.32) vs FMV **−6.03** → contrast **6.78 dB** against a **6.0** bar — 0.78 dB of margin where there was 2.35, i.e. a HARDER test now. Composed: Orange **+2.67** vs JCM **−5.09** → **7.76 dB** against 4.0 (it IMPROVED, because the 330 p and the F.A.C. take the bottom out of the Orange exactly where the FMV boosts it). The one-sided `orangeNotch > +1.0` sub-bound **would now fail at +0.75** and was moved to `> 0.0` — §57.4 said in terms those margins were "recorded rather than snugged" and that a component-value correction was allowed to move them; the teeth are in the contrast, which did not move.
- **Three other first-release claims REFUTED and reported.** (1) The cathodyne clip is **NOT "asymmetric by construction"** — AC-coupled and centred in its own compliance it is symmetric to **0.25 %** (rails 0.0000 / 208.202 V), where the DC-coupled version measured −132.7/+67.3. (2) The **F.A.C. range is not "330 p to .047"** — it is `[through] · 4n7 · 4n7 · 2n2 · 1n · 330p`, a **10× error at the fat end**, so the low-E span is 17.21 → **13.69 dB** and the top three clicks sit 0.45 dB apart. **Positions 2 and 3 carry the SAME 4n7 on both factory sheets** and ship literally, with `kCaps[1] == kCaps[2]` asserted so nobody "tidies" it. (3) A **ground return for the cathodyne's 1 M grid leak is non-viable** — measured **Vk 5.47 V / Ip 54.7 µA**, against the 48 V the transcribed EL34 bias needs.
- **The one value the sheet does not carry, derived rather than fitted.** The cathodyne's grid-leak RETURN node. The only arrangement keeping the transcribed equal 100 k/100 k split loads is a tap in the cathode leg, placed so the stage idles at the CENTRE of its own compliance (`Vkc = C+/4`) — the cathodyne's published design rule — and then CHECKED against an absolute number from the same schematic: the swing must clear the EL34s' 48 V. It measures **104.23 V**; the derived tap is **1432 Ω** of the 100 k leg. Two other named gaps: the 2 mH choke's DCR (8 Ω) and the HT choke's DCR/inductance (ideal at DC, which merges A+ and B+ into one 66 µF node and removes the screen time constant the reconstruction invented).
- **ONE property was LOST and it is an XFAIL, not a loosened bound.** `orange-schematic-alias-44k1`: the composed cranked alias floor is **−73.0 dB at 48 kHz / 4×** (it IMPROVED 5.9 dB) but **−50.8 dB at 44.1 kHz**, against the −56 bar the first release shipped. Attribution measured: **−52.0 / −50.8 / −50.7 dB at H.F. BOOST 0 / 0.5 / 1.0**, so the new resonant cathode network is NOT the cause, and it improves 11 dB at 8×, so it is genuine foldover not §54's rail-clipping signature. `clipper_orange_tests` gains its **first ledger** (ctest 28 → 29, repo ledgers 4 → 5); the fix is one shared OS domain around the whole cascade, never a lower bar.
- **Two traps and one process finding from that slice.** (1) **A DC-step probe stopped being a valid way to read the cathodyne's leg gains** the moment the pair became AC-coupled: identical code measured a split-load gain of **0.95 at a 3 ms settle and 0.76 at 23 ms**, because the step decays through the 68 n/1 M (τ ≈ 68 ms). The probe is now a steady 440 Hz tone with signed in-phase amplitudes. (2) **The D+ dropper fixed point has a loop gain of about −7**, so the obvious `ip += k·(f−ip)` oscillates for any k above ~0.25 and silently returned a rail 9 V off; both the stage current and the rail are **bisected** on a monotone residual instead. (3) **The perturbation run found THREE bars that could not fail** — the two supply checks were Ohm's-law *identities* (the code computes the rail that way) and nothing measured whether the tone pots did anything, so `kRB` 1 M → 10 k, `kRdropCplus` → 0 and `kRscreen` → 250 all left the suite GREEN. Replaced by absolute windows off the transcribed resistors plus a knob-authority block; all three now go red. **12/12 perturbations RED, restore GREEN.**
- **The perturbation that matters most is also a result: P12.** Swapping ONLY the era-defining treble cap for the **post-'74 330 pF** takes the James network from a mid BUMP to **−2.31 dB (a SCOOP)** and the contrast from 6.78 to **3.72 dB** — failing the bar. The early **1n5** cap is not a detail; on this metric it is what makes an early OR120 mid-forward, and a post-'74 voice (the cross-check sheet is a complete second amp) could not inherit this bar.
- **Denormal scope for the new inductor branch, decided by measurement (ADR 006), and it is the §59 case not the §56.4b one.** The 0.47 µF rests at the **driver's cathode (1.9453 V)** — a state that looks zero-resting and is not. The branch current and choke voltage do rest at zero mathematically but are computed as `(Vkd − Eh)`, a cancellation of two ~2 V quantities, so they **floor at 1.386e-16** and can never reach subnormal: 292 decades of head-room, so a flush there is unreachable code in the hottest loop. Comment, not guard, with the floor asserted. The James network's **ten** companions and the F.A.C.'s two DO rest at exactly 0.0 and are guarded.
- **In flight (2026-07-31): the "Squash" OTA compressor — M13.1, the first DYNAMICS pedal** (docs **§59**, `docs/work/2026-07-31-dyna-comp.md`, branch `claude/dyna-comp-6f557i`, **ADR 019** — the compressor engine is config-parameterized from the first line, with the optical voice M13.3 and the noise gate M13.6 as its named consumers). An MXR Dyna Comp / Ross-style **CA3080 OTA compressor** as pedal type `comp`, two knobs (SUSTAIN + LEVEL), wired end to end on **both** fronts in one slice. Shared `CompressorEngine` + `CompressorConfig`; `CompModel` is the Dyna Comp voice as a config — the §21 `OverdriveEngine`/`SdModel`/`TsModel` shape, and the M13.3 seam is written out in full in the engine header so the next slice does not have to guess where it is. **Goldens: all five UNCHANGED at ±0.00, nothing blessed** (the compressor is in no golden rig and touches no other model). Core ctest **26/26**, Playwright **72/72**, node suites 15/10/12/20, WASM artifact rebuilt (69 hashed inputs).
- **The research channel was one GitHub-hosted LTspice file, and the netlist was reconstructed from it node by node.** The proxy here permits **github.com only** — the DAFx-11 Dyna Comp paper (`recherche.ircam.fr`, `dafx.de`), ElectroSmash, the CA3080 datasheet and coda-effects **all 403 at CONNECT**, and `WebFetch` failed on every host including Wikipedia. What was reachable: `Cushychicken/ltspice-guitar-pedals` (`mxr-dyna-comp.asc`, traced from R. G. Keen's Geofex archive), parsed out of its `SYMBOL`/`WIRE`/`FLAG` records. Component values were cross-checked against ElectroSmash's list through search extracts (different designators, same values). **Recorded gaps: no published static compression curve or ratio for a real unit exists in reach, so every curve in §59.4 is this model's own; and the SUSTAIN pot's taper letter could not be sourced** (shipped as the literal linear 500 kΩ rheostat, with the top-heavy knob law measured and reported rather than fitted).
- **The strongest check available was an INDEPENDENT simulation, and it landed.** Chesney's SPICE run reports the bias current starting "just under 200 µA" and settling at "about 16 µA" on a 100 mV 1 kHz sine with the sensitivity pot at 10 kΩ. This model, at the SUSTAIN position giving the same 37 kΩ, measures **199.85 µA idle and 16.54 µA settled** — 4 % and 3 %, on a number produced by the gain cell, the load, the splitter, the rectifier, the envelope integrator and the control map all in series. That check is also what **caught the one real bug this slice made**: the first implementation put the **collector** current in the clamp node's KCL (only base current flows into that node), which starved the discharge ~β-fold and gave a **500–650 ms attack** and a 9:1 ratio. The *idle* current still agreed to 4 % — the error was only visible under signal, and only against an outside number.
- **The three things the brief said must be modelled, not approximated, and what each measures.** (1) **The CA3080 gain cell** is `Iout = Iabc·tanh(Vd/2Vt)` with a real `Iabc` computed from real node voltages, not a `gain = f(env)` curve. (2) **The detector time constants ARE the pedal**: release is the components' own `R18·C9 = 1.500 s` and measures **1.496 s**; attack is a current-starved peak detector, not an RC, and measures **14 / 10 / 5 / 3 ms** at SUSTAIN 0.30/0.50/0.80/1.00 against a published ~5 ms. (3) **It is FEED-BACK** — the detector taps the gain cell's output — settled from the netlist and then *proved by measurement*: at SUSTAIN 1.0 over 26 dB of input, feed-back gives **216:1** and the identical code tapping the input gives **3.3:1**.
- **"SUSTAIN is not a threshold" is now a measured pair of numbers, not a claim.** Across the whole knob the small-signal gain moves **25.33 dB** (+11.25 → +36.58) while the settled output above the knee moves **0.28 dB** (0.383 → 0.400 V). The threshold is a base-emitter drop and no knob is in front of it. Ratio over the top 18 dB of input **106:1**; slope below the knee **0.9992**. Sustain extension, level-matched against a bypassed reference: **+0.36 s at SUSTAIN 0, +2.26 s (2.19×) at 1.00**.
- **The honest expectations were measured and REPORTED, and are asserted in the direction that keeps them.** Attack-transient loss **7.72 / 10.00 / 15.18 / 20.00 dB**; idle gain on a −84.75 dBFS floor **+8.22 / +13.76 / +33.56 dB** (turning SUSTAIN up costs **25.34 dB** of noise gain — the famous hiss, as a number, since this project synthesises no noise anywhere); low E **−2.42 dB** and 20 Hz **−15.21 dB** re 1 kHz (three high-passes, smallest coupling cap 10 nF); 10 kHz **−2.88 dB**; and the pedal **inverts phase**, because the signal drives the OTA's inverting input. None of that was designed out — a better-behaved compressor would be a different pedal — and each bar fails if a future slice quietly improves it.
- **A finding that is in no prose source, and two house rules it exercised.** The CA3080 sees only **~12 % of the signal** as a differential voltage: the 2 kΩ offset trimmer bridges the two inputs while the non-inverting one is AC-terminated in 15 kΩ, and *that* is the input attenuator. It is second order, and it RISES with frequency while the 150 kΩ ∥ 1 nF load pole (1210 Hz) FALLS — they cancel to **1.74 dB across 110 Hz…5 kHz**, which is why a Dyna Comp is "not very bright" without sounding filtered, and why neither network may be simplified away without the other. Its slow pole is **9.29 Hz** — radius 0.99970 at 192 kHz, exactly §56's audible-hiss shape — so it ships as a **cascade of two first-order sections in `double`**, the cure §56.4b named, applied at the first opportunity rather than after the fact.
- **Denormal scope (ADR 006) was applied by measurement here, and one case is a new kind.** Guarded: the six linear filter states, which rest at exactly zero (`maxAbsRestingState()` covers exactly those and asserts 0.0). NOT guarded, envelope node: rests at **8.9684 V / 8.6353 V** — a real operating point. NOT guarded, the two clamp caps and their Newton warm starts: their *physical* rest is **−4.90e-272 V**, which IS below the flush floor, but **the solver cannot resolve it** — Newton exits at its residual tolerance, so the node floors at `tol × (Rsrc + T/C)` ≈ **1.9e-11 V**, measured flat after a 40 s silent tail. 297 decades above subnormal, so a guard there is unreachable code in the hottest loop in the file. Comment, not flush.
- **Two traps worth knowing before you write the next dynamics test.** (1) **A two-tone alias stimulus measures nothing on a compressor.** The first attempt reported **−25 dB flat across 1×…8×**, which looks like a bad alias floor and is not aliasing at all — the gain is modulated by the detector, so two tones fill the spectrum with legitimate IMD sidebands. The tell is §54's: a real alias floor MOVES with the factor. Single tone: **1× −7.61 → 4× −62.37 → 8× −97.73 dB**. (2) **The web spec had to be cut from eight OfflineAudioContexts to four.** Chromium's documented silent-render flake (playwright.config.ts's own note) fired at six and returned a −563 dB "compression ratio"; the input step is now taken as exact by construction (an oscillator through a GainNode) and a HARNESS GATE fails first and by name if any render comes back silent. **Also: port 4173 is contended between agent worktrees** — a parallel agent held it, and the real config deliberately refuses to reuse a foreign server, so the run was done on a temporary throwaway config on another port (deleted; do NOT `pkill` a sibling's preview server, which this session did once by accident).
- **CPU 12.1 % of a 48 kHz stream on signal and 6.1 % on silence** — silence is CHEAPER than signal, which is the §33/§34 check that the Newton early-out and the denormal policy are both working. Latency 72 samples at 4×, block-size invariance **exactly 0.0**, rate spread **0.014 dB** over 44.1–96 kHz, DC-on-signal 0.0000 % of peak, `reset()` re-parks exactly on a fresh model and takes one NaN from 245/256 poisoned samples to **0/48000**. Five perturbations all red, restore green (§59.9) — including P4, which **stayed green at first** and was answered by adding a bar against an ABSOLUTE reference (the published ~5 ms attack, 3–9 ms window) rather than by narrowing a bar around the perturbation.
- **Named follow-ups this slice leaves:** **M13.3** the optical/LA-2A voice (a `GainCellKind` case + a config — the seam is documented in `CompressorEngine.h`); **M13.6** the noise gate (the same `Sidechain`, a different `ControlMap` — do not re-derive a second envelope follower); and unifying the envelope followers with the parallel wah slice's — **SETTLED 2026-08-01, docs §58.8 / ADR 023: they are different circuits, measured, and the wah keeps its own.**
- **Current phase:** Post-v1.1. **EIGHT amp voices** as of 2026-08-25, the eighth being **M10.10's Fender tweed Champ 5F1** (docs §72) — the first SINGLE-ENDED output stage here, the first Fender that distorts, and the first voice with NO TONE STACK AT ALL. It brings a fourth cab, the synthesised `tweed8` 1×8. The older count reads:
- **Current phase:** Post-v1.1. The gear lineup is THIRTEEN audio pedals — the eight below plus M13.6a's Curfew gate, M13.7's Ensemble chorus, M13.3's Lumen optical compressor, M13.5's Swirl Uni-Vibe (docs §67) and **M13.10's Cellar drop-tune, the first PITCH effect** (docs §70) — and **SEVEN** amp voices, the seventh being **M10.4's Mesa/Boogie Dual Rectifier** (docs §69). The older count reads: EIGHT audio pedals (RAT, SD-1, TS, Muff, Phaser, Gold, M13.1's Squash compressor — the first DYNAMICS processor — and M13.4's Echoman BBD analog delay, the first DELAY) plus **SIX** amp voices (Clean 120, JCM800 2204, blackface Twin, AC30 top boost, Orange OR120 Overdrive, **Orange Rockerverb 100 — the first amp here with an independent GAIN and MASTER**), **three** cabs + user IR upload, tuner, and the conversational assistant. Web and native both ship a dynamic, reorderable pedal board.
- **Current phase:** Post-v1.1. The gear lineup is six audio pedals (RAT, SD-1, TS, Muff, Phaser, Gold) plus **five** amp voices (Clean 120, JCM800 2204, blackface Twin, AC30 top boost, **Orange OR120 Overdrive**), **three** cabs + user IR upload, tuner, and the conversational assistant. Web and native both ship a dynamic, reorderable pedal board.
- **In flight (2026-07-31): the Orange OR120 "Overdrive" — the fifth amp voice** (docs **§57**, `docs/work/2026-07-31-orange-or120.md`, branch `claude/orange-or120-6f557i`) — ROADMAP M10.3, and the direct **counterweight to the JCM800**. Amp voice **4** (`orange`) plus a synthesised **`orange412`** cab, wired end to end in one slice (core, C ABI, worklet, web UI + face + tokens, native engine + editor + cab menu, assistant prompt). It is EL34 push-pull like the 2204 and **reuses that power machinery wholesale** (Koren EL34 fit, per-tube plate Newton, grid blocking, OT, rail/screen sag — no new device model). **The acceptance bar was "measurably NOT a re-skinned JCM800", and it is a hard assert in dB:** at noon the mid-notch metric (min 300–800 Hz re the mean of 100 Hz and 4 kHz) measures **Orange James +2.32 dB (a BUMP) vs Marshall FMV −6.03 dB (a SCOOP) — 8.35 dB of network contrast**, and **+1.15 vs −5.09 = 6.24 dB on the COMPOSED amps**. Bars shipped 6.0 / 4.0, margins recorded not snugged. **All five goldens UNCHANGED at ±0.00 — nothing blessed, and a new voice cannot need it.** Core ctest **25 → 26**; ledgers unchanged at 4 (this suite registers none — zero known-bad properties).
- **What makes it an Orange is CIRCUIT, not EQ — four structural differences, each measured.** (1) A **CATHODYNE (split-load) phase inverter, DC-coupled to its driver** — sourced as the players' own explanation ("it distorts differently than the long-tail pair … stiffer, punchier, fuzzier"). The driver plate node IS the cathodyne grid node, so the pair is one **3×3 Newton** in (Vpd, Vkd, Vkc). Its legs are anti-phase and equal **BY TOPOLOGY: balance 1.000000**, against the JCM LTP's 0.988 that took audit finding 8 plus a resistor sweep (§45); it clips on **compliance** (Vk pinned 0…B+c/2, measured −132.7/+67.3 V — asymmetric by construction), and the split load's own gain is **0.9763** so the driver carries a 300 k plate load. (2) A **JAMES / passive-Baxandall** BASS+TREBLE stack (no mid) — **two PARALLEL shelving branches** that leave the midrange standing where an FMV notches it. That is the mid-forward mechanism, and it is why the bar is topological rather than fitted. (3) The **F.A.C.** six-position rotary of series coupling caps: **17.21 dB** of low E across the switch, monotone in both level and tilt. (4) **Global NFB into the DRIVER'S CATHODE** (so the loop encloses the driver *and* the inverter, unlike the 2204's), depth **7.81 dB**, with **HF DRIVE** as its presence — **+6.80 dB** of 5 kHz-vs-220 Hz tilt across the control. Plus **NO MASTER VOLUME** (breakup tracks the knob, §46 convention: ≥5 % THD onset at **VOLUME 0.50**, 0.76 % clean end → 47.29 % cranked, THD and RMS monotone) and **no bright cap** on the volume pot (the 2204's 470 pF one, §47, is a real part of why a JCM is tighter).
- **THE RESEARCH HONESTY ITEM, and it is the biggest caveat on this voice: NO SCHEMATIC WAS EVER READ.** There was no OR120 material in the repo, and **no host outside the search tool was reachable from the container** — `WebFetch` returned **403 for every URL tried, including `example.com`**, and `curl` reported "Host not in allowlist" for Prowess Amplifiers, the Orange Field Guide, SchematicHeaven, the Orange forum and Wikipedia alike. So the channel was **search-result summaries only**. §57.1 tabulates exactly what is sourced (the topology; the quad EL34 + **solid-state bridge** rectifier; the volume between V1's two stages; the DC-coupled cathodyne; the James stack with no mid; the six-position F.A.C. spanning "330 p to .047"; NFB to the pre-PI cathode with presence in the loop; ampbooks' R1 = R3 = 100 k) and **one component value: the early '72/'74 treble cap at 1500 pF** against 330 pF post-'74 — which is precisely why owners say the early treble knob "brings high mids up with it". **Everything else is a documented reconstruction**, chosen against physical constraints (every triode 0.5–0.9 mA, EL34s at **16.64 W = 67 %** of their 25 W rating, the amp reaching its **rated 120 W**) and against the James network's *own published defining property* (flat at noon) — never against a tone. **Do not re-tune any of them toward a sound; find the schematic.**
- **Three of that slice's own premises were refuted by measurement and reported, not fitted away.** (1) **`kInterstageScale` does NOT set the breakup onset** — swept 0.010 → 0.050 the onset sat at **VOLUME 0.59 throughout**, because down there the **PREAMP** clips first (two fully-bypassed 12AX7s with the volume pot between them). Picking it "so breakup lands at 0.5–0.6" would have been picking it for a reason that is not true, so it is derived by the §42 criterion instead — the **smallest value at which a cranked OR120 makes its rated 120 W**, which is **0.12** (130.2 W; 0.08 tops out at 99 W). `kFullScaleSecV = 50.7` follows from that measured 45.65 V cranked secondary (peak lands at 0.9004). (2) **More input can mean LESS output** — the cranked-power column is non-monotonic at scales 0.20/0.40 because grid conduction charges the EL34 coupling caps and shifts the bias toward cutoff (§18's blocking mechanism), which is *why* the criterion is "reaches rated power", not "peaks highest". (3) **The cab claim "more 60 Hz than the Brit" was FALSE and was replaced, not massaged** — the bark IS the normalization peak, so the Orange sits lower everywhere else (−9.88 vs −7.69 dB absolute at 60 Hz). The true, box-only property shipped instead: its **−6 dB low corner reaches 63.4 Hz vs the Brit's 72.6**, alongside **+4.58 dB** more 1.2 kHz-relative-to-200 Hz bark.
- **Two things to know before you touch this voice.** (1) **The native `CabChoice` ids and the C ABI's built-in cab ids DIVERGE at 2, deliberately.** Native already had `CAB_CUSTOM = 2` and that value is stored in the APVTS `cabModel` choice parameter and in saved sessions, so inserting the Orange cab at 2 to match the ABI would silently turn every session saying "Custom IR" into "Orange 4×12". Native appends **`CAB_ORANGE412 = 3`** and the engine maps the two spaces **in code** (`loadCurrentCabIntoPair`), never by index arithmetic; the popup-menu ids are a third space again (the Orange is menu id 5). (2) **The F.A.C. is a NEW param id (`13`), not a reused knob slot** — no other voice has a six-position switch, and reusing a slot would make a stale rig state silently mean something else. Everything else is the house reuse: VOLUME(0)/BASS(1)/TREBLE(3)/REVERB(9), and PRESENCE(11) → **HF DRIVE** (the slot the AC30 takes as TOP CUT). Adding `fac` to `AmpParams` changes the rig JSON shape, so the three literal round-trip Playwright fixtures gained the field.
- **Perturbation-proven, 10 patches, all RED, suite GREEN restored** — including **P10 (bass pot 1 M → 10 k), which trips THE BAR itself** (`orangeNotch > +1.0` fails at **+0.58**, contrast 8.35 → 6.61). Also red: the 1500 pF treble cap → 47 nF, C1 → 0, unequal split loads, a flattened F.A.C. ladder, a flipped NFB sign, HF DRIVE disconnected, and the cab's bark removed. Alias floor on the house composed probe (4186 Hz / 0.3 V, cranked) **4× = −67.1 dB at 48 k / −59.2 at 44.1 k** against 1× −10.2 (bar −56, plus "4× must beat 1× by ≥ 12 dB"); DC on signal **0.126 %** of peak with and without +0.1 V of input offset; reset + 128-frame ragged blocking differ by **0.000e+00**; the James stack's eight cap companions rest at **exactly 0.0** after 4 s of silence. Native `identical_core_test`, `chain_edit` and `cab_state` all green; full Standalone+VST3 build clean (the editor is not compiled by CI — worth knowing).
- **Web-suite caveat from that slice, environmental not code:** the Playwright run is unstable in this container — the failing set is DIFFERENT on every run, never includes the three new Orange tests, and matches `playwright.config.ts`'s own documented Chromium `OfflineAudioContext` flake (two consecutive renders returned byte-identical buffers; `page.goto` also hit `ERR_CONNECTION_REFUSED` because a PARALLEL agent's worktree squats the hard-coded `--strictPort 4173`). Every failing test passes in isolation on retry. **A consequence for the web tests: the mid-forward BAR was tried in Playwright and deliberately NOT kept** — the worklet form needs six `OfflineAudioContext`s and two consecutive attempts measured +5.92 dB then −9.50 dB from identical code. The bar lives in the core suite where it is measured over 16 renders; the web spec asserts the DELIVERY PATH instead (voice 4 reachable, param id 13 reaching the core — F.A.C. 1→6 drops broadband RMS **17.49 dB**, against the core's 17.21 dB at the tone bin).
- **Current phase:** Post-v1.1. The gear lineup is six audio pedals (RAT, SD-1, TS, Muff, Phaser, Gold) — SEVEN with the in-flight wah, the first FILTER pedal — plus four amp voices (Clean 120, JCM800 2204, blackface Twin, AC30 top boost), two cabs + user IR upload, tuner, and the conversational assistant. Web and native both ship a dynamic, reorderable pedal board.
- **In flight (2026-07-31): the wah / envelope filter — the lineup's FIRST FILTER pedal** (docs **§58**, **ADR 018** — `docs/decisions/018-wah-filter-and-gain-stage-split.md`, `docs/work/2026-07-31-wah-envelope-filter.md`, branch `claude/wah-envelope-6f557i`). Pedal type `wah`, wordmark **"Weeper"**, `FILTER Nº7 · TREADLE`: a GCB-95-class Cry Baby whose POSITION is an ordinary automatable parameter PLUS an envelope follower driving the SAME tank, so one resonant primitive covers treadle-wah and Mu-Tron territory. Slots **POSITION / SENSE / VOICE**. Wired end to end in one slice — core, C ABI (`wah_*`), worklet, web `rocker` face + TEAL accent, native engine/APVTS/PedalCard, assistant `add_pedal` + coaching. **ALL FIVE GOLDENS UNCHANGED at ±0.00**, nothing blessed. Core ctest **25 → 26 entries, 26/26**; native 3/3; web 69 passed; node 15/10/12; electron 20. CPU **14.25× realtime / 7.0 % of one stream**, latency 72 samples.
- **The sweep law is DERIVED and the one fitted number came out being a pot taper.** `f_LC = 1/(2π√(LC))` from the published `L = 500 mH` / `C = 0.01 µF` is **2250.79 Hz**, and the toe of the sweep IS that bare LC resonance — **1.57 % from the CCRMA/Julius-Smith MEASURED toe of 2216.06 Hz** (Faust `vaeffects.lib` `crybaby`, fitted to three measured GCB-95 responses; cloned from GitHub because **every** primary reference 403'd — electrosmash, geofex, dafx.de, ccrma, wikipedia, github.io). Pinning the published 450 Hz heel fixes the span (`A = 24.0176`), leaving exactly ONE free parameter — the pot taper — whose fitted `kTaperBeta = 23.537247` puts the wiper at **17.09 % at half rotation, inside the documented audio-taper spec (10–20 %)**. Rendered vs the independent measurement: **worst 1.48 %, rms 0.59 %** over **2.322 octaves (450.0 → 2250.8 Hz)**. **The SHAPE is the bar that matters and it is asserted:** halves **1.147 / 1.176** octaves against a linear-taper counterfactual's **0.472 / 1.851** — "all the wah in the last inch" is the classic modelled-wah failure and the test computes the counterfactual rather than quoting it.
- **Q was decided BY the measurement, and it is counter-intuitive.** Fixed damping + fixed L + variable C is a parallel RLC → `BW ∝ f0²` (exponent exactly **2.000** vs the reference's measured **1.870**), so **the resonance is SHARPEST at the heel and BROADEST at the toe**. The series-LCR alternative predicts constant absolute bandwidth and is **refuted**. `Rp = 12558.32 Ω`; Q rendered vs derived worst **2.6 %**, vs the independent measurement worst **9.8 %** (the −1.000/−0.870 exponent gap is **REPORTED, not fitted away**). **VOICE is ElectroSmash's documented "vocal mod" as a knob** (R7 log-centred on the stock 33 k): Q **2.253 → 5.292** with the centre pinned at **996.40 Hz** and the height at **17.91 dB on all three** — width only. **Peak boost 17.90–17.91 dB against the published 18.0, spread 0.010 dB across the whole travel** (the topology's constant-peak prediction, measured). Staging carries **no fitted constant**: the transistor stage's own gain is MEASURED in `prepare()` and `kTankDivider = 10^(18/20)/G0`, so `G0 × divider = 7.9433` exactly.
- **AUTO is asserted as a player-observable property, not a tautology.** A real plucked D sweeps the peak **0.381 / 0.762 / 1.146 / 1.534 octaves** at SENSE 0.25/0.5/0.75/1.0, peaks **82.7 ms** after the pick and returns within 10 % in **~840 ms**. **SENSE = 0 measures EXACTLY 0.000 octaves and the whole render is bit-identical (0.000e+00)** to a model that never had the feature — there is deliberately NO discrete mode hidden in a float slot. Honest note: 82.7 ms to peak against a 10 ms follower constant is the rectifier's own behaviour on a 147 Hz note (|x| crosses zero twice a cycle), not the coefficient.
- **Three things that slice found by measuring, one of them refuting its own hypothesis.** (1) A **9.6 dB peak-height tilt** (12.18 dB heel → 21.74 toe) traced to `BjtStage::Config::Cin` at 10 nF putting a **~1.1 kHz coupling corner in the MIDDLE of the sweep** — the real pedal's tank cap already DC-blocks the base, so `Cin` is 1 µF (corner ~11 Hz) and the spread is now **0.010 dB**. (2) **The 2-pole POSITION smoother was added on a hypothesis the measurement then REFUTED** — re-aiming POSITION per-block, per-8-samples and PER-SAMPLE all floor identically (−68.8/−75.0/−79.0/−81.3 dB across 3–20 kHz), so the fast-sweep skirt is the discrete resonator's own time-varying residual, not the control staircase; it decays with frequency and drops 8.7–14.4 dB at 96 kHz. The second pole was KEPT for a different measured reason (the pathological per-block slam improves 23.7 dB, −80.6 → −104.3). (3) **The perturbation run found two bars that could not fail** — a peak-height assert written against `WahModel::peakBoostDb()` was an identity (moving it to 24 dB left the suite green), and nothing pinned the shipped 4× default (`testAliasing` sets the factor per row). Both fixed; **6/6 perturbations now red**, plus the web spec (removing the worklet's `wah` dispatch takes the heel band 5.4e-04 → 9.0e-05).
- **Two process warnings from that slice.** **`git checkout -- <file>` destroyed uncommitted work in this worktree** — it restores from the INDEX, so a one-file "undo" after a perturbation silently reverted the worklet's whole wah wiring; use scratch file copies for A/B, never `git checkout --` (and never `git stash`, per §55). And the artifact staleness guard **fired correctly and fast** when a perturbation edited only the served worklet copy — it is not decoration.
- **Open on the wah, named rather than fitted:** the full GCB-95 netlist was NOT obtainable, so `kTaperBeta` is pinned to published frequencies rather than derived from the divider · the Q exponent gap (−1.000 vs −0.870, ±11 % at the ends) · **the tank is modelled OUTSIDE the transistor's feedback loop** (the ADR's named cost — the resonance does not damp or detune under a slammed stage) · inductor CORE SATURATION is not modelled (the measured Fasel 565.2 mH / halo 580.7 mH spread is only 0.10 octaves, so the audible difference players report is mostly core behaviour) · two envelope followers now exist in the repo (this one and the parallel compressor slice's) and **unifying them was REFUSED on measurement, 2026-08-01 — docs §58.8, ADR 023** · `kMaxChain` is one-instance-per-type natively, and a wah before AND after the dirt is the first genuinely normal request for two of a kind.
- **Last shipped:** the scrolling native pedal board on a milled rail · the GOLD "Myth" overdrive (sixth pedal type, parallel clean/dirt blend + germanium WDF) · the AC30 gain-structure fix (starved phase inverter) · M11 Player Expectations Suite.
- **In flight (2026-07-31): the AC30 dynamic sag** (docs §55, `docs/work/2026-07-31-ac30-sag.md`, branch `claude/ac30-sag-6f557i`, base `7e2a10d`) — **audit finding 4**, and the finding was worse than its own phrasing. The "GZ34 sag" multiplied the OT **secondary** by `1/(1+k·(Idemand−Iidle))` off a `|ipUp−ipDown|` envelope, and since `vSec ∝ (ipUp−ipDown)` that is algebraically **`y = x/(1+k|x|)` — a memoryless soft clipper placed after the transformer**, where no supply can reach. Measured on the pre-fix binary: **5.02 dB of output for a 6.02 dB input step at DEAD-CLEAN level**, **0.82 dB of output for a 34 dB drive increase** (a brick wall with no touch sensitivity left), and 2·f₀ envelope ripple that put **1.77× more THD at 82 Hz than at 440 Hz**. Retired **outright**, replaced by a Thévenin HT source behind a CONSTANT `kRsupply` = GZ34 75.6 Ω (Philips 17 V @ 225 mA) + the PT HT winding's conducting half 59 Ω, charging a 32 µF reservoir, plus the real shared cathode (`kCkCathode` **50 → 250 µF**, the JMI value, τ 2.5 → **12.5 ms**) and `kCoupRg` **1 MΩ → 220 k** (the AC30's actual grid leak; both siblings already carry it). After: small-signal step **5.98 dB**, dynamic range **2.37 dB**, low-E pluck band RMS **+2.2…+2.8 dB**, THD-at-82-Hz ratio **1.52/1.36/1.33×**. `kFullScaleSecV` 12.2 → **18.733** re-derived on the §42.6 probe (cranked secondary 16.858 V at 48 k, rate-independent to 0.03 %, peak back at 0.8999).
- **Three claims that slice had to refute — two of them its own first draft's — and none of them fitted away.** (1) **The choke is not in the plate path.** The draft used the AC30's published 7 H/260 mA/**78 Ω** choke DCR as the HT series resistance; in a stock AC30 the OT centre tap is fed from the reservoir **BEFORE** the choke (the OT red lead lands on the same cap as GZ34 pin 8), and that 7 H unit is the *Brian May* mod part. Corrected to `kRptSecondary` = 59 Ω. (2) **The bias is NOT "the dominant compressor moving a couple of volts"** — swept to 64 V at the PI grid, Vk moves 9.518 → **10.347 V (+8.7 %)** against a real AC30's **10.0 → 12.5 V (+25 %)**. Attributed by measurement, not guess: the EL84 idles at **34.92 mA plate + 12.67 mA screen** against a real ~45 + ~5, so a quarter of the standing draw sits in finding 9's ~3× hot screen term whose curvature does not grow with drive. **New XFAIL `finding4-ac30-bias-swing-short`**, owned by findings 9/10 — **do not chase it with a gain term on the cathode integrator**. Repo ledgers 3 → 4, ctest 24 → 25. (3) **The AC30 is now the LEAST compressing of the three, not the most** — on the shared JCM/Twin burst metric **AC30 1.07 < Twin 1.16 < JCM 1.76 dB**, and that is correct: the JCM is fixed-bias class AB behind 150 Ω and drops its rail **39.1 V**, the Twin 13.4 V, the AC30 (class A, near-constant draw, the stiffest valve rectifier) **1.9 V**. `testSag`'s ordering assertion is **REVERSED** with the derivation in the test.
- **The protected property held where it matters, and the honesty gate fired where it did not.** The owner's protected item was the high-volume distortion character. **h2 — the harmonic the AC30's character and the whole §46 chime argument rest on — moved +0.26 dB (VOL 0.60) and +0.31 dB (0.85)**, inside the ~1 dB gate, and it is 6–21 dB above every other harmonic. **The gate FIRED elsewhere and nothing was compensated:** at 0.85 h3/h4 rose 3.4/4.8 dB (the amp is 3.2 dB louder at the same knob now the wall is off) and at 0.60 the **odd ladder h3/h5/h7 collapsed by up to 15.9 dB** — which is exactly the signature of deleting an ODD function (`x/(1+k|x|)`), i.e. the model stopped making distortion it had no circuit reason to make. **Second honesty correction: "thin low end" was NOT a steady-state level tilt and this slice does not fix one** — the per-frequency tilt re 440 Hz went 0.1–0.3 dB *deeper*. What the low end gains is dynamic (less LF-specific distortion, a surviving pick attack, **6.8 dB** of restored range — the 0.05 → 25 V drive sweep spanned 13.1 dB of output and now spans 19.9); a steady-state tilt, if still wanted, is the tone stack's or the OT's.
- **`kVsupply` = 335.1131 V is DERIVED to preserve the idle point, deliberately, and the 32.5 V gap to the published 342 V CT is left open.** Closing it here would make `kVsupply` absorb finding 9's known-hot screen fit — the exact failure mode that put two factor-of-2 mistakes inside `kFullScaleSecV`. Idle rail / Vk / Ip / Ig2 are unchanged to **seven figures** (309.4904457 → 309.4904201 V), so every difference the slice measures is dynamics. `ts_ac30` golden **−1.86 dB RMS / 3.12 dB @ 200 Hz — NOT blessed**, so core ctest is red at exactly that gate and nowhere else; the other four goldens **UNCHANGED** (the scope check). **CPU: no measurable change** (order-balanced interleaved A/B, 12 runs — AFTER 36.80 % of a stream, BEFORE 37.26 %, against a 12.6-point within-binary spread; the slice deletes work per oversampled sample and adds none). Latency unchanged. **Process warning found the hard way: `git stash` writes a repo-GLOBAL `refs/stash` shared by every worktree** — a sibling agent's stash landed in this worktree on `pop`. Recovered via `git fsck --dangling`; use scratch-directory file copies for before/after builds, never `git stash`, while parallel slices are running.
- **In flight (2026-07-31): the native cab/IR picker** (docs §30 amendment, `docs/work/2026-07-31-native-cab-picker.md`, branch `claude/native-cab-picker-6f557i`) — the owner's *"where do I pick my cab/ir? I want that for recording"*: the web has had Clean 2×12 / Brit 4×12 / user-IR upload since v1.1 and the plugin was hard-wired to `generateDefaultCab2x12IR`. Chain-parity catch-up, **native/ + docs only** (no core, no web, no WASM rebuild, goldens untouched by construction). The engine's convolver pair is now **double-buffered** (`cab_[2][2]` + an atomic active index) on ADR 003's split — the MESSAGE thread synthesises/decodes the IR, peak-normalizes a user one and runs `CabConvolver::prepare` on the SPARE pair, the AUDIO thread does one CAS + one integer flip at the declick fade zero, and the retired pair is reclaimed back on the MESSAGE thread. **The worklet's audio-thread `free()` is NOT copied** — that is the one thing this fixes rather than mirrors. The handshake needed **five** states, not three: a naive Idle/Armed/Swapped word lets the message thread compute the retired pair as the one about to go live, so the audio thread claims with `Armed→Committing`, flips, then release-stores `Swapped`. **Measured: seam step 0.002413 against a 0.003017 bound — exactly the settled signal's own 220 Hz slope, i.e. the fade contributes nothing** — against **0.021781 (7.2×)** for the same switch spliced hard; perturbation (arm replaced by an immediate commit) takes the seam to **0.060711, 20× the bound**, and the case goes red. **Latency does not move for any cab** (partition stays 128: `clean212 264 / brit412 264 / custom 264`). **`identical_core_test` is UNTOUCHED and green at max |plugin−ref| = 0.000e+00 on all eight cases** — the proof that the default state still renders bit-identically. `cabModel` is an APVTS **choice parameter**; the custom IR's **PATH is a state-tree property, never a parameter** (a host cannot automate a path), and host automation of the choice can land on the audio thread so the listener does nothing but `triggerAsyncUpdate()`. The loader follows `web/src/cab.ts` deliberately (mono-ise by AVERAGING, resample to the engine rate, cap 4096 with a 128-sample raised-cosine tail fade, hand the samples over UN-normalized — the engine normalizes, where the C ABI does). Missing IR → the web's fallback (Clean 2×12 + a note under the chip), with one deliberate divergence: native **keeps the path**, so a session opened before a drive is mounted is one click from repair. New ctest entry `clipper_cab_state` (33 checks: round trip, path-is-not-a-parameter, stereo/44.1 k load, missing-file fallback, bad-file refusal, retry, cab-independent latency). UI: a CAB IR chip under the Cab lever, both themes, snapshot scenes `native_cab_*`.
- **Two things that slice found, worth knowing before you touch the native cab path.** (1) **Do NOT peak-normalize a BUILT-IN IR** — the generators already do (M6.6), and a second pass rescales by ~1+ε and moves the low mantissa bits of a render `identical_core_test` compares exactly; normalization belongs to the CUSTOM branch only. (2) **One menu click was arming TWO declick fades**: a user pick reaches `applyCabFromState` directly (so the UI is synchronous) AND again through the APVTS listener's async hop, and if the audio thread committed in between, the second pass armed a second swap. Deduplicated by a `choice|path` signature — which then broke the *retry* case (the file came back; re-picking Custom looks identical to the request that failed), so a USER action carries `force` and bypasses the dedup. Both are asserted. **Open, and needed before it ships:** `.github/workflows/ci.yml`'s native job filters `ctest -R 'clipper_identical_core|clipper_chain_edit'`, which does NOT match `clipper_cab_state` (a one-line change, out of that slice's native/+docs scope), and **the AU target cannot be compiled on Linux** (`CLIPPER_FORMATS` adds AU only `if(APPLE)`) — Standalone + VST3 both link clean, nothing platform-specific was added, but the AU build has to be run on the Mac.
- **In flight (2026-07-31): native visual pass 3** (`docs/work/2026-07-31-native-visual-pass-3.md`, branch `claude/native-visual-pass3-6f557i`) — the owner's screenshot review of pass 2 ("power button shadows are clipped, bright switch goes too far down and has a white line on top of the orange, the pedals don't have shadows, the chorus/trem is broken, I wanted light/dark mode like the web app"). Five fixes, UI-only (no DSP, no chain, no core/web, no WASM rebuild, goldens untouched by construction). **(1) The clipped power cluster was a WIDTH bug pass 2 never touched:** it fixed the cluster's height and left the width at `jlimit(96,168,in.getWidth()/3)`, which hands the power control **36 px for a 46 px rocker** — the widget was narrower than the thing it draws, so the rocker, its 5/5/12 cast shadow and the halo's outer ring were sliced at the bounds. Each widget now reports its own shadow head-room (`LeverToggle::preferredWidth()` 46 / `PowerControl::preferredWidth()` 70 / `ModeSwitch::preferredWidth()` 94) and the cluster asks for the sum, scaling slots *together* under squeeze; measured 36 → **70 px** at 1360×640 and 31 → **70** at the 1040 minimum. **(2) The lever's white line was the token being right and the material wrong** — `.t-lever`'s `inset 0 1px 1px --sh-light` is `rgba(255,255,255,.95)`, which across a saturated accent reads as a white line painted on the orange; the lit lever now takes its own accent brightened, the unlit cap keeps the web rim. "Too far down" was easeOutBack peaking at **1.0987** = 2.17 px outside the slot at both ends; `leverPos_` is clamped to [0,1], so the spring keeps its timing and gains a physical stop (settled lit = the web's `top:26px`, 4 px inset, by construction). **(3) The mode switch was two bugs in one:** the inset shading was stroked as FULL rounded rects instead of each segment's own corner-specific path (so every carved neighbour drew a capsule outline inside itself while the active middle segment — correctly corner-less — drew none: the "sharp-cornered wider red block"), and the stack ran edge-to-edge so the active `2px 3px 6px` cast was clipped and piled up as apparent width. Rebuilt to the CSS: one footprint per segment, the −1 px overlap, only the stack's outer corners rounded, active drawn LAST inside shadow head-room. Both the 3-state and 2-state forms. **(4) Pedal shadows were a JUCE clipping rule, not a missing recipe** — a component's paint is clipped to its own bounds, so `PedalCard` painting its own cast shadow put all of it underneath itself (the input/amp cards had shadows only because the EDITOR paints them). `drawChassisCard` split into `drawIslandCastShadow` + `drawChassisBody`; the parent (`paintBoardContent`) casts the cards' shadows onto the rail. **(5) DARK MODE, and the finding that made it cheap:** the dark token root's control values are **byte-for-byte** the values pedal.css pins `.pedal` to on every theme, so `darkIsland()` already WAS the dark bench scheme — no third scheme, and the pedals are theme-invariant for free (web parity). Only the bench values and the ACCENTS differ, and accents are NOT pinned (they resolve at the root), so `PedalFace::accent` became a token (`skin::AccentId`) rather than a value. Themed: bench, amp panel + every amp control, rail + mat, scrollbar, edge veils, top bar, cables, the input card's cast shadow. Theme follows the OS via `juce::DarkModeSettingListener` plus a 3-way **Auto/Light/Dark** chip in the top bar, persisted in a `juce::PropertiesFile` (`~/.config/Clipper/Clipper.settings` on Linux — `folderName` is set to `.config/Clipper` or JUCE would use `~/Clipper/`), **NOT an APVTS parameter**, so it never reaches host automation or a session; the store is opened/closed per call because a `PropertiesFile` (Timer + ChangeBroadcaster) outliving the message manager is a shutdown assert waiting to happen. **Known limitation: `Desktop::isDarkModeActive()` has no X11 implementation in JUCE 8.0.4, so Auto resolves LIGHT on Linux** and the override is how you get dark there (macOS/Windows follow the OS). Measured by headless before/after screenshots (`clipper_editor_snap` + Xvfb, both themes × all four amp voices); native tests pass, full Standalone+VST3 build clean, scope check `native/` + `docs/` only.
- **In flight (2026-07-31): reverb coaching** (docs §48 amendment, `docs/work/2026-07-31-reverb-coaching.md`, branch `claude/reverb-coaching-6f557i`) — the follow-up to §48's wet trim ("the top half of the reverb knob is too strong to ever use") answered with **no DSP change**: the research says a blackface Twin's usable range genuinely IS the bottom third (real owners run 1-2.5 of 10, surf past 3-4, and there is a dwell-mod industry for everyone else), and our parity-at-knob-~0.60 is *polite* next to a real Twin's ~0.35. Owner decision 2026-07-31: keep the authentic law, move the fix to the **assistant** — the stable `SYSTEM_PROMPT` block now coaches 15-35 for classic spring presence and ~40+ as deliberate surf/drip, and says that this matches the real amp rather than apologizing for it. Prompt + docs only; no tool added, no volatile rig-state path touched, no golden moved, no WASM rebuild.
- **Just landed (2026-07-25, in merge order):** CI at last (`.github/workflows/ci.yml` — core ctest, web tsc+vite+Playwright, node suites, a dependency-free Conventional Commits gate, JUCE identical-core advisory) · `fix/proxy-loopback` — the audit's Security & app layer cleared: the proxy binds `127.0.0.1` by default (`HOST` opts into wider exposure, loudly), the banner reports the address actually bound, the Electron window gets a CSP (with `'wasm-unsafe-eval'` — without it WASM never compiles and the app makes no sound), `will-navigate`/`setWindowOpenHandler` denial, a media-only permission handler, an explicit `chmod 0600` on a pre-existing `config.json`, a real path-containment check, and `req.destroy()` on body-limit overflow (node suites 27 → 35) · **audit finding 1 fixed** — the non-finite parameter guard + engine reset path (docs §28, ADR 002): one shared NaN-rejecting clamp (`core/include/clipper/dsp/ParamGuard.h`) replaces ~14 broken copies, every `*_set_param` C ABI export hard-rejects non-finite, and `reset()` exists down the whole tree (`clipper_reset` / `rat_reset` / `sd_reset` / `ts_reset` / `phaser_reset` / `muff_reset` / `gold_reset` / `amp_reset`). Measured 48000/48000 → **0/48000** non-finite samples after one NaN, on all ten units; `Jcm800Amp::reset()` is ~302 000× cheaper than `prepare()`. New ctest target `clipper_nan_guard_tests`.
- **ALL THREE audit shipping-blockers are now fixed.** **Finding 2 — the cab swap is off the render path** (docs §30, ADR 003): split into `amp_prepare_cab_*` + `amp_commit_cab`, double-buffered at the ABI, so the audio-thread step is **0.000001 ms and 0 allocations** against a prepare of 10.97–42.66 ms. Worst render block over a 4096-tap IR load: 104.92 ms (39 quanta missed) → **0.42 ms (none)**. Bit-identical to the pre-fix audio.
- **Honest residual on finding 2 — it is NOT "real-time safe".** `amp_prepare_cab_*` runs in the worklet's `onmessage`, which **is** the audio rendering thread; this moves the work *between* quanta rather than off-thread. A guaranteed multi-quantum stall becomes at worst one late quantum on a deliberate action. The real fix is main-thread spectra. Also still on the audio thread: `_destroyPedal`'s `free()` inside `_commitPending`, and `_postLatency()` posting from `process()`.
- **Found while fixing finding 2, not yet fixed:** `_prepareChain` mutates `engaged` on the *live* node, so an engaged-flag change arriving in a `chain` message is not declicked (a stomp via `_pendingBypass` is). `CabConvolver::prepare()` zeroing the FDL means a swapped-in cab is silent for up to two partitions — handled with a 256-sample `'hold'` declick phase rather than by touching the convolver. The `removed`-list union also fixed a **WASM handle leak that was live on `main`**, masked there by the eager commit.
- **In flight (2026-07-31): the Muff DC-blocked diode branch** (docs §53, **ADR 010**, `docs/work/2026-07-31-muff-dc-blocked-diodes.md`, branch `claude/muff-dc-diodes-6f557i`) — ADR 009's named follow-up, closing audit finding 16's bass half and the owner's "still a little gutless, it doesn't scream through". The published clip stage puts a **1 µF cap (C6/C7) in series with the feedback diodes** and this model never had it, so the pair carried DC, **conducted at idle**, clamped the collector 0.26 V above the base and shunted the base node to ~1.8 k (coupling corner ~900 Hz per clip stage = the missing bass). `BjtStage::Config::Cdiode` adds a config-gated **4th Newton node** (the cap↔diode junction; 4×4 Gaussian with partial pivoting, sharing ONE templated damped Newton with the 3-node path) and `Config::Rbg` (RA = 100 k) is finally **used** — the two are one decision. **`Cdiode == 0` is bit-identical**: 15 stage digests (5 rates × 3 shapes) and 10 whole-`MuffModel` digests match the pre-slice build, iteration counts included. Measured: clip bias Vc **1.213 → 4.946 V** / Ic 0.777 → **0.397 mA** (published ~0.4 mA), idle diode current **0 A**, low E **−14.24 → −5.48 dB** re 1 kHz (finding 16 CLOSED), max-sustain sustain time **+3.275 → +4.175 s** over the input's own decay (the "scream"), max THD 40.8 → 37.6 % (the wall stayed articulate). **`kClipDriveMax` 6.0 → 1.0 is an UN-FITTING** (a passive pot cannot pass more than unity; the 6× existed to slam stages that had no headroom), with the §43 floor re-derived −70 → **−65**. **Both XFAILs XPASSed → deleted → hard**, so `clipper_muff_tests` has ZERO known defects and its ledger registration came off (ctest 25 → 24, repo ledgers 4 → 3). `muff_twin` golden **−1.09 dB / 13.18 dB @ 252 Hz — NOT blessed**, so core ctest is red at exactly that gate and nowhere else.
- **Two things that slice found by tracing rather than tuning — read before touching `BjtStage`'s solver.** (1) The damped Newton's ±10 V safety clamp clips step **components**, which rotates the direction off Newton's; a rotated direction need not be a descent direction, so the line search rejects all 30 backtracks, `lam` collapses to 2⁻³⁰ and the solve **stands still** for the whole 60-iteration cap. That is the mechanism behind `muff-slam-exhausts-newton-cap`. The 4-node path **scales** the direction to the same bound (0 of 16 at the cap, worst 18/60); the **3-node path deliberately keeps the defective clamp**, because fixing it moves every existing `BjtStage` user's audio. (2) **Idle is no longer free, and that is physics:** the 1 µF cap can only discharge through the diodes' own leakage (τ ≈ 5 s), so a played-then-quiet stage presents a ~2e-11 A residual, six decades above the 1e-17 early-out — **1.75 evals/solve vs 1.00 parked**, but **0.00 %** backtrack burn, so finding 12's pathology (31.00) has NOT returned. CPU is the real cost: interleaved A/B **5.62× realtime / 17.8 % of a stream → 3.2× / 30 %**. Named follow-up: the 4×4 is sparse (J[2][3] = J[3][2] = 0), so a specialised solve or a Schur complement should recover most of it. Also fixed in passing: `testIdleSolverCost` claimed to cover a played-then-quiet stage and never did — it read a fresh `probe` model.
- **In flight (2026-07-31): the GOLD drive pre-filter** (docs **§56**, `docs/work/2026-07-31-gold-prefilter.md`, branch `claude/gold-prefilter-6f557i`) — §54's named follow-up, and the last knowingly-approximate block in the drive path. `kDrivePreScale`/`kDriveHpHz` (a scalar 0.65 into a one-pole 600 Hz HP) are **deleted**; the drive branch's input network is now the reference's own netlist — `C3` 0.1 µF into `(R6 10 k ‖ C5 68 n) + gang-1` ‖ `(R7 1.5 k + (R19 15 k ‖ C16 1 µF))` — **third order and knob-dependent**, because the GAIN pot's OTHER gang half is its shunt leg. One wiper, two networks: `driveLegOhms()` is shared, so §50's law and the divider can never desynchronize. **Worst |H_pre| error vs the reference's own WDF tree (rebuilt on this repo's pinned chowdsp_wdf and driven sample by sample): 7.23 dB → 0.0006 dB** across 41 Hz–10 kHz × three knob points; phase within **0.06°**. §54's quoted "±1.3 dB" was the 82 Hz–1 kHz window at one knob position — the honest whole-band figure was 7.23. **GAIN 0 bit-exact (6 render hashes identical), ALL FIVE GOLDENS UNCHANGED at ±0.00, nothing blessed.**
- **What the GOLD refit found that was not on its plan — a −73 dBFS noise floor, in code that looked fine.** The netlist divider is a THIRD-order recursion whose slowest pole (~60 Hz) runs at the OVERSAMPLED rate, so at 192 kHz that pole sits at radius 0.998 and a direct form amplifies its own state rounding by ~(1−r)^−order. With `float` state — the style `AmpStageNetwork` already used — the model emits **audible broadband hiss**, and no bar in the gold suite could see it because every one of them is a single Goertzel bin. Measured against a long-double build: shipped 48 k×4 **−73.4 → −136.2 dBFS**, 96 k×8 **−32.0 → −112.5**. Both networks' state is now `double`; §54's `AmpStageNetwork` measured **−120.1 dBFS** shipped on its own and was widened too (drive-path latitude, isolated proof, NOT bit-identical — the residual IS the noise). New perturbation-proven `testDrivePathNumericalFloor` (project's own −120 dBFS gate; float measures −72…−80 and fails by 40 dB). **Honest residual: −112.5 dBFS at 96 k×8 is still above the gate** — the cure is a cascade of first-order sections (every pole AND zero of this network is real), named and deferred.
- **…and a THIRD thing the GOLD refit found, which is a house-wide idiom bug: `flushDenormal` DOES NOT CONVERGE above first order** (docs §56.4b). `clipper_denormal_tests` went red on "pedal never settled to EXACT digital silence" and it was right. The house one-liner `y1 = flushDenormal(y)` guards only the NEWEST recursive tap — complete for a one-pole, WRONG for a direct form of order ≥ 2, because the older taps keep what they held and the recursion re-injects them with coefficients of magnitude ~3: y1 is zeroed, the next sample computes −a2·y2 − a3·y3 which is LARGER than either, and **the state pumps itself back over the floor and sits there**. Measured plateau on the GOLD: **3.373e-27 at 2 s of silence, 1.531e-26 at 8 s, 3.300e-27 at 16 s** — not decaying, sitting. Bisected: divider bypassed settles in 1.523 s, whole-state flush settles in 1.501 s. Both GOLD networks now test ALL their taps and zero the state as a unit; `AmpStageNetwork` was not failing (its poles are decades higher so the ring-down clears the floor fast) but is now written correctly rather than luckily. Bit-transparent (floor 1e-30, ~600 dB down) — GAIN-0 hashes and every field row unchanged by the fix. **Before you write the next order-≥2 filter in this repo, use the whole-state form** — the scalar guard's contract is per-value, and the POLICY it is meant to implement is only satisfied at first order.
- **Two traps in the KlonCentaur oracle, and one in this repo's tooling.** (1) `PreAmpStage::processSample` returns the physical divider **NEGATED** — a `PolarityInverterT` port convention, measured 180.08° at 41 Hz with magnitudes agreeing to 0.0000 dB. The dirt branch is SUMMED with the clean feed, so a sign error there would change the sum, not just a scope trace. (2) It also reads its element voltages BETWEEN the two `incident()` calls, so it reports the previous sample: residual phase error is **exactly 360·f/192000** at every probe point. Neither is a disagreement about the circuit; both read as one. (3) **`git stash` is NOT safe in a worktree here** — stashes are shared across every worktree of the repo, and an A/B build that stashed raced the parallel AC30 slice and popped THEIR stash into this tree. Both sides were recovered; use plain file copies for A/B builds while another agent is active.
- **The GOLD refit crossed the web `pushedH3` bar exactly as §54 predicted, and it was RE-DERIVED, not loosened.** 0.1056 → **0.09774** against a 0.1 bar. The cause is not less distortion: `pushedF1` is the **vector sum** of the dirt branch and the parallel clean core, and the refit rotates the dirt branch **−15.48°**, so f1 rose **+0.375 dB** while h3 moved only −0.274. Predicted from the netlist plus PRE-refit measurements only (no post-refit number enters): f1 0.771613 vs measured 0.771566, h3 0.076203 vs 0.075409, h3/f1 **0.09876 vs a measured 0.09774** → bar 0.1 × 0.93773 = 0.09377, shipped **0.0938** (rounded UP from the derivation). Margin 4.2 % against the old 5.3 %; the contrast clause got no easier (167× against a 20× bar). **Field movement, REPORTED not aimed at:** default GAIN 0.35 THD **4.586 → 4.020 %**, max **14.588 → 13.563 %**, 5 % breakup onset **0.4163 → 0.4963**. The owner said the GOLD is "potentially still a bit gainy by a touch but let's not change for now" — this moves that way **by accident of the component values**, and nothing was tuned to produce it.
- **The M11 ragged-block window was re-derived by this slice, and made HARDER in the same change** (§56.7). `gold_ ABI` at 100 frames went red (1.22e-02 vs a 2e-03 settled bar) and the stream is **not** corrupted — the difference decays to **exactly 0 by 400 ms**. `kBlockSettleSecs` 0.05 → **0.25**, because the old derivation ("past every parameter smoother's ~5–8 ms settle") was measurably incomplete: what has to settle is the slowest recursive **state** the differing trajectory excites, and §56's divider carries a ~30 ms corner. Widening a window can only make a test easier, so the same change adds the bar that actually separates "smoothed differently" from "stream corrupted" — **the difference must CONVERGE** (`kRaggedTailBar = 1e-4` over the final quarter). Every unit measures **exactly 0.0** except the Muff at 3.34e-05, which is §53's τ ≈ 5 s diode caps and is documented as such.
- **In flight (2026-07-31): the GOLD clipping-stage fidelity trio** (docs §54, ADR 016, `docs/work/2026-07-31-gold-clipping-fidelity.md`, branch `claude/gold-fidelity-6f557i`, **stacked on `claude/gold-summing-6f557i`**). The owner's answer to §52's honesty gate ("full fidelity slice"): the three defects §52 named are fixed against the cloned `KlonCentaur` netlist, and the derived summing weight **4.1702 stays** (its perturbation test still guards it). (1) **The diode node** — the reference's fit to a REAL unit's measured clipper (`Is = 15 µA, Vt = 25.85 mV`, n = 1 → knee **0.109 V**) behind the schematic's **`R13` = 1 kΩ**, with the node's own **`R16` = 47 kΩ** load in the WDF tree (the same 47 k that carries the dirt current — exactly as `ClippingStage.h` builds it). The old datasheet-shaped 200 nA / n 1.3 pair measured **+5.7…+8.5 dB hot** across a 0.05–20 V sweep. §36's silicon fix is untouched; the Ge/Si CONTRAST survives as a property, re-derived off the two device models' own knees (0.1086 vs 0.5839 V = **14.61 dB** analytic, **12.70–14.29 dB** measured, band 4–9 → **11–18 dB**). (2) **The 495 Hz summing pole** (`C13` 820 pF across `R20` 392 kΩ) — the creamy filter, and the reason `--golden-report` did not move: it is applied to the **dirt branch only**, which is *algebraically identical* to "pole on the sum with the clean feed pre-emphasized" and therefore keeps this model's flat-2.0 composed clean path exactly as §27 left it. **GAIN 0 is BIT-EXACT** — four render hashes identical before/after. ADR 016. (3) **`C7` = 82 nF across `R10b`** — the drive amp is now the real R10b/R11/R12/C7/C8 network (`AmpStageNetwork`), so its gain is knob- AND frequency-dependent (25.8× DC but **47.6× at 220 Hz / 100.5× at 1 kHz** at max knob). Its ground leg is recovered from the smoothed `A` (`R10b+R11 = R12/(A−1)`), so **§50's gang law is the network's DC gain by construction** — an identity, asserted as bar 4 of the new test.
- **What the trio measures, and the one acceptance row it MISSES.** Field anchor (0.15 V / 220 Hz, TREBLE noon, OUTPUT 0.5): default GAIN 0.35 **5.84 → 4.59 %** THD / −5.34 → **−11.33 dBFS**; max GAIN **23.70 → 14.59 %** / −0.74 → **−7.90 dBFS**. Breakup onset 5 % **0.28 → 0.42**, 10 % **0.61 → 0.87**. Drive-node error vs the reference at max knob: 220 Hz −4.6 → **+0.7 dB**, 500 Hz −9.6 → **+0.35**, 1 kHz −13.1 → **−1.3** (worst anywhere 13.1 → **3.6 dB**). Whole-pedal HF harmonic energy **−22.9 → −29.4 dB** = the pole doing its job. **MISSED: max THD 14.59 % is 0.4 points UNDER the reference's 15–25 % band**, and "near-clean at knob ≤ 0.10" reads 2.86 % (inside the real unit's own 0.2–3.9 % min-THD range, but not clean). Reported per the gate, **not fitted**. Max level is 6.1 dB above §50's and 7.2 dB below §52's.
- **BOTH §52 XFAILs XPASSed → deleted → hard.** `gold-summing-rails-engage`: the tone stage's node **8.624 → 1.274 V** against the 8.600 V rail. `gold-summing-alias-at-treble-max`: 4× **−26.5 → −92.9 dB**, and it MOVES with the factor again (1× −27.3 / 8× −106.8) — so it gets **two** bars, −60 dB *and* ≥ 20 dB of 1×→4× improvement, because the defect was never "too high", it was "stopped depending on oversampling". `clipper_gold_tests` has no ledger: **ctest 26 → 25, repo ledgers 5 → 4**. New `testClippingStageFidelity` carries one perturbation-proven bar per fix (revert each alone → red; C7 removed collapses the knob-dependent tilt spread +8.07 → **−0.04 dB**) plus the §50 DC identity (12.480 measured vs 12.468 analytic). M11 GOLD rows re-baselined; the A4 window (9..29) measures +12.5 and was **deliberately not re-snugged**.
- **Two traps this slice found, worth knowing before you probe the GOLD.** The reference germanium's `Is = 15 µA` gives a zero-bias incremental resistance of **861.7 Ω** (against 84.0 kΩ for the old pair), so the pair loads its own node by 6.78 dB at *any* level — it has **no truly linear region**, which is the bloom as a number and is why every small-signal bar in the suite is a ratio. And `kDrivePreScale`/`kDriveHpHz` were **re-scoped, not changed**: §52 validated them against `H_pre·H_amp/A(g)`, but with `H_amp` now exact they stand for `H_pre` ALONE (within ±1.3 dB across the core band; the real `H_pre` is a ~1105 Hz one-pole into a 0.93 shelf where the model has 600 Hz into 0.65 — a named refit candidate, deliberately not taken here so each fix keeps an isolated perturbation proof). Also newly tight: the web `gold worklet` spec's `pushedH3 > 0.1·pushedF1` bar now measures **0.1056** — left alone rather than loosened, but it is the pedal's tightest guard now.
- **In flight (2026-07-31): the GOLD summing network** (docs §52, `docs/work/2026-07-31-gold-summing-network.md`, branch `claude/gold-summing-6f557i`, UNPUSHED — this one needs an owner decision before it goes anywhere). §50's last FITTED constant, `kClipBlendWeight = 0.65`, is now the schematic's own number. **The reference WAS reachable this time** — §27's honesty note says the DAFx-19 paper was refused by the proxy; `electrosmash.com`/`coda-effects.com` still fail (DNS/403) but Jatin Chowdhury's `KlonCentaur` repo clones fine and carries the section-by-section netlist AND the ElectroSmash schematic figures. The summing stage is a **transimpedance amp, not a mixer**: dirt reaches U2A's virtual ground through `C10` 1 µF + **`R16` 47 kΩ** (`C10` is a 3.4 Hz HP, so NO in-band post-diode attenuation), clean through **`R19` 15 kΩ** (FF1, bass) and `C11`/`R15` (FF2, treble), all against **`R20` 392 kΩ**. So `kClipBlendWeight = (R20/R16)/kSumGain = 8.3404/2.0 = **4.1702**`. Independent check that landed: `R20·G_clean` measures 1.82–2.28 across the band vs this model's `kSumGain = 2.0` — the clean side was already right, only the dirt side was a fit. `kDrivePreScale`/`kDriveHpHz` **validated not changed** (netlist `H_pre·H_amp/A` = 0.2234/0.5343 vs the model's 0.2238/0.5574 at g = 0.35); `kClipBlendFadeTo` re-examined and deliberately unchanged (the network gives it no support at all — it is purely the `clipBlend(0)=0` contract).
- **…and the HONESTY GATE FIRED: the derived weight is 6.42× LARGER than the fit, so the pedal got LOUDER and DIRTIER — the opposite of the field report that commissioned the slice.** Shipped anyway, unfitted, per the plan's gate. Measured at the 0.15 V unity-trim anchor: max GAIN **−14.00 → −0.75 dBFS (+13.3 dB)**, THD 16.8 → **23.7 %** (27.5–28.3 % at 0.30/0.50 V, i.e. **outside** the 15–25 % reference band), 5 %-THD breakup onset **GAIN 0.53 → 0.28**, dirt/clean at max 1.29 → **8.27**. GAIN 0 is **bit-identical** (FNV-1a `85a97e9efc5686ba` both sides) so the transparency contract and the web min-gain spec are untouched by construction; **NO golden moved** (all five UNCHANGED at ±0.00). Two properties lost → two new XFAILs, same root cause (`clipper_gold_tests` gains a ledger; ctest 25 → **26**, repo ledgers 4 → 5): `gold-summing-rails-engage` (at max GAIN + max TREBLE the tone stage hits **8.62 V** against the ±8.6 V charge-pump rails and clips at BASE rate — §27's "the diodes are the only clipper" is broken) and its shadow `gold-summing-alias-at-treble-max` (1× −20.3 / 4× −26.5 / 8× −26.4 dB, flat in the OS factor because it is rail clipping, not aliasing). `testAliasing` was **split, not loosened** — the −60 dB M2 bar is now hard on the isolated stimulus (TREBLE noon: **−108.3 dB @44.1 k / −127.7 @96 k** at 4×).
- **Where the GOLD's perceived gain actually lives — the named next probe, NOT the mix.** (1) **The diode node runs ~2.1× hot:** our Ge pair is `Is = 200 nA, n = 1.3` (knee 0.286 V) through `Rs = 2.2 kΩ`, against the reference's fitted `Is = 15 µA, Vt = 25.85 mV` (knee **0.109 V**) through the schematic's `R13 = 1 kΩ`. The schematic's summing weight is right; the node it multiplies is too loud. Fixing it closes both new XFAILs. (2) **The `R20 ∥ C13` = 495 Hz summing-amp pole does not exist in this model at all** — that pole is what keeps a clipped Klon *creamy* rather than bright, and is the best explanation on the table for "sounds like a Marshall at mid-high gain"; it CANNOT be ported alone (on our flat clean feed it would put a ~−14 dB midrange hole in the GAIN-0 path), so it needs the tone stage in the same slice. (3) The reference's drive shaping is strongly gain-dependent (`C7 = 82 nF` across `R10b` → 1 kHz gain 100.5× at g = 1 vs the 25.8× DC law) where ours is flat — measured, and it makes the dirt hotter still. **Do NOT answer any of this by re-fitting `kClipBlendWeight`, and do not raise `kRailVolts` — it is a real supply.** The owner's call: keep the derived value and fix the diode node, or revert to 0.65 and record it as an acknowledged fit. **DECIDED 2026-07-31: keep the derived value, fix the node — all three of (1)(2)(3) above are done in `claude/gold-fidelity-6f557i` (docs §54, ADR 016), stacked on this branch.**
- **Just landed (2026-07-31, merged as PR #29): the JCM GAIN taper** (docs §51, `docs/work/2026-07-31-jcm-gain-taper.md`, branch `claude/jcm-gain-taper-6f557i`) — the last knob-feel item of the field-report round, and the first report made at **unity trim** with §45 + §47 already in: "breakup is still slightly early — 20 sounds like what I want 30 to sound like; 100 is perfect right where it is." No circuit change: the taper constant is now **per pot**. `kGainTaperK` 4 → **5.0521652926683824**, the bisected root of `gainTaper(0.30) == audioTaper_{k=4}(0.20)` — i.e. the owner's sentence IS the design equation; `kMasterTaperK` stays 4.0 (MASTER's IEEE-754 bit fingerprint over 101 positions identical). The GAIN wiper is the only thing the knob controls (the §47 bright-cap coefficients rebuild from it), so the remap is exact: the new GAIN 0.30 render reproduces the pre-slice GAIN 0.20 render to **−147.0 dBFS / −119.6 dB rel**, and `taper(1) = 1` for any k pins **GAIN 1.0 bit-identical by render hash** (`484ed0c37929dc69`), which is "100 is perfect" honoured by construction. **Correction to the plan's premise, found by measuring first:** the ≥5 % THD onset was NOT at knob 0.20 at the §45 0.10 V probe — it was 0.2717 there; the owner's "20" reproduces at **0.15 V peak** (a normal unity-trim pickup level, onset 0.2057). Both levels are reported: onset **0.2057 → 0.3064** at 0.15 V (the 0.30 ± 0.02 bar, MET) and **0.2717 → 0.3764** at the §45 0.10 V convention. Curve monotonic both sides; travel is 4.1 dB colder at the 0.5 default, 0.0 at 1.00. New perturbation-proven `testGainTaperOnset` brackets the onset into (0.28, 0.34] with two composed-amp renders (4.27 % @ 0.28 / 5.93 % @ 0.34; k back to 4.0 measures 7.10 % and fails). Five player-expectations reference rows re-baselined (A3 GAIN THD 0.2→10.8→48.5 → **0.2→6.9→48.5**). Golden **`rat_jcm800` −1.08 dB RMS / 4.50 dB @ 2016 Hz — owner-blessed 2026-07-31 (GOLDENS.md) before merge**; other four goldens unchanged. Web/Playwright/node/electron all green; WASM artifact rebuilt.
- **Just landed (2026-07-31, merged as PR #28): native visual pass 2** (`docs/work/2026-07-31-native-visual-pass-2.md`, branch `claude/native-visual-pass-6f557i`) — the owner's "draw order issues on amp on/off switches / no sfx / web is the golden oracle" report. Root finding: **the web amp resolves the LIGHT token context** (the dark pinning is `.pedal`-scoped), so the native amp now paints as the web's porcelain panel via `skin::Scheme` (light/dark token sets from tokens.css, verbatim) — knobs, `.raised` card, levers, rocker, mode segments all on the light recipes; pedals stay dark. The clipped-glow bug was the jewel at its component's top edge (JUCE clips to bounds → square halo) plus a cluster sized `max(cellH,84)` for a ~132 px anatomy (rocker rendered as a sliver): `PowerControl::preferredHeight()` + `skin::glowSpread()` fix the layout contract, and `drawJewel` is two smooth radial-gradient layers (web `0 0 16px + 0 0 5px`) replacing four stacked discs — pedal LED banding gone too. **UI sfx exist natively** (`UiSound`, ui-sound.ts constants verbatim, own output-only device, **Standalone only**, self-disabling headless): knob detent ticks (0.04, user-drag only) + press/release thunks on every switch. Levers slide with the web's 160 ms overshoot. Measured by headless before/after screenshots (`clipper_editor_snap` + Xvfb — the in-container visual loop works); native tests pass; core/web untouched, no WASM rebuild, goldens untouched by construction. UI-only.
- **In flight (2026-07-31): the JCM GAIN taper** (docs §51, `docs/work/2026-07-31-jcm-gain-taper.md`, branch `claude/jcm-gain-taper-6f557i`) — the last knob-feel item of the field-report round, and the first report made at **unity trim** with §45 + §47 already in: "breakup is still slightly early — 20 sounds like what I want 30 to sound like; 100 is perfect right where it is." No circuit change: the taper constant is now **per pot**. `kGainTaperK` 4 → **5.0521652926683824**, the bisected root of `gainTaper(0.30) == audioTaper_{k=4}(0.20)` — i.e. the owner's sentence IS the design equation; `kMasterTaperK` stays 4.0 (MASTER's IEEE-754 bit fingerprint over 101 positions identical). The GAIN wiper is the only thing the knob controls (the §47 bright-cap coefficients rebuild from it), so the remap is exact: the new GAIN 0.30 render reproduces the pre-slice GAIN 0.20 render to **−147.0 dBFS / −119.6 dB rel**, and `taper(1) = 1` for any k pins **GAIN 1.0 bit-identical by render hash** (`484ed0c37929dc69`), which is "100 is perfect" honoured by construction. **Correction to the plan's premise, found by measuring first:** the ≥5 % THD onset was NOT at knob 0.20 at the §45 0.10 V probe — it was 0.2717 there; the owner's "20" reproduces at **0.15 V peak** (a normal unity-trim pickup level, onset 0.2057). Both levels are reported: onset **0.2057 → 0.3064** at 0.15 V (the 0.30 ± 0.02 bar, MET) and **0.2717 → 0.3764** at the §45 0.10 V convention. Curve monotonic both sides; travel is 4.1 dB colder at the 0.5 default, 0.0 at 1.00. New perturbation-proven `testGainTaperOnset` brackets the onset into (0.28, 0.34] with two composed-amp renders (4.27 % @ 0.28 / 5.93 % @ 0.34; k back to 4.0 measures 7.10 % and fails). Five player-expectations reference rows re-baselined (A3 GAIN THD 0.2→10.8→48.5 → **0.2→6.9→48.5**). Golden **`rat_jcm800` −1.08 dB RMS / 4.50 dB @ 2016 Hz — NOT blessed**, so the core suite is red at that one assert by design (§36/§47 precedent); other four goldens unchanged. Web/Playwright/node/electron all green; WASM artifact rebuilt.
- **Just landed (2026-07-31, merged as PR #26, last of the four-PR stack #23→#26): the GOLD gain gang law** (docs §50, `docs/work/2026-07-31-gold-gang-law.md`) — the "gainy at even 35+ / too saturated" fix, judged against the published schematic + the Klon research report's reference THD rows. Three §27 approximations corrected in one network: drive law linear `1+g·66.7` (24.3× at the 0.35 default = the real knob-0.99) → the schematic's end-loaded `A = 1 + 422k/((1−g)·100k+17k)` = **4.61→25.82×**; the drive branch now attenuates before the diodes (`kDrivePreScale` 0.65 + 600 Hz HP — the 106 Hz corner was FF1's, mis-assigned); blends re-derived (clean feed FLAT, dirt weight FIXED 0.65 — the knob changes drive, not mix; `clipBlend(0)=0` kept as the documented bit-exact-clean contract). Default 0.35: 19.2/13.0 → **1.41/6.37 %** THD; max 30.6 → **15.3 %** vs the reference 15–25 band. Germanium untouched and vindicated again. Two suite probes re-derived in diode-node-voltage units (they were calibrated to the too-hot drive); gang law perturbation-proven. **NO golden moves** — the one slice in the stack needing no bless (GOLD is in no golden rig; GAIN 0 bit-exact).
- **Just landed (2026-07-31, merged as PR #25): the Muff series base resistors** (docs §49, `docs/work/2026-07-31-muff-series-rs.md`) — finding 16's bass half = the max-sustain blowout (research-proven same defect). `BjtStage::Config::Rs` (Rs = 0 bit-identical, hash-verified — RAT/GOLD/SD/TS untouched), Q2/Q3 = the schematic 10 k. Max sustain **150 → 40 % THD** (the wall became articulate), low E −41 → −14 dB (XFAIL stays, re-owned to the 4-node DC-blocked-diode follow-up), slam ledger 6 → 5 (stays). §43 floor re-derived −84 → −70 on the corrected circuit (same player bars: 0.14 → −25.9 dBFS / 10.7 %). `muff_twin` −3.06 dB / 3.76 @ 4032 Hz — owner-blessed 2026-07-31 (GOLDENS.md) before merge.
- **Just landed (2026-07-31, merged as PR #24): the reverb wet trim** (docs §48, `docs/work/2026-07-31-twin-reverb-level.md`) — the owner's "about twice as strong" measured (wet at parity with dry by knob 0.40, +6.5 dB OVER at 0.60) and answered with exactly −6 dB: `kWetGain` 3.0 → 1.5, knob law untouched, parity now ~0.60. `sd1_twin_reverb` −0.83 dB RMS / 6.02 dB @ 504 Hz — owner-blessed 2026-07-31 (GOLDENS.md) before merge.
- **Just landed (2026-07-31, merged as PR #23, first of the stack): the JCM800 gain-pot bright cap** (docs §47, `docs/work/2026-07-30-jcm-bright-cap.md`) — slice 4 of the round, the "flabby chugs" fix. The 2204's 470 pF top-lug→wiper cap now exists as the exact 2-node network solve (H(0) = the old scalar → settled levels unchanged by construction; GAIN 1.0 bit-identical by render hash). Measured drive tilt into V1B: +7.8 dB at GAIN 0.5 / +5.6 at 0.7 vs analytic 7.5/5.5; perturbation-proven `testBrightCap` (C = 0 fails). **Honesty correction:** the diagnosis's +10–18 dB was a pot-only law; the series 470 k halves the tilt in dB. Golden `rat_jcm800` −0.44 dB RMS / 6.73 dB @ 1008 Hz — owner-blessed 2026-07-31 (GOLDENS.md) before merge. Bless-ritual note for future sessions: this environment has no /dev/tty, so the three 2026-07-31 blesses ran the script's gates by hand on explicit in-chat owner authorization — clean tree, `--golden-report` table verified against the approved figures BEFORE `--update-goldens`, entry + justification hand-written into GOLDENS.md in the same commit.
- **Just landed (2026-07-30/31, overnight): the AC30 gain structure — the top-boost channel completed** (docs §46, ADR 015, `docs/work/2026-07-30-ac30-gain-structure.md`) — the big slice of the round, closing the 5-XFAIL ledger §42.8 opened AND audit finding 5. The preamp is now V1 → VOLUME (k = 8 taper, owner-chosen kit position so breakup tracks the knob) → **V2 top-boost triode** (the stage the model never had) → **cathode follower** (226 Ω into the stack) → the corrected 6-node stack. The stack had **three** structural errors: finding 5's series-Cb and missing wiper load, plus a third found in-slice — the slope resistor fed the treble pot's BOTTOM, which inverted the treble knob under low-Z drive; R1 belongs on the top with Ct. Bass rheostat behind a square-law (log-pot) knob map. Constants re-derived per §42.6: kInterstageScale 0.67 → 0.03 (joint parameter search with the taper), kFullScaleSecV 10 → 12.2 (cranked-swing probe). Measured: onset ≈ 0.5–0.6 tracking the knob, **10.8 % at the documented 0.6 vs Twin 3.0 %**, composed h2 **+10.8 dB over the Twin**, clean 0.24 % at 0.35, the 880 Hz hole flat within 1.4 dB, cranked 0.898. **All five finding7-ac30 XFAILs XPASSed → deleted → hard** (chime probe re-derived to the composed amps per §42.9 — balanced PIs rightly cancel evens, the class-A chime lives in the single-ended V2; character guard re-derived off its notch-contaminated 1 kHz reference). AC30 suite has ZERO known defects; repo ledgers 5 → 4, ctest 26 → 25. CPU 30 → 47 % of a stream (interleaved A/B; < JCM 55) and latency +144 samples — consolidating the preamp's per-stage OS domains is the named follow-up. `ts_ac30` golden −4.94 dB / +17.96 dB @ 800 Hz, owner-blessed. Web swap-spec detector re-derived (level → harmonic signature).
- **Just landed (2026-07-30): audit finding 8 — Ra2 → 120 k** (docs §45, `docs/work/2026-07-30-jcm-ra2.md`) — slice 3 of the round. The post-tailRef sweep put the JCM PI's balance optimum at 120 k (ratio 0.703 → **0.988**; the audit's own 100 k guess measures 0.841). Leg-balance XFAIL XPASSed → deleted → hard assertion (perturbation to 82 k fails at 0.703). **The push-pull even-harmonic XFAIL did NOT XPASS** — 8.1 → 12.7 dB of 20: balanced legs are necessary, not sufficient; re-owned to a push-pull-residual investigation (candidates in the decl — don't re-detune Ra2 to chase it). Two bounds honestly re-derived with their derivations in comments: the NFB window ceiling 6 → 8 dB (loop deepened to 6.37 measured vs 6.35 analytic — the identity assert pins the value) and the composed 48 k alias floor −55 → −52 (measured −54.7; escape hatch = oversample the PI, not a lower bar). Full-amp THD floor at GAIN 0.2/0.5: 3.59/13.2 → 2.71/10.7 %. Golden `rat_jcm800` +0.14 dB RMS / 0.26 worst band — inside the gates, NO bless, drift documented. **Field-report note that reframes the round: the owner's original reports were made at input trim 0.8 = +16.8 dB of boost** (unity is knob 1/3); they are re-testing at unity — the GAIN-taper decision waits for that + the bright-cap slice (next).
- **Just landed (2026-07-30): the native trem switch** (§20-amendment parity note, `docs/work/2026-07-30-native-trem-switch.md`) — the Twin's tremolo ON/OFF finally exists in the JUCE editor (the engine had mapped `chorusMode ≥ 1` → `PARAM_TREMOLO_ENABLE` since §20, but the editor never showed the switch). `ModeSwitch` gained `setLabels`/`setAccent` + a clamped `setSelected`; the Twin panel shows Off/On, Clean 120 keeps Off/Chorus/Vibrato, and `applyAmpModel` re-syncs the display from the param on panel change. UI-only, zero DSP/chain change; full Standalone compile-verified in-container (CI does not compile the editor — worth knowing before touching `PluginEditor.cpp`).
- **Just landed (2026-07-30): the Twin VOLUME pot position** (docs §44, `docs/work/2026-07-30-twin-volume-position.md`) — slice 2 of the field-report round ("breakup at 50 means not enough headroom"). The model had the channel VOLUME **after** the V2 recovery stage, so V2's drive was volume-independent and a hot pickup carried a ~4.4 % THD floor the knob could not remove. Moved to the AB763 position (between the stack and V2's grid; the bright bleed moved with the pot). Measured: hot-input breakup onset **VOL 0.45 → 0.75**, RMS matched ≤ 0.08 dB per position, VOL 1.0 bit-identical by construction, clean bar 3.41 → 1.17 %. New perturbation-proven headroom bars in `test_twin_amp.cpp` (pre-fix topology fails 2× over). Goldens `sd1_twin_reverb` +2.18 dB / `muff_twin` +5.85 dB re-blessed with owner authorization (pedal-driven rigs were being compressed by the mis-placed V2 — louder and brighter is the fix, not a drift); other three unchanged (scope check). Web drift guard needed no re-centre. The AC30 XFAIL Twin comparisons got harder, as expected — still XFAIL, owned by the AC30 slice.
- **Just landed (2026-07-30): the Muff SUSTAIN taper floor** (docs §43, `docs/work/2026-07-30-muff-sustain-taper.md`) — first slice of the post-v1.1 field-report round. The owner's "way too gainy, even on sustain=14" measured as the **Muff, not the RAT** (the report conflated them; the RAT at 0.14 is digitally clean). The −54 dB taper floor left the whole knob travel past the clip stages' ~1–3 mV clean window (output moved < 2.2 dB across the travel; 0.14 was the sweep's HOTTEST point via Q2/Q3's gain-expansion hump). Now piecewise dB-linear: knob ≥ 0.6 is the old law **verbatim** (default and above bit-identical by construction — `muff_twin` golden Δ 0.00 dB, no blessing), knob < 0.6 runs down to `kSustainMinDb = −84` (chosen by measurement over −80). Knob 0.14: −4.7 dBFS / 43 % THD → **−26.0 / 14.2 %**. A2 min-gain hum improved 10.6 dB free. New perturbation-proven bars in `testSustainRange` (kSustainMinDb = −54 reproduces the old law exactly — the perturbation is one flip). **The rest of the round, diagnosed and owner-approved, in order: Twin volume-pot position (AB763 order, onset 0.37 → 0.73), JCM Ra2 → 120 k + NFB-window re-derivation (finding 8), the JCM 470 pF bright cap (the "flabby chugs" finding), the AC30 top-boost + finding-5 stack slice (volume between V1/V2, stack correction included), then SD-1/TS tone-network rolloff and the GOLD gain-gang law.** Diagnosis tables live in the three 2026-07-30 agent reports (session); key figures are reproduced in each slice's plan file as it starts.
- **Just landed (2026-07-30): audit finding 7 — the PI tail reference** (docs §42, ADR 014, `docs/work/2026-07-29-pi-tail-reference.md`). `LtpInverter::Config` gains `tailRef`: the tail resistor now sets common-mode rejection and the reference sets standing current, like the real topology. All three amps' PIs idle at the project targets for the first time (plates 79–82 % of B+, 0.59–0.79 mA/triode; AC30 balance 0.550 → **0.912**, Twin **0.9978**); the five finding-7 XFAILs XPASSed and are hard assertions. **The slice grew two heads, both ADR-008-style un-fittings decided by measurement:** the JCM/Twin staging constants had been absorbing the starved-PI gain (`kInterstageScale` 0.25 → 0.16 / 0.16 → 0.107, derived from the measured **closed-loop** power-section gain ratios — the NFB eats half the PI rise; `kFullScaleSecV` 26 → 33 / 24 → 42 because both amps now genuinely reach rated power: JCM 34 → 54 W of 50, Twin 31 → 89 W of 85). **Two audit claims refuted by measurement:** the Twin's tail must stay 22 k (at 10 k the single-ended NFB injection's common-mode half leaks through the tail and the loop *generates* +24 dB of h2 — clean bar fails), and Ra2 = 100 k cannot balance the legs (0.718 measured; 119 k is the optimum, 0.9978). All four valve goldens re-blessed with owner authorization (rat_jcm800 −1.77 dB, sd1_twin_reverb −4.89, muff_twin −4.74, ts_ac30 +0.44; clean120_chorus untouched — the scope check). Owner accepted the Twin's honest −4.9 dB level un-compensated (RAT precedent); the web amp-level drift guard was re-centred on the new measured levels with the decision recorded in the comment.
- **The AC30's "chime" was its PI defect — now a 5-XFAIL ledger and an owner decision.** Balancing the AC30's legs removed the 2nd-harmonic leakage that WAS its mid-volume breakup and chime (h2 −19.8 → −33.0 dBc at VOLUME 0.6; odd harmonics unchanged). Five §23 voicing bars are XFAILs (`finding7-ac30-chime`/`-breakup-onset`/`-breakup-vs-twin`/`-mid-thd`/`-mid-vs-twin`), owned by a future **AC30 gain-structure slice** (the missing top-boost stage + VOLUME re-taper — owner chose this over re-introducing the imbalance or reverting). Watch before touching the JCM PI again: its NFB "meaningful loop" window passes at **5.977 of < 6.0 dB**. ctest is 26 entries again (the AC30 ledger returned); repo XFAILs 15 across 5 ledgers.
- **Still open from the audit:** `docs/audits/2026-07-24-project-audit.md` remains the source of truth. **Four circuit defects are now fixed: finding 15** (the diode ideality factor, docs §36), **finding 6** (valve-amp parameter smoothing, docs §35), **finding 7** (the PI tail reference, docs §42) **and finding 4** (the AC30 static sag saturator → a real dynamic supply, docs §55 — which also sharpened findings 9/10 into a measured, ledgered number: the shared cathode reaches a +8.7 % bias swing where a real AC30 measures +25 %, because the EL84 fit's screen term carries a quarter of the standing draw and does not grow with drive). Remaining physics cluster — findings 5, 8, 9, 10: the AC30's ~37 dB mid notch, `Ra2` (re-measure the sweep post-tailRef; the JCM ratio is 0.703 and its even-harmonic cancellation 8.1 dB), the single-ended plate load line, the screen-current fits. **Finding 16 is CLOSED** (docs §37 the output DC blocker, §49 the series base resistors, §53/ADR 010 the DC-blocking diode caps that ADR 009 correctly predicted were the upstream error).
- **The artifact staleness gate is real now** (docs §31, ADR 004). `web/public/generated/.build-stamp.json` is committed build output: `build-wasm.sh` writes a SHA-256 over the *contents* of the 65 inputs that actually affect `clipper.js`, and `check-artifact.mjs` recomputes it with **no toolchain**, naming the file that changed. This was not theoretical — two PRs each rebuilt `clipper.js`, the merge conflicted on the binary, and taking either side would have shipped an engine holding one of the two fixes while the source held both. **Do NOT hand-write `.build-stamp.json`**, and don't move an emcc flag outside the `STAMP:EMCC-ARGS` markers: both turn the guard into decoration.
- **Re-blessing a golden is now a ritual, not a command** — clean tree, a printed dB table measured against the *previous* goldens, a confirmation `yes |` cannot answer even through a pty, and a justification in `core/tests/goldens/GOLDENS.md` that CI requires. Measured on a deliberately wrong golden: the old path reported **0.00 dB** and blessed it; the new one reports **17.35 dB @ 800 Hz** and writes nothing.
- **§ and ADR numbers are assigned centrally.** Two slices reached for ADR `001`, and two reached for docs `§29`. Ask rather than guess.
- **Also just landed (2026-07-25): `test/assert-real-properties`** — the audit's *systemic* finding, sequenced before the circuit work so the fidelity fixes are measured rather than asserted (docs §29). Tests + `core/CMakeLists.txt` + docs only, **zero DSP change**, goldens untouched. Highlights: **`-UNDEBUG` is no longer behind `if(NOT MSVC)`** (on MSVC the entire 1129-line expectations suite compiled to a no-op `main` that printed its success banner with zero goldens present) and `core/tests/support/AssertsLive.h` makes it a **build error** if `NDEBUG` ever survives in a test TU again · the phase inverter is measured properly for the first time (plate as a **fraction of B+**, standing current from **Ohm's law on the plate load**, and the **leg-gain ratio** — shared across all three amps in `core/tests/support/LtpProbe.h`) · the JCM800 push-pull test's `f(V+v) − f(V−v)` **algebraic identity** is gone · DC offset is asserted **on signal**, with a +0.1 V input-offset case because deleting a coupling cap changes nothing on a clean input · block B's `tol` 2e-5 → **0.0 (bit-identical)** plus a **ragged 100-frame** pass, the only segmentation that can catch a block-size bug · **`OptoTremolo` has a test** (`clipper_opto_tremolo_tests`) · the three amp-swap "no pop" tests land the swap **mid-render** via `ctx.suspend()`/`resume()` · the three perf-smoke tautologies (`performance.now() delta > 0`) now assert real audio · `audio.spec.ts` gets a finiteness guard covering **every** render, channel and sample. Core ctest **24/24**.

- **Also just landed (2026-07-25): `perf/muff-newton-earlyout`** — **audit finding 12 fixed** (docs §34): the Muff cost ~3× more CPU when you were NOT playing. `BjtStage`'s damped Newton had no residual-based exit, so at the parked operating point the backtracking line search was **unsatisfiable by construction** (nothing can strictly decrease a residual already at the floating-point floor) and burned all 30 backtracks every iteration — **31 Ebers-Moll evaluations, 124 `exp` calls, per stage per sample**, to reproduce the answer it already had. A residual early-out (`kNewtonResidualTolA`, a **current in amps**, scaled by the existing `tubeSolverTolScale()`) takes idle from **31.00 → 1.00 evaluations per solve**: silence **6.9 s → 0.50 s** per 10 s of audio (**~13.7×**), silence/signal ratio **2.58–2.87× → 0.23–0.27×** (idle is now *cheaper* than playing), `clipper-bench` **3.9× → 4.9× realtime / 25.4 % → 20.3 %**. Also fixes `dampedNewton` returning `it + 1`, which really did make `lastMaxNewtonIterations()` report **61** against a cap of 60.
- **Finding 12's headline caveat: it is NOT bit-identical, and that is a measured result, not a shortcut.** The pre-fix solver drove the residual to the floating-point floor, so **any** early-out that fires declines refinement it performed — a tolerance sweep against the pre-fix binary shows bit-identity only at 1e-19, where the early-out never fires and the pathology returns. 1e-17 is the tightest value that still fires: worst difference **−127.4 dBFS absolute / −134.1 dB relative**, i.e. **7.4 dB inside the project's own −120 dBFS solver gate** (§25), and 53 femtovolts of node error per solve. The **DC operating-point solve deliberately opts out** (`tol = 0.0`) — it runs once in `prepare()` and seeds the quiescent point every render references. Two new gates pin both directions: `clipper_muff_tests` fails if the early-out stops firing, `clipper_tube_solver_tests` (now covering the Muff) fails if it starts costing accuracy — both confirmed red under perturbation.
- **Two traps from that slice, worth knowing before you write a pedal harness:** `MuffModel`'s **`PARAM_VOLUME` smoother defaults to 0**, so a Muff driven only through `PARAM_SUSTAIN` renders **digital silence** — that made an entire 25-render bit-identity sweep vacuous until an `assert(peak > 0.01)` caught it. And the worst case for solver accuracy was **not** hot DI (−228 dBFS) but a **loud** input (−127 dBFS, 19 dB worse), so a hot-DI-only test would have reported a number 100 dB better than the truth.
- **Newly found, now an XFAIL (`muff-slam-exhausts-newton-cap`):** widening the old ±10 V single-oversampling slam test to **±20 V across every rate × factor** showed **6 of 16 combinations exhaust the 60-iteration cap** (2× at all four base rates; 4× at 88.2 and 96 kHz). Output stays finite and bounded, so it is not the old cascade blow-up, but the solve has not converged there. **Pre-existing** — identical counts measured on the pre-early-out solver. The shipped desktop path (4× at 44.1/48 kHz) converges in 17–18. Its own slice; **do not raise `kMaxNewtonIter`**, that buys iterations rather than convergence.
- **Also just landed (2026-07-25): `fix/valve-amp-smoothing`** — **audit finding 6 fixed** (docs §35). The three flagship valve amps assigned knob values straight to state, so a knob move behind up to 76 dB of gain was a step discontinuity — while both front-ends carried a comment asserting the core already smoothed it. The four knobs the audit measured, its figure → now (0.40 jump, worst of 16 step positions): **AC30 VOLUME 38.9× → 0.98×**, **Twin VOLUME 29.5× → 0.95×**, **JCM800 MASTER 10.3× → 0.96×**, **GAIN 6.8× → 0.90×**. Below 1.0 means the seam step is *smaller than the signal's own steady-state slew*. Worst across every knob on all three amps: **1.11× / 2.07×** against the already-compliant clean amp's 1.05× / 2.84× in the same run. **All five goldens UNCHANGED at 0.00 dB** and no CPU regression (interleaved same-machine A/B: two of three amps *faster*). Core ctest 26 → **27**.
- **Why that slice is bit-identical and not merely close:** `OnePoleSmoother` is now `OnePoleSmootherT<T>` — the float instantiation is byte-for-byte the old class for all eleven existing users, and the valve amps use a new **double** sibling, because they carry pot fractions and post-taper scales as doubles all the way to the multiply. A settled double smoother returns *exactly* the old constant; a float one would perturb the last mantissa bits of a static render, which the goldens and `identical_core_test` forbid. New `settled()` makes a parked smoother a no-op, so an idle tone stack never rebuilds its matrix. **Deferred snap** (first `process()` after `prepare()` snaps every smoother) is what stops the C ABI's prepare-then-set order from ramping every golden.
- **Do not quote §32's absolute bench numbers on new hardware.** The JCM800 measures ~58 % of a stream on the current machine against §32's recorded **53.3 %**, which reads as a regression and is not — `main` measures 58.04–60.25 % in the same session. Absolute columns are machine-dependent; the only defensible CPU claim is an **interleaved same-machine A/B**, which is what §35 records.
- **The XFAIL ratchet (read this before "fixing" a red suite).** `core/tests/support/Xfail.h`: a known-bad property is measured, its real number printed, the finding named — and **an XPASS is a hard failure**. If you fix finding 7/8/16 or the tremolo and the suite goes red with `[XPASS]`, that is the design working: delete the XFAIL and assert the property for real, in the same slice. Never `#if 0` a property or loosen a bound to go green — that is exactly how the starved AC30 phase inverter shipped. Open XFAILs are visible in a plain `ctest` run as `<target>_xfail_ledger ... ***Skipped`; run `./build/<target> --xfail-ledger` for the detail. Currently **6 ledger entries** (the Muff's two came off in docs §53 — finding 16 is closed and the ±20 V slam cap is fixed — the AC30's came BACK in docs §55, the §57 schematic correction opened the Orange's first, `orange-schematic-alias-44k1`, docs §63 opened the Rockerverb's twin of it, and **docs §63.14 CLOSED the Rockerverb's with one shared oversampling domain — which measured WORSE on the OR120, so that one STAYS and is re-owned to the cathodyne**), plus **§70 opened `clipper_drop_tests`'s first and only entry, `drop-triad-spread-at-minus-2` — this slice's OWN 2-cent target, not a perceptual one, with the 5-cent perceptual bar met by 3.75 cents; owned by a frequency-domain shifter**, covering finding **8**, **findings 9/10 via `finding4-ac30-bias-swing-short`** (opened by §55: fixing the sag made the AC30's bias swing measurable against a published number for the first time, and it falls short — see the AC30 entry above), two Medium/DSP items (the opto tremolo's speed sag, control-rate parameter sampling) and one found in-flight by a slice widening its own test (`muff-slam-exhausts-newton-cap`, docs §34).
- **Known gaps (from the audit, not yet actioned):** **the proxy still has no auth and no rate limit** (its own slice; loopback binding is the mitigation until then) · per-triode oversampling costs 7.5 ms of latency on the JCM800 · the AC30's tone stack has a structural ~37 dB mid notch (its "sag" is no longer a static saturator — docs §55) · the SIGNAL path is still NaN-transparent (`OutputLimiter::clamp1`) and `native/src/ClipperEngine` has no reset seam · **`web/playwright.config.ts:34` sets `retries: 2`**, so a fault appearing in under a third of runs is retried away — the last remaining way for a real fault to vanish silently (recommendation in docs §29; deliberately not changed unilaterally).
- **Still vacuous, deliberately deferred:** every tone-stack test compares the discrete MNA against an analytic `H(jω)` **derived from the same netlist** — it validates the discretization but structurally cannot catch a wrong topology or component value, which is exactly how audit finding 5 (the AC30's ~37 dB mid notch) and the JCM800 preamp flatness shipped. Fixing it needs published response curves per amp; that is a research slice.
- **Also just landed:** the audit's **"Test & process integrity"** artifact + goldens holes (docs §29, ADR 004). `web/public/generated/.build-stamp.json` is a new piece of committed build output: `build-wasm.sh` writes a SHA-256 over the *contents* of the 65 inputs that actually affect `clipper.js` (all of `core/src` + `core/include`, the worklet, and the emcc flag region of `build-wasm.sh` itself), and `check-artifact.mjs` recomputes it with **no toolchain** and fails naming the file that changed. This was not theoretical: two PRs each rebuilt `clipper.js`, the merge conflicted on the binary, and taking either side would have shipped an engine holding one of the two fixes while the source held both. `EMSDK_VERSION` is pinned `latest` → **6.0.4**. Separately, re-blessing a golden is now a ritual (clean tree, printed dB table vs the *previous* goldens, a confirmation `yes |` cannot answer even through a pty, a justification recorded in `core/tests/goldens/GOLDENS.md`), and the `--update-goldens` path no longer compares each render against the file it just wrote. Measured on a deliberately wrong golden: the old code reported **0.00 dB** and blessed it; the new report says **17.35 dB @ 800 Hz** and writes nothing.
- **Do NOT hand-write `.build-stamp.json`.** A hand-made stamp is a lie about which sources built the artifact, and it is the only thing standing between a source change and a silently stale engine. If the check fails, rebuild.

- **Also just landed (2026-07-25): `perf/halfband-no-modulo`** — the audit's fidelity-neutral perf item 2, and **bit-identical** (docs §32, ADR 005). `HalfbandFilter.h` indexed its ring as `ring_[(w_ - p + M) % M]` with a runtime `M_`, so the compiler emitted a hardware integer **division per tap** — 64 per output sample on the interpolator's tight stage, 65 on the decimator's, at the *oversampled* rate, for every oversampled pedal and once per triode stage in every valve amp. Now a **doubled ring buffer** (`2*N` floats, every write mirrored `N` apart, unwrapped `ring_[w_ + N - p]` reads, compare-advance) plus parallel tap arrays in the decimator. Measured **2.72× on the oversampler alone** (new `os2x`/`os4x`/`os8x` bench units: 2.90× / 2.72× / 2.65×), 2.2× on SD-1 / Screamer, 1.8× on RAT / Gold, and **the JCM800 from 60.6 % to 53.3 % of one realtime stream**; every non-oversampled control row unmoved. **0 differing bits** out of ~1.4 M samples vs a copy of the old implementation (both filter classes, the full cascade at 1×/2×/4×/8× over ragged block sizes, up *and* down, plus after a NaN-poisoned `reset()`); `--alias-report` identical to the digit; goldens untouched. New ctest target `clipper_halfband_tests` (core ctest 24 → **25**).
- **Two things `perf/halfband-no-modulo` pinned that you must not "tidy":** the tap loop walks `p` **upward** (reading memory backwards) — reversing it into a forward-streaming loop is mathematically identical and changes 38 % of samples by ~1 ULP, which a tolerance test cannot see, so the comparison is a `memcmp`; and **every** write to the ring must mirror both halves, `reset()` included, or stale history reappears one filter length later. No SIMD in that loop without a fidelity argument — vectorising a float reduction reassociates it.
- **New, quantified, still open:** `Oversampler::latencySamples()` **over-reports** the true round-trip delay by 0.5 / 0.75 / 0.875 base samples at 2× / 4× / 8× (measured 63.500 / 71.250 / 75.125 vs reported 64 / 72 / 76). The decimator emits on the *second* sample of each pair, and `(2M) >> (s+1)` cannot express the half. Pre-existing (bit-identity proves it), inaudible (18 µs), errs safe — but it is the number the UI and the plugin report, so fixing it is a plumbing slice. Also observed: docs **§7's `--alias-report` table is stale** (pre-existing, dates from the §11.1 RAT re-voice).
- **Also just landed (2026-07-25): `fix/denormal-guards`** — **audit finding 11** fixed (docs §33, ADR 006). Fidelity-neutral, **goldens untouched**, core ctest 24/24. §25 wrote the anti-denormal policy and applied it to two shared primitives; the tool meant to watch for the rest, `scripts/denormal_bench.cpp`, **only exercised those same two classes**, so it reported a clean ~1.00× cliff for months while a RAT ran **1.97× slower on silence than on a riff**. Per 10 s of silence, silence-time before → after (each now meeting its hardware-FTZ floor): **RAT 647 → 309 ms**, **GOLD 629 → 305 ms**, **AC30 1982 → 1603 ms**, **`Processor` at GAIN 0 23 ms → 4 ms against a 2 ms FTZ floor**. Subnormal output samples emitted **downstream into the rest of the chain**: **GOLD 393 607 → 0**, **SD-1 429 236 → 0**, **Screamer 428 761 → 0**, **`Processor` 427 187 → 0**, AC30 1588 → 2 (a bounded ring-down transient). Guarded: the two output DC blockers' `dcY1_`, the `chowdsp` WDF capacitor in RAT + GOLD, all three valve tone stacks' cap companions (**68.2×** and **18.6×** isolated — the two worst cliffs in the core), the AC30 power section's OT pair, `TwinPreamp::brightS_`, and `Processor`'s hand-rolled smoother. Whole-lineup A/B at knobs min/noon/max: **max |Δ| = 1.0151e-30** (−600 dB), all of it in silent tails; eight of eleven units bit-identical everywhere. New: `Denormal.h` gains `flushDenormalWdfCapacitor` / `isSubnormal(double)` / `maxAbsState`; `denormal_bench` gains a whole-unit section 2; `clipper_denormal_tests` gains block 2, perturbation-checked 7/7.
- **Also just landed (2026-07-25): `fix/diode-ideality`** — the first *circuit* defect off the audit backlog, and a **deliberate tone change** (docs §36, ADR 008). Finding 15: `RatModel`'s WDF clipper had the 1N4148's `Is = 2.52 nA` with its **ideality factor dropped to 1.0** (the SPICE card is `IS=2.52n N=1.752`), so the pair clipped at **0.32–0.43 V instead of 0.6–0.7 V — 5–6 dB low** — while three places in the tree documented ±0.6 V. Now `n = 1.752` everywhere it belongs: measured clipping node **0.381 → 0.678 V** at the shipped default DISTORTION 0.7, and the RAT is **+4.2 to +5.0 dB louder** at realistic input (0.3 V-peak sine: −10.80 → −5.86 dBFS, THD 38.3 % → 36.5 %). **Nothing was re-gained to compensate** and that is the ADR's second decision — clipping onset moves only from DISTORTION 0.24 to 0.32 (THD ≥ 5 %) and the pedal is still at 36 % THD at its default, so the M6.1 "gutless" failure mode does **not** return; what wants its own slice is downstream level staging.
- **The bug had grown a second head: `DiodeClipperADAA::kDefaultVk` was fitted to it.** Its header cited the "~0.33-0.39 V" WDF measurement to justify `Vk = 0.35` — a comparison path calibrated to agree with the circuit's error, so fixing the diode alone would have left `--stage2 adaa` silently comparing two different circuits while still looking plausible. Refitted **by measurement** (least squares in dB over 41 log-spaced drive levels, 0.5–100 V): **0.35 → 0.67** (optimum 0.6659, rms residual 0.605 dB; stale 0.35 measured **5.346 dB** off the corrected curve). New `testAdaaTracksWdf` ties the two constants together through the real model so it cannot recur. **`GoldModel`'s silicon counterfactual** had the identical error (`kSiIdeality` 1.0 → 1.752): its germanium-vs-silicon A/B measured **~1 dB** where a real 1N34A-vs-1N4148 pair is ~6 — now **+5.88…+6.11 dB**. The **germanium side was always right**, so GOLD's own voice is unchanged.
- **`rat_jcm800`'s golden MOVED and was deliberately NOT re-blessed** — `clipper_player_expectations_tests` **fails on `main` until a human blesses it**. Broadband RMS barely moves (−0.15 dB, the JCM800 is already compressing) but the energy is redistributed: **+6.50 dB @ 800 Hz, −5.85 dB @ 1008 Hz, +4.89 dB @ 1600 Hz**. The other four rigs are UNCHANGED (≤ 0.11 dB), which is the scope check. Full per-band table in docs §36; re-bless via `scripts/update-goldens.sh` with a `GOLDENS.md` justification, or don't.
- **Two tests moved for reasons that look wrong at a glance — read §36 before "fixing" them back.** `testFactorOneRegression`'s hardcoded samples were **regenerated** (they are a drift guard, not a property; the check is that they grew by ~5 dB, the amount the ceiling grew). `testGermaniumKnee`'s bounds were **TIGHTENED** (onset 5× → 20×, slope ratio 0.25× → 0.05× — both strictly harder to satisfy) while the *margin* to the measurement widened, because the bug had been making its genuine properties harder to pass and the measured ratios improved by an order of magnitude at the same time (26× → 189×, 0.032 → 0.0063). Both new absolute tests were perturbation-proven: reverting either ideality factor fails them.
- **Reset exports exist but nothing calls them yet** — a worklet watchdog (detect non-finite in `process()`, schedule a declick-bracketed reset) is the follow-up, not a UI button.
- **ADR numbers are assigned centrally.** Two parallel slices both reached for `001`; if you are working a slice, ask rather than guess.
- **Also just landed:** **audit finding 3 fixed** — `CabConvolver` is exact for ANY host block size, not just multiples of 128. It zero-padded a partial block but advanced the FDL by a full partition and discarded the remainder, so the stream stayed misaligned forever; at 100-sample blocks the error EXCEEDED the signal (0.1636 vs a 0.1521 peak). Now feed → maybe-one-block → drain, with the deferral moved out of the FDL indexing and into an output FIFO, so it costs **no** extra latency and the 128-aligned path stays bit-identical (goldens untouched). Measured 0.0 error at every block size from 1 to 4096.
- **Still open on the native side of finding 3:** the *convolver* now handles any block size, but `native/src/ClipperEngine::process` still only chunks when `numFrames > maxBlock_`, so the rest of the native chain still sees raw host block sizes. Its own slice.
- **CI native job — now blocking.** Its `ctest` inherited the core's registered tests (the native CMakeLists pulls the core in) and reported them Not Run because only the two native targets are built, so the job failed on every run with `continue-on-error` hiding it — the load-bearing identical-core test had **never** actually run in CI. Fixed with `-R 'clipper_identical_core|clipper_chain_edit'`; observed green on PRs #11 and #12, so the advisory flag is off and it blocks like every other job. **Owner action outstanding:** add it to the required checks in branch protection.
- **Repo hygiene note:** project history lived on a long-running feature branch for a while and `main` held only a README. That is being corrected — `main` is now the trunk. Do not start new work from anything but `main`.
- **Env note:** **Node 22+** for the web app and tests — `npm run test:server` / `test:history` / `test:scripts` pass a quoted glob to `node --test`, and native glob support landed in Node 22, so on Node 20 they fail with "Could not find …/*.test.mjs". (Documented as 18+ until CI proved otherwise.) CMake ≥ 3.16 and a C++17 compiler for the core; Emscripten only for the WASM rebuild (`scripts/setup-emsdk.sh`), now pinned to **emsdk 6.0.4**. The artifact is byte-reproducible **from any directory as of 2026-07-25** — it was not before. The emcc link is `-O3` with no `-DNDEBUG`, so live `assert()`s bake their absolute `__FILE__` into the WASM, and the committed artifact recorded whichever directory the builder was in (main's briefly embedded an ephemeral agent-worktree path). `-ffile-prefix-map` makes those repo-relative; verified byte-identical across two different build directories. **Asserts still ship inside the audio engine** — removing them is `-DNDEBUG`, a runtime-behaviour change, deliberately not done here (docs §30).

---

- **In flight (2026-08-25): the MESA'S KNOBS DID NOTHING — a SECOND live-parameter path, plus the CONTROL-TYPE pass** (`docs/work/2026-08-25-control-fidelity-and-mesa-params.md`, branch `claude/dual-rectifier-knobs-unresponsive-fxwjw2`). Owner, on a build one week after §71: *"my dual rectifier's knobs don't do anything, it only has one voice, unchanging"*, *"the amp selector is dogfooded, missing amps, don't line up"*, *"give me semitones not a 0-100 knob"*, *"mode should be a switch not a knob"*. **The CORE was cleared FIRST and was never at fault** — the C ABI probe moves every Mesa control (GAIN +47.28 dB, MASTER +115.14, BASS +8.68, RECT −3.12, POWER −2.24; MODE detents 0.1587/0.2001/0.2575/0.0728/0.1474). **NO file under `core/` was touched, so NO golden can move and NO WASM rebuild was needed.** Core **38/38**, native **4/4**, Playwright **85 → 87**, node 15/10/12, electron 20.
- **§71 FIXED ONE PARAMETER PATH AND THERE WERE TWO.** `PluginProcessor::processBlock` calls `engine_.updateParams(p)` every block — an "apply only the changed ones" body **entirely separate** from the `applyParamsToModels()` §71 corrected — and it had **no Mesa branch of any kind**: `jcmGain`/`jcmMaster` reached `jcm_` and `rockerverb_`, `jcmPresence` reached `jcm_`/`ac30_`/`orange_`, bass/middle/treble/reverb reached every voice EXCEPT `mesa_`, and the three switch fields were never compared at all. **The Mesa was parameterized exactly once, at `prepareToPlay`, and every knob move afterwards was dropped** — which is precisely "one voice, unchanging". **The fix is a MERGE, not the missing lines:** one `applyAmpRouting(p, old)` where `old == nullptr` applies every row and otherwise only changed rows. A voice can no longer be added to one path and missed by the other because there is only one path.
- **THE TEST THAT COULD NOT HAVE CAUGHT IT, AND WHY — read before writing the next plugin test.** `amp_voice_test` sets every parameter **BEFORE `prepareToPlay`**, so it only ever exercised the SETUP path. §71 named "the editor has no automated behaviour test" as its follow-up; the real hole was one layer down and cheaper to close. New `testMesaKnobsRespondLive` moves each control **between `processBlock` calls**, the way a host does, and compares against a render that never moved it. **Perturbation P1 (the shipped state) reads `max|d| = 0.000e+00` on ALL NINE controls** — a dropped write is exactly a render bit-identical to not touching the knob, which is the bar shape that catches it.
- **A BAR WRITTEN AT THE WRONG OPERATING POINT FAILED ON CORRECT CODE.** The live-edit test's PRESENCE row first read exactly 0.0000 dB **after** the fix — because the default MODE is RED MODERN, where §69's sheet `mbdr7` opens the power amp's loop and presence is inert **by topology**. Re-probed at MODE 0.25 (Orange NORMAL) it reads +0.31 dB. **A wiring bar must not be stated at an operating point where the CIRCUIT makes the control inert** — the same suite asserts that inertness two tests earlier.
- **THE AMP SELECTOR WAS THE SAME BUG IN TWO MORE PLACES.** `PluginEditor.cpp` built its top-bar combo from its **own five-entry list** against a **seven-choice** parameter, so Rocker Verb and Dual Rectifier were unlistable and — a `ComboBoxAttachment` maps item i to choice i — everything after AC30 selected the wrong amp. Now built from `ClipperAudioProcessor::ampModelChoices()`, the parameter's own array, with a `jassert` against `AMP_MODEL_COUNT`. On the web `AMP_TYPE_LABEL` was `Record<string, string>` and simply **missing `mesa`**, so the menu item rendered **blank**; typing it `Record<AmpType, string>` makes a missing voice a build error. **And `editor_snapshot.cpp`'s voice list was stale at FOUR**, so no visual pass had ever photographed voices 4/5/6 — now seven.
- **THE WEB START PATH HAD THE MIRROR DEFECT.** `startEngine`'s amp-voice if/else chain stopped at `rockerverb`, so **a persisted Mesa rig started the engine on voice 0** while the UI drew a Dual Rectifier: the tone knobs still did something (they reach the Clean 120) and GAIN/MASTER/PRESENCE/MODE/RECT/POWER did nothing, because those ids never reach it. Replaced by the total lookup `engine.setAmpModel(opts.ampType)`. It also never sent the three Mesa switch ids, so they ignored saved state. **`amp.spec.ts` had NO Mesa spec at all** — that absence is why both bugs survived; there is one now.
- **EIGHT DISCRETE PARAMETERS WERE DRAWN AS 0-100 POTS IN BOTH FRONT ENDS.** The Cellar's AMOUNT (9 positions), the Mesa's MODE (5) / RECT (2) / POWER (2), the OR120's F.A.C. (6), and the Lumen / Swirl / Ensemble MODE slots (2 each). **The models quantize all of them and the source comments already said "genuinely DISCRETE" and "a 9-position ROTARY, not a continuous control" — the WIDGET never followed**, so the readout named nothing and nothing stopped the UI emitting a value between clicks. New `web/src/components/Selector.tsx` (`SegmentSwitch` 2-3 states, `RotarySelector` 4+, both emitting only detent centres) and `NeuKnob::setPositions` natively, which sets the slider's INTERVAL so the value cannot land off a detent **through a `SliderAttachment`**, i.e. host automation of the raw lane snaps too. The Mesa's RECT/POWER are real `ModeSwitch` segmented switches natively; MODE stays a rotary because five segments is past legible.
- **"THE PITCH JUMPED BACK UP AT 100" IS OCT+DRY WORKING AS DESIGNED, AND THE FIX WAS LEGIBILITY.** The Cellar's last detent sums the DRY signal with the octave, so the original pitch is audible beside it. Faithful to the reference (§70.2) and the pure octave is the click BEFORE it — but a knob reading "100" cannot say so. It now reads **`OCT+DRY`**. Kept faithful rather than changed; changing it is a departure that wants an ADR.
- **THE SCREENSHOTS CAUGHT THREE THINGS THE BUILD COULD NOT.** The first native switch layout truncated "Silicon" to "Silic…", **collided with the CAB IR chip**, and captioned BOTH switches "MODE" (`ModeSwitch` hardcoded that string; it has `setCaption` now). Fixed by giving the switches their own row at their own preferred width and counting that row in the card's height arithmetic. **The headless loop works in-container** — `cmake -DCLIPPER_BUILD_SNAPSHOT_TOOL=ON` + `xvfb-run` — but JUCE's Linux deps are NOT installed by default here (X11/Xrandr, ALSA, GTK, curl); `apt-get update` first, or the configure fails at `juceaide`.
- **THE CELLAR'S ~36 ms LATENCY IS REPORTED, NOT FIXED, and probing it found a latent defect.** Mean algorithmic delay is `dMin + 0.5*(1+x)*W` = **35.8 ms** at the shipped 65 ms window, which the SOLA span a triad needs is what pins (§70). Measured while sweeping the window: **`PitchShifter` DOES NOT TERMINATE at a 20 ms window** (1 s of audio renders in 425 ms at 65 ms, 454 ms at 40 ms, hangs at 20 ms). Not a shipped configuration, but it means the window is not a constant that can be turned down — **a latency cut is its own slice with its own bars**, and §70 already names the frequency-domain shifter.

## ⚠️ Pre-Session Checklist — Complete Before Writing Any Code

Complete every step in order. Do not proceed to code until the plan file exists and has been confirmed.

**1. Orient**

- [ ] Read the **Current State** section above + the most recent file in `docs/work/` — know what shipped last and what was deferred
- [ ] Identify today's slice from `ROADMAP.md` (or the open findings in `docs/audits/`)
- [ ] If touching a circuit model, read its `docs/DEVELOPMENT.md` section first — it records the component values, the measurements, and *why* a constant is what it is
- [ ] If touching the signal chain, read **both** `web/worklet/clipper-processor.js` and `native/src/ClipperEngine.h` — the chain is implemented twice and parity is a hand-maintained invariant

**2. Clarify**

- [ ] If the task is ambiguous, ask one focused clarifying question before proceeding. Do not make assumptions and build the wrong thing.
- [ ] For a tone change, establish up front how it will be judged — a measurement, a golden, or an explicit "this is a taste call and here is the A/B"

**3. Plan**

- [ ] Write a plan file to `docs/work/YYYY-MM-DD-{slug}.md` using the format below
- [ ] Plan must include a **Manual test steps** section — happy path + at least one edge/failure case
- [ ] For DSP work the plan must name the **measurement** that will show it worked (alias floor, THD, DC offset, latency, CPU, golden diff)
- [ ] Present the plan as a summary to the user and get explicit confirmation before writing code
- [ ] **Do not write a single line of application code until the plan is confirmed**

**4. Branch**

- [ ] Run `git status` to confirm you are NOT on `main`, then `git checkout -b feat/{slug}` (or `fix/` `chore/` `docs/` `perf/`)

---

## Plan File Format

Filename: `docs/work/YYYY-MM-DD-{short-slug}.md`
Example: `docs/work/2026-07-24-nan-parameter-guard.md`

```markdown
# {Feature / Task Name}

**Date:** YYYY-MM-DD
**Branch:** feat/{slug}
**Roadmap item:** {milestone / audit finding / deferred item}

## Goal

One sentence: what does "done" look like for this session?

## Approach

How will this be built? Key technical decisions made upfront.
Call out anything non-obvious or where multiple approaches were considered.
For DSP: state whether this is fidelity-neutral or a deliberate tone change.

## Steps

- [ ] Step 1
- [ ] Step 2
      (Be specific — vague steps lead to vague output)

## How this will be measured

The number that proves it worked, and the tool that produces it
(`clipper-bench`, `clipper-render --alias-report`, a new ctest, a golden diff).
"It sounds fine" is not a measurement.

## Manual test steps

How to verify this works end-to-end after the code is written.
Cover the happy path and at least one failure/edge case.

- [ ] Test step 1 (e.g. plug in, set X, expect Y)
- [ ] Edge case: what happens if …

## Out of scope for this session

Explicitly list anything related but not being done today.

---

<!-- Fill in below during/after the session -->

## What actually happened

(decisions made, approaches changed, surprises)

## Measured results

(the actual numbers, before → after)

## Files created / modified

(list key files)

## Deferred to next session

(anything punted — be specific so next session picks it up cleanly)

## Status

- [ ] In progress
- [ ] Complete
- [ ] Partial — see deferred
```

---

## Post-Session Checklist

Do not close the session without completing these steps:

- [ ] Fill in the "What actually happened", "Measured results", "Files changed", and "Deferred" sections of the plan file
- [ ] Update the **Current State** section of this file (`CLAUDE.md`)
- [ ] Update the relevant `docs/DEVELOPMENT.md` section if behaviour it documents changed
- [ ] Add an ADR to `docs/decisions/` if a significant architectural decision was made
- [ ] **If `core/` or `web/worklet/` changed:** run `bash scripts/build-wasm.sh` and commit the regenerated artifacts (see **The Committed WASM Artifact**)
- [ ] Run the core suite: `cmake -S core -B build -DCMAKE_BUILD_TYPE=Release && cmake --build build -j && ctest --test-dir build --output-on-failure`
- [ ] Run `cd web && npm run build` (this includes `tsc --noEmit`) and `npm test` (Playwright)
- [ ] Run `npm run test:server && npm run test:history && npm run test:scripts` at the root, and `cd electron && npm test`
- [ ] Commit with a conventional commit message and push the branch
- [ ] Open a PR — even for the smallest slice, always go through PR review

---

## Branching Strategy

### Branch model: **trunk-based with short-lived feature branches**

```
main          ← the trunk; always buildable and always green
  └─ feat/<slug>        new features, e.g. feat/gold-overdrive
  └─ fix/<slug>         bug fixes, e.g. fix/nan-parameter-guard
  └─ perf/<slug>        fidelity-neutral performance work
  └─ chore/<slug>       deps, config, tooling
  └─ docs/<slug>        documentation only
```

### Rules

1. **`main` is the trunk and is always buildable.** Core tests, web build and web tests must pass on it.
2. **Never push directly to `main`.** All changes go through a feature branch + PR, no exceptions. There is no "trivial fix" exception.
3. **Branch early, merge fast.** Branches should live < 2 days. Prefer small, focused PRs. Long-running branches are how this project ended up with 100 commits off-trunk.
4. **One slice = one branch = one PR = one session.** Don't bundle a bug fix with a new feature.
5. **Branch names** are lowercase, hyphen-separated: `feat/spring-reverb`, `fix/cab-block-size`.
6. **Commit messages** follow Conventional Commits:
   - `feat:` new user-facing feature (a new pedal, a new amp voice, a UI capability)
   - `fix:` bug fix
   - `perf:` fidelity-neutral speed/latency work — state the measured before → after in the body
   - `refactor:` internal restructure, no behaviour change
   - `chore:` deps, tooling, CI
   - `docs:` documentation only
   - `test:` tests only
   - `style:` formatting, naming — no logic change
   - For a deliberate **tone** change, use `feat:` or `fix:` and say in the body what changed audibly and what measurement backs it.
7. **No force-push to `main`** under any circumstances.

### GitHub branch protection (owner must configure once)

In **Settings → Branches → Add rule** for `main`:

- ✅ Require a pull request before merging
- ✅ Require approvals: 0 (solo project — self-merge is fine, just needs a PR)
- ✅ Require status checks to pass (once CI exists — see **Automated Checks**)
- ✅ Do not allow bypassing the above settings
- ✅ Restrict who can push to matching branches (remove direct-push access)

### Agent-specific rules (Claude Code)

- **Always work on a branch.** Before making any code change: `git checkout -b feat/<slug>`. Even a one-line fix.
- Before starting any task, run `git status` to confirm you are NOT on `main`. If you are, branch first.
- Never run `git push origin main` or `git push --force`. Push your feature branch and open a PR.
- Never commit secrets, `config.json`, or an API key. The Electron key store lives at the OS config path, never in the repo.
- Binary blobs: the committed WASM artifact and the golden `.wav` files are the **only** sanctioned binaries. Don't add others.
- Always run the core `ctest` suite and `cd web && npm run build` before committing.
- Co-author commits with: `Co-Authored-By: Claude <noreply@anthropic.com>`
- Never bypass git hooks with `--no-verify`. If a hook fails, fix the issue.

---

## Automated Checks

> **Status: wired up** (`.github/workflows/ci.yml`, landed 2026-07-25 — this note said "not yet" until then). Runs on every PR to `main` and every push to `main`. **Every job blocks** — the native job's `continue-on-error` came off once it had been observed green (see below). The Post-Session Checklist commands are still the local gate — run them before you push, don't outsource that to CI.
>
> **What CI cannot catch, and you must therefore still think about:** `web/playwright.config.ts` sets `retries: 2`, so a fault appearing in under a third of runs is retried away; and the goldens are `.wav` files no reviewer can read in a diff (the Goldens-changelog job forces a written justification, but cannot check it is *true*). See docs §29.

### The CI pipeline (GitHub Actions)

1. **Core:** `cmake -S core -B build -DCMAKE_BUILD_TYPE=Release && cmake --build build -j && ctest --test-dir build --output-on-failure`
2. **Web:** the artifact staleness gate, then `cd web && npm ci && npm run build` (includes `tsc --noEmit`) then `npx playwright test`
3. **Node suites:** `npm run test:server`, `npm run test:history`, `npm run test:scripts`, `cd electron && npm test`
4. **Artifact staleness:** `node web/scripts/check-artifact.mjs` — a real content-hash check against `web/public/generated/.build-stamp.json`, needing no emsdk. See **The committed WASM artifact**.
5. **Goldens changelog:** a PR that changes a `.wav` under `core/tests/goldens/` must also change `core/tests/goldens/GOLDENS.md`.
6. **Commit messages:** a dependency-free Conventional Commits regex over the PR's first-parent subjects.
7. **Native:** the JUCE `clipper_identical_test` and `clipper_chain_edit_test`, run through a filtered `ctest` (the native build registers the core's tests too, without building them).

### Useful commands

| Command                                        | Purpose                                                       |
| ---------------------------------------------- | ------------------------------------------------------------- |
| `ctest --test-dir build --output-on-failure`   | The core DSP suite. **MEASURED on merged `main` after BOTH 2026-08-25 slices: 34 targets + 7 `_xfail_ledger` entries reported Skipped = 41 ctest entries, 41/41** (M13.6's EQ adds `clipper_eq_tests` AND its ledger; M10.10's Champ adds `clipper_champ_tests` with NONE. A THIRD pair of parallel slices, and as before each was right in its own worktree — the EQ measured 40, the Champ measured 39 against a 38 baseline. Re-measure, do not reconcile by hand.) Before them, the EQ slice alone measured on `claude/roadmap-priorities-kok3vx`: 33 targets + 7 `_xfail_ledger` entries reported Skipped = 40 ctest entries, 40/40** (M13.6 adds `clipper_eq_tests` AND its ledger). Before it, on `feat/drop-pitch-shifter` 2026-08-19: 32 targets + 6 `_xfail_ledger` entries reported Skipped = 38 ctest entries, 38/38** (M13.10 adds `clipper_drop_tests` AND its ledger; §69's Mesa added `clipper_mesa_tests` with none). Before it, merged `main`: 31 + 5 = 36. Earlier: **MEASURED on `feat/univibe` 2026-08-10: 30 targets + 6 `_xfail_ledger` entries reported Skipped = 36 ctest entries, 36/36** (M13.5 adds `clipper_vibe_tests` and registers NO ledger). Before it, on merged `main` 2026-08-10: 29 targets + 6 ledgers = **35** (`ctest -N` is the fast way to check — it lists without running). History: 24 after the GOLD ledger closed with docs §54; 25 when docs §55 re-opened the AC30's with one entry, `finding4-ac30-bias-swing-short`; 26 with `clipper_wah_tests` (§58); 27 with `clipper_orange_tests` (§57); 28 with `clipper_comp_tests` (§59); 29 when the §57 schematic correction opened `clipper_orange_tests`'s first ledger, `orange-schematic-alias-44k1`; 30 with `clipper_ce1_tests` (§62); 31 with `clipper_gate_tests` (§61); 32 with `clipper_delay_tests` (§60); 33 with `clipper_opto_tests` (§64); 34/35 with `clipper_rockerverb_tests` and its ledger `rockerverb-alias-44k1` (§63). **The two parallel 2026-08-01 slices then cancelled out and BOTH of their counts were right in their own worktrees:** §63.14 closed `rockerverb-alias-44k1` (−1, and that slice reported 35 → 34) while §66 opened `clipper_rat_tests`'s FIRST ledger, `rat-level-pot-linear-not-log` (+1, and that slice reported 35 → 36). Merged, 35 − 1 + 1 = **35**, ledgers 6 − 1 + 1 = **6**. **Then two more parallel 2026-08-10 slices landed and again both were right in their own worktrees:** §67 (the Uni-Vibe) added `clipper_vibe_tests` (+1 target, that slice reported 35 → 36) while §68 (the lineup-wide output-pot tapers) closed `clipper_rat_tests`' only ledger entry `rat-level-pot-linear-not-log` (−1 ledger, that slice reported 35 → 34 before the univibe merge). Merged: **30 targets + 5 skipped ledgers = 35 ctest entries**, ledgers **5**. If a Current-State bullet disagrees with this row, it is quoting its own pre-merge baseline — re-measure, don't reconcile by hand. |
| `build/denormal_bench`                          | Silence-vs-signal-vs-hardware-FTZ cost per unit (docs §33). Build: `c++ -std=c++17 -O2 -I core/include scripts/denormal_bench.cpp build/libclipper_dsp.a build/libclipper_core.a -o build/denormal_bench` |
| `./build/<target> --xfail-ledger`               | List that binary's known-bad properties (exits 77 = Skipped)  |
| `build/clipper-bench`                          | Per-unit CPU cost table (× realtime, % of one 48 k stream)     |
| `build/clipper-render --alias-report`           | Alias floor vs oversampling factor                            |
| `build/clipper-render --gen sweep:20:20000:4 …` | Render any gear to a WAV for listening / spectrum             |
| `bash scripts/build-wasm.sh`                    | Rebuild the committed WASM artifact + worklet copy            |
| `bash scripts/update-goldens.sh -m "why"`       | Re-bless the golden renders — **deliberate act only**, and the script enforces it |
| `npm run server`                                | The assistant proxy (needs `ANTHROPIC_API_KEY`)               |

---

## Project Structure

```
core/                     Portable C++17 DSP core — ZERO platform/OS/browser deps
  include/clipper/dsp/    Public headers; each carries the circuit rationale + component values
  src/dsp/                Pedal, amp, modulation and cab models
  src/clipper_c_api.cpp   The C ABI consumed by WASM and any FFI
  tests/                  Plain-assert ctest targets + goldens/ (blessed .wav renders)
  tools/                  render / bench / measure harnesses (native only, never in the WASM build)
web/
  src/                    React UI, audio.ts (engine lifecycle), rig.ts (rig state), assistant/
  worklet/                AudioWorkletProcessor — THE audio thread. Authored here.
  public/generated/       COMMITTED build output: clipper.js (WASM) + a copy of the worklet
  tests/                  Playwright OfflineAudioContext audio verification
server/                   Zero-dependency Node proxy for the assistant (SSE + tool use)
electron/                 Desktop shell — runs the proxy in-process, serves the built web app
native/                   JUCE plugin: Standalone + VST3 + AU, wrapping the same core
  src/ClipperEngine.*     The native chain — mirrors web/worklet/clipper-processor.js
  tests/                  identical_core_test (load-bearing), chain_edit_test
scripts/                  setup-emsdk.sh, build-wasm.sh, update-goldens.sh, native.sh, mac.sh
docs/                     DEVELOPMENT.md (engineering log), work/, decisions/, audits/
```

---

## Key Design Conventions

### The core is portable, and that is load-bearing

`core/` must compile with a plain C++17 compiler and has **zero** platform, OS, browser or Emscripten includes. That is what makes every model offline-testable and every audio bug attributable to the shell rather than the model. Don't reach for a platform header in `core/` — if you need one, the design is wrong.

### The audio thread allocates nothing, locks nothing, and never blocks

No `malloc`/`free`, no locks, no I/O, no unbounded work inside `process()` or anything it calls — in the C++ core, in the worklet, or in the native engine. Scratch buffers are sized in `prepare()`. This rule is currently violated by the cab-swap path (audit finding 2); that is a bug, not a precedent.

**Corollary:** "it happens at the declick zero so it's inaudible" is not a real-time safety argument. Output-zero prevents a step discontinuity; it does nothing about missing the render deadline.

### Every parameter is smoothed, and every topology change is declicked

- **Knob moves** are smoothed in the core with `OnePoleSmoother` (~5–8 ms) and applied **per sample**, not once per block. The chain layer explicitly relies on this and does not bracket knob moves. (The valve amps currently don't do this — audit finding 6.)
- **Topology changes** (chain edit, pedal engage, cab swap, amp voice, amp power) are bracketed by the ~6 ms raised-cosine declick fade, with the swap landing exactly at the output zero. Both the worklet and `ClipperEngine` implement this; keep them in step.
- A parameter that reaches the engine must be **finite**. **Use `clampParam01` / `clampParam` / `paramToInt` from `core/include/clipper/dsp/ParamGuard.h` — never write a clamp by hand.** `v < 0 ? 0 : (v > 1 ? 1 : v)`, `std::clamp(v, 0, 1)` and `Math.min(1, Math.max(0, v))` all pass NaN straight through, and one NaN latches permanently in recursive state (audit finding 1, fixed 2026-07-25 — docs §28, ADR 002). At the C ABI and the worklet boundary a non-finite value is **rejected outright**, not clamped.
- **Every unit has a `reset()`, and it must never re-solve.** `reset()` clears recursive state and re-parks at the *cached* DC operating point (`TriodeStage`/`BjtStage` `cachePark()`, the power amps' `parkState()`). `prepare()` is not a recovery path — it settles ~50 k silent samples per tube stage (87.6 ms for a `Jcm800Amp`). If you add recursive state to a model, add it to that model's `reset()` **and** to the park snapshot, or `clipper_nan_guard_tests` block C will fail.

### Denormals: guard every recursive state **whose rest value is zero**

`core/include/clipper/dsp/Denormal.h` is the policy. **WASM has no flush-to-zero at all** — the runtime cannot be asked to flush — so a state that asymptotes toward zero sticks in the subnormal range forever and becomes a permanent audio-thread denormal generator. Every recursive accumulator (filter state, smoother value, feedback node, cap voltage) gets `flushDenormal`, in both the `float` and `double` overloads as appropriate. The guard only acts below −600 dB, so it is bit-transparent.

**Refined by audit finding 11 (fixed 2026-07-25 — docs §33, ADR 006). Read this before adding or removing a flush:**

- **The scope rule:** guard a state iff its value **at rest (silent input) can be zero**. A state that rests at a **nonzero DC operating point** — a cathode cap at its bias voltage, a B+ rail, a screen node, a sag envelope at idle draw, a Newton warm start at 100–300 V — can never be subnormal, and a guard there is unreachable code in the hottest loops in the codebase. Those sites get a **comment naming the measured resting value** instead. `TriodeStage.cpp` deliberately has no flush and does not include `Denormal.h`: `vCc_` measures 8.15e-4 V after 20 s of silence (the audit expected it to decay to zero — it does not).
- **Measure which, don't guess.** The AC30's cliff was in the OT bandwidth states, *not* the TOP CUT states that look like the obvious candidates; the JCM800 and Twin power sections had **no** cliff at all. Only bisection showed that. A flush kept where it cannot fire must be **labelled a guard-rail**, not cited as a fix.
- **To tell a denormal cost from any other cost**, compare a unit's silence time against **its own silence time with hardware FTZ+DAZ forced on** — never against its signal time. The Muff is 3.01× dearer on silence with *zero* denormal problem (`BjtStage`'s Newton iterates more near the quiescent point); its hwFTZ column shows the same 3.01×. `build/denormal_bench` section 2 does this per unit and prints both.
- **A double subnormal is invisible in the audio** (`float(1e-310)` is exactly `0.0f`), so an output-only test cannot see it — that is how this survived a green suite *and* a benchmark reporting a clean 1.00× cliff, because the benchmark only covered the two classes already fixed. Classes with zero-resting `double` state expose `double maxAbsRestingState() const` and the tests assert it is **exactly 0.0** after a silent tail. Don't add one to a class with no zero-resting state — the assertion would have no teeth.
- **`chowdsp` WDF capacitor state** is guarded via `flushDenormalWdfCapacitor()`, which reaches the library's private `z` through its own public API (`wdf.a` mirrors it; `incident(0)` zeroes both) — no fork of the SHA-pinned dependency. Call it **after** reading the sample's output voltage, never before.

### Oversampling

4× is the measured default for every nonlinear stage (2× measures at ~−21 dB alias floor, 4× at ~−104 dB). Nonlinearities go *inside* the oversampled domain; linear filters and volume networks stay at base rate. Prefer **one** oversampling domain around a cascade over one per stage — per-stage oversampling stacks group delay and repeats band-limiting (audit performance item 1).

The halfband filters under all of it (`HalfbandFilter.h`) are the hottest loop in the project and are held to a **bit-identical** bar: performance work there must produce `0` differing bits against the previous implementation (`clipper_halfband_tests` keeps a copy of it), which rules out SIMD, `-ffast-math`, and any reordering of the tap sum — float addition is not associative (docs §32, ADR 005). Their delay line is a **doubled** ring buffer; every write must update both halves, `reset()` included.

### The chain exists twice — keep parity

The rig graph is implemented in `web/worklet/clipper-processor.js` (JS) and `native/src/ClipperEngine.cpp` (C++). They must stay behaviourally identical: same order, same declick discipline, same latency accounting. `native/tests/identical_core_test.cpp` proves the *plugin* matches a hand-built core chain, but nothing currently proves *web* matches *native* — so when you change one, change the other in the same slice and say so in the PR body.

### The committed WASM artifact

`web/public/generated/clipper.js`, `web/public/generated/clipper-processor.js` and `web/public/generated/.build-stamp.json` are **committed build output**, so a `git pull` updates the engine without an Emscripten toolchain. The contract: **if you change `core/` or `web/worklet/`, run `bash scripts/build-wasm.sh` and commit all three regenerated files in the same commit.** A stale artifact means new UI bound to an old engine — knobs that do nothing.

This is now **enforced** (docs §29.1, ADR 004). `check-artifact.mjs` — wired into `prebuild`, `npm test` and CI — recomputes a SHA-256 over the contents of the 65 inputs that affect the artifact and fails naming the file that changed. What it covers: everything under `core/src/` and `core/include/`, `web/worklet/clipper-processor.js`, and the emcc flag region of `scripts/build-wasm.sh` (hashed *out of the script*, between its `STAMP:EMCC-ARGS` markers — don't move a flag outside them). What it deliberately doesn't: `core/tools/`, `core/tests/` (measured: zero files from either are in the artifact's compile closure) and the pinned `chowdsp_wdf` SHA in `core/CMakeLists.txt` (a known gap). The served worklet is additionally compared **byte-for-byte** with its source, because `build-wasm.sh` copies it with `cp`. It all runs without emsdk, by design.

### Measure, don't assert

This is the project's hardest-won convention, and the audit found it is the one most often skipped. A test must assert a **player-observable property**, not an identity:

- Don't compare the implementation against an analytic reference **derived from the same netlist** — that validates the discretization (which is fine) and can never catch a wrong topology or a wrong component value.
- Don't assert an algebraic tautology (`f(v) − f(−v)` is odd) and call it a test of the amp.
- Don't assert `performance.now() delta > 0` and call it a perf test.
- Don't check DC offset on **silent** input only — asymmetric clipping produces DC on *signal*.
- For a "no click" test, land the change **mid-render** (`ctx.suspend()` / `resume()`), not before rendering starts — otherwise an entirely removed declick still passes.
- Prefer an absolute reference where one exists: a published curve, a datasheet limit, an insertion loss at 1 kHz, a measured unit.

`core/tests/test_player_expectations.cpp` is the model to follow — it drives gear the way a player does (default knobs, min knobs, realistic levels, realistic noise, the worklet's exact calling conventions) and pins what they must hear.

**Shared test-support headers exist to make this convention enforceable rather than aspirational (docs §29 — read it before writing a test):**

- **`core/tests/support/AssertsLive.h`** — include it from every new test `.cpp`, and call `clipper::test::requireAssertsLive()` first in `main`. It is a **compile error** if `NDEBUG` reaches a test translation unit, because `assert()` would then compile away and the whole binary would pass unconditionally. Register new targets with `clipper_add_test_flags()`, never by hand-rolling the flags.
- **`core/tests/support/Xfail.h`** — the ONLY sanctioned way to leave a correct-but-currently-failing assertion in the tree. `expectXfail(measuredProperty, decl, detail)` prints the real number and names the finding; `reportXfails()` is the return value of `main`. **An XPASS fails the suite**, so a fixed defect cannot leave a stale XFAIL behind. Add `clipper_add_xfail_ledger(<target>)` in CMake so it shows as `***Skipped` in a plain `ctest` run. Do **not** `#if 0` a property, and do **not** loosen a bound to go green.
- **`core/tests/support/LtpProbe.h`** / **`DcOffset.h`** — the phase-inverter and DC-offset properties, defined once so the three amps and four pedals cannot drift on what "healthy" means. Extend these rather than re-deriving a bar per file.
- **`web/tests/support/finite-output.ts`** — two lines in a `test.beforeEach` and every offline render in that spec is scanned for non-finite samples, in every channel. Adopt it in any spec that renders audio.

And when you rewrite a test, **prove it has teeth**: perturb the constant or topology it names in a scratch copy, confirm it fails, revert, and record the perturbation in the PR body. A rewritten test that still cannot fail has not been fixed. (Perturbation harnesses must `touch` the file after both patch and restore — restoring a backup preserves the backup's mtime, `make` skips the rebuild, and you measure stale code. This produced one false reading in the §29 slice.)

### Goldens

Blessed renders live in `core/tests/goldens/`. `scripts/update-goldens.sh` re-blesses them and is a **deliberate act only**: re-blessing is how a regression becomes canon, and a reviewer cannot see the drift in a `.wav` diff. The script now enforces that rather than asking nicely — clean working tree, a printed per-golden dB table measured against the *previous* goldens, a confirmation typed at a terminal (`/dev/tty`, so `yes |` and CI cannot answer it), and a justification appended to `core/tests/goldens/GOLDENS.md` and staged with the goldens. CI fails a PR that changes a `.wav` there without a changelog entry. Say in the PR body what changed audibly and why it is correct, too.

Do not reach for `clipper_player_expectations_tests --update-goldens` directly — it is what the script drives, and using it raw skips every one of those gates. `--golden-report` measures without writing, which is the safe way to see where you stand.

### UI conventions

- **Design tokens** live in `web/src/styles/tokens.css`. Never hardcode a colour — it must work in both themes or come from a token. Note that `.pedal` pins its chassis to the dark tokens on every theme; anything painted on that chassis must pin its accent to match (audit UI finding).
- **Knobs** are the most numerous control in the app: vertical drag, pointer capture, double-click to default, keyboard operable with a visible focus ring, and `role="slider"` with live `aria-valuenow`/`aria-valuetext`.
- **Never update React state at pointer-move rate** from the audio thread or a drag. Meters and needles are driven imperatively from a ref + rAF (`Tuner.tsx` is the pattern); persistence is debounced, not written per move.
- **Destructive actions** (remove pedal, swap pedal) need an undo path. There is none today — don't add more destructive surface without one.
- The native editor (`ClipperLookAndFeel`) is the light-bench sibling of the web UI. Widgets should derive from `juce::Button`/`juce::Slider` so keyboard operability and accessibility come for free.

### Assistant

- Tool arguments are **allowlisted and clamped** before they reach the engine (`web/src/assistant/tools.ts`, re-validated in `App.tsx`). This is the best-defended surface in the codebase — keep it that way. Add a tool only with an explicit param allowlist and a range clamp that rejects non-finite values.
- The system prompt is cached via `cache_control` on the stable block; volatile rig state goes in the user turn so it never breaks the cache prefix. Don't move rig JSON into the system block.
- `thinking` is sent explicitly. Don't add `temperature`/`top_p`/`top_k`/`budget_tokens` — they are rejected.
- Keep the tool surface small and typed. The assistant gets smarter through prompting and rig-state context, not more tools.

---

## ADR Format

When a slice makes a significant architectural decision, record it at `docs/decisions/NNN-{title}.md`:

```markdown
# ADR NNN: {Title}

Date: YYYY-MM-DD
Status: Accepted

## Context

Why did this decision need to be made?

## Decision

What was decided?

## Consequences

What are the trade-offs? What does this make easier or harder?
```

Circuit-modelling decisions belong here too — especially a deliberate departure from the real circuit. Record *what* was simplified, *why*, and *what it costs audibly*, so the next session doesn't "fix" it back or calibrate another constant against it.

---

## Environment Variables

| Variable            | Required | Description                                                             |
| ------------------- | -------- | ----------------------------------------------------------------------- |
| `ANTHROPIC_API_KEY` | ✅       | Claude API key — **server-side only**, never shipped to the browser      |
| `MODEL`             | optional | Override the assistant model id                                         |
| `MAX_TOKENS`        | optional | Override the response cap                                               |
| `PORT`              | optional | Proxy port (default 8787; `0` = ephemeral)                              |
| `HOST`              | optional | Proxy bind address (default `127.0.0.1`). The proxy is an unauthenticated relay to your key — only widen this on a network you trust |
| `MOCK`              | optional | Serve canned assistant responses — no API key, no network               |
| `EMSDK_VERSION`     | optional | Emscripten version for `scripts/setup-emsdk.sh`. **Pinned: `6.0.4`.** `latest` made the committed artifact irreproducible; if you bump it, rebuild the artifact in the same slice |

The Electron app stores the key in the OS user-config directory at mode `0600`, resolved by `electron/config.mjs`. It is never written into the repo and never reaches the renderer.

---

## Deferred / Post-v1.1

- **The 2026-07-24 audit backlog** — `docs/audits/2026-07-24-project-audit.md` is the ordered list. Its own suggested order: the three shipping-blockers (findings 1 and 2 done 2026-07-25; finding 3 remains), then bind the proxy to loopback, then stand up CI *before* the circuit work (so fidelity changes are measured rather than asserted), then the AC30 sag + stack topology + the shared PI tail reference.
- **Get the cab IR spectra off the audio thread properly** — `amp_prepare_cab_*` still runs on the audio *rendering* thread (that is where `onmessage` lives). The real fix computes the partitioned spectra on the **main** thread and copies them into the inactive pair; needs an ABI exposing the partition layout (or a main-thread WASM instance used purely as an IR compiler). Docs §29.
- **Wire up the reset exports** — `*_reset` exists in the C ABI but nothing calls it. Wanted: a worklet watchdog that notices non-finite output in `process()` and schedules a declick-bracketed reset, and/or an explicit affordance. Also `ClipperEngine::reset()` for native parity.
- **Native tuner** — display-only pedal; needs a pitch tap + needle widget in JUCE. Web-only for now (a fake one would be worse than none).
- **Duplicate pedal instances** — the native engine is one-instance-per-type (`kMaxChain = PEDAL_TYPE_COUNT`), so a Screamer into a Screamer works on the web and not in the plugin. Options written up in `docs/DEVELOPMENT.md` → **Duplicate pedal instances**.
- **Native dark theme** — SHIPPED in visual pass 3 (2026-07-31): both token contexts, OS-following with an Auto/Light/Dark override persisted outside the APVTS. What remains is a **Linux** OS-theme reader — JUCE 8.0.4's `Desktop::isDarkModeActive()` has no X11 implementation, so Auto is light there.
- **The Rockerverb's CLEAN channel** and its footswitch (docs §63.11). The dirty channel
  shipped 2026-08-01 from a real netlist; no netlist for the clean channel was reachable,
  and §57's rule applies to inventing a whole channel too. Also open on that voice: the
  shared-oversampling-domain slice that owns BOTH `rockerverb-alias-44k1` and
  `orange-schematic-alias-44k1`, and `TriodeStage` gaining independent input/output
  coupling configs so a NON-uniform valve cascade can carry its grid blocking (§63.3).
- **CLAP** plugin format.
- **Presets & sharing** — the rig-state JSON is already the format; needs named slots and an undo ring.
- ~~**M13.3 the optical (LA-2A-style) compressor**~~ — **SHIPPED 2026-08-01 (docs §64, ADR 025)**,
  and it closed ADR 019's seam question in the negative on BOTH halves: it is not a config of
  `CompressorEngine` (one of eight structs applies) and not a consumer of `SidechainDetector` (that
  pedal has no envelope capacitor — the EL panel is the rectifier and the photocell is the
  integrator). Measured, not argued; the substitution was built and run. **`CompressorEngine` now has
  exactly one consumer.** Do not add a `kOpticalCell` case to `applyGainCell()` in a later slice —
  the seam was checked and it is not there.
- ~~**M13.5's Uni-Vibe is `OptoCell`'s named consumer**~~ — **SHIPPED 2026-08-10 (docs §67,
  ADR 026), and the instruction was followed: `OptoCell` gained ZERO new fields and ZERO new
  methods, and the lamp came out as its own component (`core/include/clipper/dsp/LampDrive.h`).**
  The seam is `elExponent = 1.0` — the existing field with a degenerate exponent, so the quantity
  handed to the cell IS the light. The header's stated reason turned out to be stronger than
  written: a lamp's light is a function of a STATE, not of the drive (measured 21.43x different
  light from the same 0.66 V after different histories), which no memoryless `lightSourceKind` case
  could have expressed. **Do not re-open it by adding one.**
- ~~Unify the wah's envelope follower with the shared detector~~ — **CLOSED 2026-08-01 (docs §58.8,
  ADR 023): they are different circuits and the wah keeps its own.** `SidechainDetector` is a
  THRESHOLD detector (1.95 dB of open-loop proportional range on the compressor's own component
  values, 2.38 on the gate's, against **19.09 dB** for the wah's one-pole); both existing consumers
  want that steepness, and a wah — having no gain to reduce — is feed-forward by necessity and needs
  the opposite. Measured, not argued: the substitution was built and every §58.6 AUTO number
  regressed. **Do not re-open it by adding a `rectifierKind` field.**
- **BD-2 Blues Driver**, **DD-3 delay** (new DSP family: delay lines).
- **Riff integration** — Clipper's rig as riff's practice-tone engine. The assistant patterns already converge (both grew an "applied chip" chat UI).

---

## Things Not To Do

- Don't start writing code before the plan file exists and is confirmed
- Don't push directly to `main` or force-push — ever
- Don't bundle multiple concerns into one branch/PR — one slice per PR
- Don't skip the PR step — every slice gets a PR, no matter how small
- Don't allocate, lock, or do unbounded work on the audio thread — and don't accept "it's at the declick zero" as a justification
- Don't let an unsmoothed parameter or a non-finite value reach the engine
- Don't add a recursive state whose rest value is zero without a `flushDenormal` guard — WASM has no FTZ. Conversely, don't guard a state that rests at a nonzero DC operating point; comment it with the measured resting value instead (docs §33, ADR 006)
- Don't judge a denormal cost by the silence/signal time ratio — compare against hardware FTZ on the same unit, or you will "fix" a Newton-iteration cost with a flush
- Don't change `core/` or `web/worklet/` without rebuilding and committing the WASM artifact
- Don't change the chain in one front-end without changing the other
- Don't write a test that asserts an identity, a tautology, or the implementation against a reference derived from the same code
- Don't `#if 0` a failing property, and don't loosen a bound to go green — use `expectXfail` from `core/tests/support/Xfail.h` so the defect is measured, printed and named. An **XPASS is a hard failure**: if fixing a defect turns the suite red, delete the XFAIL in the same slice rather than reverting the assertion
- Don't add a test target without `clipper_add_test_flags()` — asserts must be live on every platform, and `support/AssertsLive.h` will fail the build if they are not
- Don't re-bless goldens to make a failing test pass — that is how a regression becomes canon
- Don't hand-write or hand-patch `web/public/generated/.build-stamp.json`, and don't move an emcc flag outside `build-wasm.sh`'s `STAMP:EMCC-ARGS` markers — both turn the staleness guard into decoration
- Don't calibrate a new constant to compensate for a suspected error elsewhere; find the error (this is how `kFullScaleSecV` ended up absorbing two separate factor-of-2 mistakes)
- Don't claim a performance win is fidelity-neutral without a measurement showing it
- Don't hardcode colours, and don't paint a light-theme accent on the dark pedal chassis
- Don't update React state at pointer-move or meter rate
- Don't ship dead UI (controls wired to nothing, or a knob whose top half does nothing)
- Don't commit secrets, `config.json`, or binary blobs other than the sanctioned WASM artifact and goldens
